## 1. Slice A — linear grammar, v0 cleaner, tool caps (owns `src/services/links.py`, `src/services/embeddings.py`, `src/mcp_server/tools.py`, `src/services/vault.py`, `src/config.py`, `docs/architecture/vault-tools.md`; new tests `tests/test_asvs_link_grammar.py`, `tests/test_asvs_v0_cleaner.py`, `tests/test_asvs_tool_caps.py`)

- [ ] 1.1 `links.py`: close every class in `_WIKILINK_RE` (target/anchor/alias exclude `[`/`]`, possessive) and `_MDLINK_RE` (text excludes `[`; both href alternatives bounded `{1,2048}?`); update the grammar comment with the rules and the accepted differences.
- [ ] 1.2 `tools.py`: apply the identical rules to `_WIKILINK_REWRITE_RE` and `_MDLINK_REWRITE_RE`; keep the two pre-existing divergences (no `<href>` form; anchor crosses newlines) untouched and recorded in the test.
- [ ] 1.3 `config.py`: add `MAX_LINKS_PER_NOTE = 10_000` and `MAX_LIST_PATTERN_CHARS = 1024`. `links.py`: `extract_links(content, *, max_links=None)` returning links merged by `position` and cut to the first N in document order, plus a way for the caller to learn it was truncated (e.g. `extract_links_bounded` returning `(links, truncated)`); existing callers unchanged.
- [ ] 1.4 `tools.py` `move_note`: dispatch the rewrite computation through `asyncio.to_thread`.
- [ ] 1.5 `embeddings.py`: reimplement `_v0_clean` as a `\n`-only line scanner with the semantics in design D2; move the two v0 regexes into `tests/test_asvs_v0_cleaner.py` as the oracle; `_EXTRACTION_CLEANERS` gains version 2 bound to the v1 function (the `CURRENT_EXTRACTION_VERSION` bump itself is Slice B's).
- [ ] 1.6 `vault.py` `list_dir`: raise `ValueError` naming `MAX_LIST_PATTERN_CHARS` for an over-long pattern before `validate_visible_path`; `tools.py` `list_files_impl` needs no change beyond confirming the message reaches the caller.
- [ ] 1.7 `tools.py` `write_file`: for a `.md` destination (case-insensitive) refuse decoded content over `min(MAX_NOTE_BYTES, settings.max_file_write_bytes)`, naming the limit that applied. `import_from_url`: pass the same `.md`-aware cap as `max_bytes`.
- [ ] 1.8 Tests (`tests/test_asvs_link_grammar.py`): ratio benchmarks (n = 512 KiB vs 2n; ratio < 4; generous ceiling) for all four regexes over `[[`, `]]`, `[[a`, `[[a#`, `[[a|`, `[a](`, `[a](x`; the accepted-differences test (bracketed alias/anchor → no row; `[a[b](x.md)` → `link_text` `[b](x.md)`; href > 2,048 → no row; `[t](Foo [draft].md)` → row); the extraction-vs-rewrite agreement corpus with the two known gaps recorded; document-order cap test for `extract_links(max_links=…)`; `move_note` rewrite dispatched through `to_thread` (monkeypatch assertion).
- [ ] 1.9 Tests (`tests/test_asvs_v0_cleaner.py`): differential property test over the generator classes in design D2 plus every existing v0/v1 fixture; ratio benchmark on 160 KB vs 320 KB of ```` ```x\n ````.
- [ ] 1.10 Tests (`tests/test_asvs_tool_caps.py`): `list_files` 1,024 accepted / 1,025 refused before folder validation (non-existent folder still yields the pattern error); `write_file` `.md` at `MAX_NOTE_BYTES + 1` refused naming `MAX_NOTE_BYTES`, `.pdf` between the caps accepted, `.md` with `MAX_FILE_WRITE_BYTES` lowered refused naming `MAX_FILE_WRITE_BYTES`; `import_from_url` `.md` over the cap refused.
- [ ] 1.11 Docs: `docs/architecture/vault-tools.md` — grammar rules and accepted differences, rewrite/extraction parity rule with the two known gaps, `list_files` pattern cap, `write_file`/`import_from_url` `.md` cap parity.

## 2. Slice C — upload session scope (owns `src/transfer/routes.py`, `src/services/transfer.py`, `src/database.py`, `docs/architecture/file-transfer.md`; new tests in `tests/test_transfer_service.py`, `tests/test_transfer_routes.py`, `tests/integration/test_transfer_pg.py`). Runs in parallel with Slice A — no shared files.

- [ ] 2.1 `routes.py` `upload()`: phase-1 session performs claim, `resolve_identity_ok`, `resolve_root_ok`, `_path_ok` and `check_publication_support`, commits, and exits before any wait or body read; the claimed row is used detached; capture `token_id` for later handlers.
- [ ] 2.2 Each `release_claim`/`consume` call site after phase 1 opens its own short `async_session()`; the publish gate keeps its own session and no longer depends on an outer one.
- [ ] 2.3 `transfer.py` `stream_to_vault`: add `slot_timeout: float = 30.0`; acquire the existing semaphore (same place, `~:1246`) in ≤ 1 s slices recomputing `_deadline_remaining` each slice; on a wait that ends without a slot re-check the deadline — overrun → existing `Timeout`/consumed path; otherwise raise new `QueueTimeout(TransferError)`. `import_from_url` keeps the default.
- [ ] 2.4 `routes.py`: map `QueueTimeout` → 503 with `Retry-After: 5` and `release_claim`; `Timeout` before the body keeps the 408/`consumed` path. Apply the `.md`-aware cap: `max_bytes = min(MAX_NOTE_BYTES, settings.max_file_write_bytes)` when `row.path` ends in `.md` (case-insensitive).
- [ ] 2.5 `database.py`: `pool_timeout=30` explicit with a comment that it is the library default written down. No idle-transaction timeout (design Non-Goals).
- [ ] 2.6 Tests: unit (`tests/test_transfer_service.py`) — slot wait slices recompute the wall-clock deadline (patch `now_utc`), 408 precedence when the deadline elapses during the wait, `QueueTimeout` after 30 s with deadline remaining, semaphore cleared between cases; route (`tests/test_transfer_routes.py`) — `QueueTimeout` → 503 + `Retry-After: 5` + `pending` + no staged bytes, deadline-already-overrun-before-wait via patched `now_utc` → 408 + `consumed`, `.md` upload 413 at cap+1 with claim released, every token attribute read after phase 1 is a loaded plain column (assert no lazy load with the session closed); integration (`tests/integration/test_transfer_pg.py`, `wired` fixture) — with `TRANSFER_MAX_CONCURRENT_UPLOADS` streams stalled and one more queued, `engine.pool.checkedout() == 0` and an unrelated session connects immediately; pool-exhaustion raises after `pool_timeout`. Full existing transfer suites pass.
- [ ] 2.7 Docs: `docs/architecture/file-transfer.md` — "no session across a wait or a stream" rule with the `download_file` precedent, the sliced wall-clock slot wait and why it is not a single `wait_for`, the 408-before-503 precedence and why 503 releases rather than consumes, the `.md` cap.

## 3. Slice B — bounded indexing and durable truncation (owns `src/services/indexer.py`, `src/models/db.py`, `alembic/versions/022_*.py`, `docs/architecture/indexing-and-embeddings.md`, the `get_links` result in `src/mcp_server/tools.py` and `src/mcp_server/server.py` docstring; new tests `tests/test_asvs_indexer_bounds.py`, `tests/integration/test_asvs_links_truncated_pg.py`). **Starts after Slice A is merged** (consumes `MAX_LINKS_PER_NOTE`, the bounded extractor, and cleaner version 2).

- [ ] 3.1 Migration 022: `notes_metadata.links_truncated BOOLEAN NOT NULL DEFAULT FALSE`; model column; `make test-schema` green; `alembic check` clean.
- [ ] 3.2 `indexer.py` changed-path link rebuild: extract via `asyncio.to_thread` using the bounded extractor; insert each note's rows inside the loop (≤ 1000-row batches); set/clear `links_truncated` per note; on truncation emit one ERROR line (path, cap, count) and do **not** append to `skips`. Same treatment for the one-shot backfill if it accumulates across notes.
- [ ] 3.3 `indexer.py`: `extract_tags` dispatched through `to_thread` where it runs in the scan; `CURRENT_EXTRACTION_VERSION = 2` with the "bump in the same commit as any grammar change" comment extended to the link grammar.
- [ ] 3.4 `tools.py` `get_links_impl`: include `truncated` from the note row; `server.py` docstring for `get_links` mentions the field.
- [ ] 3.5 Update the A.7a certification docstring/`_format_skips` comment with the capped-note carve-out.
- [ ] 3.6 Tests (`tests/test_asvs_indexer_bounds.py`): instrumented insert path shows peak buffered rows ≤ one note + one batch across N link-heavy changed notes; over-cap note → exactly the cap persisted in document order, `links_truncated` true, ERROR logged, not in `skips`, re-derive certifies; marker cleared after editing under the cap; extraction and tag dispatch through `to_thread` (monkeypatch assertion); version-2 stamping without re-embedding (cleaned-output comparison equal). Integration (`tests/integration/test_asvs_links_truncated_pg.py`): migration applied, `get_links` returns `truncated: true` for a capped note.
- [ ] 3.7 Docs: `docs/architecture/indexing-and-embeddings.md` — link cap as a declared degradation (mirror the keyword-retreat paragraph), per-note flush and the unchanged body buffer (residual), off-loop extraction, the v0 scanner-with-oracle rule (`\n`-only, Unicode `\s` closers), and the version-2 bump rationale.

## 4. Integration

- [ ] 4.1 Merge A and C, run the full suite, `make audit`; then merge B, run the full suite again, `make test-schema`, `make db-check`.
- [ ] 4.2 `openspec validate --strict`; `openspec-verifier` pass; adversarial review of the merged diff (Codex only if no other Codex thread is running, otherwise a Claude adversarial verifier — say which in the report).
- [ ] 4.3 `make deploy`; confirm `alembic check` clean; wait for the version-2 re-extraction pass to complete (`indexer_runs`, `move_note(rewrite_links=True)` no longer refuses).
- [ ] 4.4 Post-deploy end-to-end exercise via the live MCP tools: `list_files` with a 2,000-char pattern (refused naming the limit); `create_note` with a 1 MiB `[[a]] ` body, then `get_links` (10,000 rows, `truncated: true`, ERROR line in the ops-health buffer, an unrelated `read_note` served during the pass); `write_file` of an 11 MiB `.md` (refused); `request_upload` for a `.md` and stream 11 MiB (413); one upload while a second is queued (health and an unrelated tool call succeed throughout). Delete the test notes afterwards.
- [ ] 4.5 Archive the change; PR with `Closes #180`, `Closes #203`, `Closes #204`, `Closes #208`; update the vault report's issue rows and the accepted residual on #203.
