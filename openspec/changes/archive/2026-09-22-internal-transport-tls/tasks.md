# Tasks — internal-transport-tls (#184, #185 application half)

Two slices with one shared new module. Slice A owns the database hop, Slice B the embedding hop; `src/services/transport_security.py` is **created by Slice A** with the database helpers, and Slice B appends its section (embedding URL classification, client factory, embedding report) after A merges — or the two run sequentially. `src/config.py` is touched by both in **separate, named regions** (A: a `DATABASE_SSL_*` block directly under `database_url`; B: `embedding_allow_plaintext` / `embedding_ca_file` directly under `openai_embedding_model`, and its validator after `_validate_provider_credentials`). `src/main.py`'s lifespan belongs to A; B's startup report call is a one-line seam the supervisor wires at merge (6.2).

**Base check for every subagent brief.** Confirm `openspec/changes/internal-transport-tls/proposal.md` and `openspec/changes/archive/2026-09-22-asvs-edge-hardening/` both exist. If not, stop and report.

## 0. Spec review before any code

- [x] 0.1 Commit this proposal and open the coordination issue/PR (Codex reads the committed tree). (Done: proposal committed; coordination via #184/#185 and PRs #284/#285.)
- [x] 0.2 **Owner decision on D6** — resolved 2026-09-22: refuse by default (`EMBEDDING_ALLOW_PLAINTEXT=false`); the owner adds the override to the production deploy `.env` (7.1). #184/#185 stay open until the infrastructure half lands.
- [x] 0.3 **Codex reviews the proposal before implementation**, in the background with both redirects, read-only sandbox, as a defensive PASS/FAIL control review. Framing: the consumer is an agent; the expensive failures are a silently wrong search result and a boot refusal on the live path. Ask specifically whether (a) any `DATABASE_SSL_MODE` path can still open a plaintext session silently under a strict mode (asyncpg string vs. context semantics, Unix sockets, multi-host URLs); (b) any input combination is resolved by precedence rather than refused; (c) the loopback test in D6 admits a non-loopback host by any URL form `urlsplit` accepts; (d) any HTTP client to the embedding URLs escapes the factory; (e) the deploy as specified can break today's production. Demand a closing JSON verdict block. Fold findings in; two rounds maximum per the review budget. (**Round 1, 2026-09-22: FAIL**, 4 MAJOR + 1 MINOR, all five accepted and folded in — see design.md, "Spec review history". Round 2 pending.) (Round 2 done: one MINOR, folded in at implementation — see design.md, "Spec review history".)
- [x] 0.4 `openspec validate internal-transport-tls --strict` passes. (Passes.)

## 1. Slice A — database transport (branch `itt-a-database`)

- [x] 1.1 `src/config.py` (region under `database_url`): `database_ssl_mode` (normalised to lower-case, stripped; `Literal` of the five modes after normalisation), `database_ssl_ca_file`, `database_ssl_cert_file`, `database_ssl_key_file` (all `str | None`, blank → `None` via `_off_means_none`). One `model_validator(mode="after")` implementing every refusal in design D3/D2a: TLS key in `database_url`'s query (parse with `sqlalchemy.engine.make_url`, compare keys case-insensitively against the nine names), any `PGSSL*` in `os.environ`, CA/mode mismatch in both directions, half a client pair, client pair with a lax mode, and file checks (exists, `S_ISREG`, `os.access(R_OK)`). Messages name the offending input and the replacement setting and never echo the URL's password.
- [x] 1.2 `src/services/transport_security.py` (new): `STRICT_DB_MODES`, `build_database_ssl_context(settings) -> ssl.SSLContext` (design D2 table; `PROTOCOL_TLS_CLIENT`, `minimum_version = TLSv1_2`, `load_verify_locations` / `load_cert_chain`; a load failure raises a `ValueError` naming the setting), `database_ssl_connect_args(settings) -> dict` (`{"ssl": False}` / `{"ssl": "prefer"}` / `{"ssl": ctx}`), `validate_database_url_transport(url: str, mode: str)` (shared by the validator and alembic: the D3 TLS-query-key refusal **and** the D3a socket-route refusal under strict modes — netloc host percent-decoded, every `?host=` value split on repeats and commas, empty list, `/` or `@` prefix on any candidate, `service`/`servicefile` keys, `PGHOST`/`PGHOSTADDR`/`PGSERVICE`/`PGSERVICEFILE`), and `install_strict_transport_listener(engine)` (D3b: a `connect` event on `engine.sync_engine` that runs the `pg_stat_ssl` query on the raw DBAPI connection and raises when not encrypted).
- [x] 1.3 `src/database.py`: merge `**database_ssl_connect_args(settings)` into `connect_args` beside `server_settings`; under a strict mode call `install_strict_transport_listener(engine)`. Comment: why a context and not a mode string (design D2), and that this is the one place engine TLS is decided.
- [x] 1.4 `alembic/env.py`: `run_async_migrations` calls `validate_database_url_transport(get_url(), settings.database_ssl_mode)` **before** building the engine, passes `connect_args=database_ssl_connect_args(settings)`, and under a strict mode installs the D3b listener. Offline mode untouched. Confirm `make test-schema`'s subprocess still imports cleanly (it sets `SECRET_KEY`; it does **not** set `EMBEDDING_ALLOW_PLAINTEXT` — see 5.1 for whether the harness needs it).
- [x] 1.5 `src/services/transport_security.py`: `check_database_transport()` per design D4 — the `pg_stat_ssl` query, the strict/lax branches, CRITICAL + `sys.exit(1)` under strict for unencrypted / missing row / connect failure (catch `OSError`, `ConnectionError`, `ssl.SSLError`, `asyncpg` and SQLAlchemy `DBAPIError`/`OperationalError`, `asyncio.TimeoutError`; log the class name, never `str(exc)` of a DSN-bearing error), the `internal_transport_plaintext` emission under lax, and the INFO transport line.
- [x] 1.6 `src/main.py`: await `check_database_transport()` in the lifespan **immediately before** `_check_embedding_dim()`, below the sandbox short-circuit.
- [x] 1.7 `src/services/security_events.py`: `"internal_transport_plaintext": frozenset({"reason", "outcome"})`. No new `ALLOWED_FIELDS` entry (both exist).
- [x] 1.8 `tests/test_internal_transport_config.py` (database half): the full mode matrix incl. normalisation and refusals (`allow`, `true`, `verify_full`); each D3 refusal (URL `?ssl=require`, `?sslmode=disable`, `?SSLRootCert=x`, a non-TLS parameter accepted, `PGSSLMODE` / `PGSSLROOTCERT` / `PGSSLNEGOTIATION` in the environment, CA with `require`, `verify-full` without CA, missing / directory / unreadable (skip as root) / non-PEM CA file, half a client pair, client pair under `prefer`); no refusal message contains the URL password. Use a CA and leaf generated at test time (`cryptography` if already a transitive dependency, else a checked-in test-only PEM under `tests/fixtures/` generated for the purpose — no real key material).
- [x] 1.8a Same file, **socket routes (Codex r1 #2)**: under each strict mode, each of these is refused — `?host=/var/run/postgresql`, `?host=db.example:5432,/run/postgresql` (mixed), repeated `?host=db.example&host=/tmp`, netloc `%2Frun%2Fpostgresql`, `@abstract` host, no host at all, `?service=x`, `?servicefile=/x`, and each of `PGHOST`/`PGHOSTADDR`/`PGSERVICE`/`PGSERVICEFILE` in the environment; under `prefer` and `disable` the socket URLs are accepted. For the refused cases, patch `asyncpg.connect` and `asyncio.AbstractEventLoop.create_unix_connection` with failing sentinels and assert **neither is called**, both for settings construction and for `alembic/env.py`'s `run_async_migrations` (driven in a subprocess, like `tests/integration/_harness.py` does, with a sitecustomize or `-c` wrapper that installs the sentinels). The D3b listener: a fake DBAPI connection whose `pg_stat_ssl` answer is false makes the `connect` event raise; one whose answer is true passes; the listener is installed on both engines only under strict modes.
- [x] 1.9 Same file: `build_database_ssl_context` — `verify-full` has `check_hostname is True` and `verify_mode == CERT_REQUIRED`; `verify-ca` has `check_hostname is False` and `CERT_REQUIRED`; `require` has `CERT_NONE` and no hostname check; all three `minimum_version == TLSv1_2`; `database_ssl_connect_args` returns `False` for `disable` and the string `"prefer"` for `prefer`; a context (not a string) for each strict mode. Assert `src/database.py`'s `engine` carries the helper's `ssl` value (inspect the dialect's connect args via `engine.dialect.create_connect_args` + the stored `connect_args`, or monkeypatch the helper and re-import in a subprocess).
- [x] 1.10 `tests/test_internal_transport_startup.py`: `check_database_transport()` with a mocked `async_session` — strict + `ssl=False` → `SystemExit` and a CRITICAL record; strict + no row → `SystemExit`; strict + connect raising `ConnectionError` → `SystemExit`, record names the class only; `prefer` + `ssl=False` → returns, exactly one `internal_transport_plaintext` with `reason="database"`, `outcome="prefer"` under `security_events.strict_fields()`; `disable` → `outcome="disable"`; encrypted → no event and the INFO line carries the TLS version and `server_verified=False` for `prefer`/`require`, `True` for `verify-*`. Lifespan ordering: the probe is awaited before `_check_embedding_dim` and not at all under sandbox mode.
- [x] 1.11 `tests/integration/test_internal_transport_pg.py` (skips without `PGVECTOR_TEST_ADMIN_URL`; the throwaway container has `ssl=off`): (a) an engine built with `database_ssl_connect_args` under `prefer` connects and `check_database_transport()` reports plaintext and emits the event; (b) under `require` the first connect **fails** and no session is opened — the #184 downgrade, proven closed against a real server; (c) under `verify-full` with a test CA, same. **Stretch, not a gate:** a second container started with `-c ssl=on` and a test-generated server cert proves the positive paths (`verify-full` succeeds with the right CA; fails with a wrong CA; fails on a hostname mismatch; `pg_stat_ssl.ssl` is true). If attempted, it must use its own port and must not run concurrently with `test-schema` (shared `SCHEMA_TEST_*`).

## 2. Slice B — embedding transport (branch `itt-b-embedding`, after 0.2)

- [x] 2.1 `src/config.py` (region under `openai_embedding_model`): `embedding_allow_plaintext: bool = False` (owner decision), `embedding_ca_file: str | None = None`, and a `PrivateAttr` holding the parsed embedding CA context. A `model_validator(mode="after")` after `_validate_provider_credentials` implementing design D6 rules 0–5 on the active provider's URL: raw-string check first (no surrounding whitespace, no C0 control or DEL), then **`httpx.URL`** parsing (never `urlsplit`), reusing `_is_loopback_host` on `url.host`. The `EMBEDDING_CA_FILE` checks run **in every mode, sandbox included**: exists, regular, readable, and **parsed** with `ssl.create_default_context(cafile=…)` into the private attribute (an `ssl.SSLError` becomes a `ValueError` naming the setting); only the CA-with-`http` refusal is URL-dependent and skipped under sandbox. Refusal messages never contain userinfo.
- [x] 2.2 `src/services/transport_security.py`: `classify_embedding_url(url)` (pure; returns scheme, host, port, is_loopback — the validator and the report share it), `embedding_http_client(timeout)` per design D7 — `trust_env=False`, `follow_redirects=False`, `verify=` the settings' parsed CA context or a once-built certifi context (`certifi.where()`), never re-reading a file — and `log_embedding_transport()` per design D8 (INFO line + event for non-loopback `http`).
- [x] 2.3 `src/services/embeddings.py`: `OllamaProvider.embed_one` and `OpenAIProvider._post` construct their clients through `embedding_http_client(30.0)` / `embedding_http_client(60.0)`. No other behavioural change; timeouts unchanged.
- [x] 2.4 `src/control_panel/routes.py`: the settings page's `/api/tags` ping uses `embedding_http_client(5.0)`.
- [x] 2.5 `tests/test_internal_transport_config.py` (embedding half): accepted — `https://…`, `http://localhost:11434`, `http://127.0.0.1:11434`, `http://127.8.9.10`, `http://[::1]:11434`, `http://LOCALHOST.`; refused without override — `http://ollama:11434` (the default), `http://127.0.0.1.evil.example`, `http://localhost.evil.example`, `http://0.0.0.0:11434`, `http://10.0.0.5:11434`; refused always — `http://127.0.0.1@evil.example:11434`, `https://user:secret@gateway.example/v1` (message lacks `secret`), `ftp://ollama`, `ollama:11434`, `file:///tmp/x`, `http://:11434`, `http://ollama:99999`; override admits `http://ollama:11434`; the inactive provider's URL is ignored in both directions; `MCP_SANDBOX_MODE=true` admits the default without the override; `EMBEDDING_CA_FILE` missing / directory / non-PEM / set with an `http` URL each refused. Run the same matrix for `OPENAI_BASE_URL` with `EMBEDDING_PROVIDER=openai`. **Parser cases (Codex r1 #5):** ` https://x.example/v1` (leading space), `https://x.example/v1 ` (trailing space), `http://127.0.0.1:11434\n`, `http://127.0.0.1:11434\r`, `http://127.0.\t0.1:11434`, `http://127.0.0.1:11434\x00`, `http://127.0.0.1:11434\x7f` are each refused with or without the override; and a **differential** table runs every URL in this task through both `urllib.parse.urlsplit` and `httpx.URL`, asserting the policy's verdict matches the httpx-derived classification wherever the two parsers disagree. **CA contents (Codex r1 #3):** a readable regular non-PEM `EMBEDDING_CA_FILE` refuses settings construction for `ollama` + `https` and for `openai` + `https` alike, and under `MCP_SANDBOX_MODE=true`; a valid CA yields a settings object whose stored context the factory returns by identity (no second parse — patch `ssl.create_default_context` after construction and assert it is not called by the factory).
- [x] 2.6 `tests/test_internal_transport_http_clients.py`: the factory's client has `follow_redirects is False` and `trust_env is False`; **environment regressions (Codex r1 #1):** with `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` set to a remote proxy and `NO_PROXY` unset, a request to `http://127.0.0.1:<port>` built by the factory goes to the loopback respx route and the proxy route records zero calls (and the client's mounts contain no proxy transport); with `SSL_CERT_FILE`/`SSL_CERT_DIR` pointing at an unrelated CA, the factory's context is unchanged (same `cert_store_stats()` as without them); with no `EMBEDDING_CA_FILE`, the context's CA count equals certifi's bundle and the report says `verify=certifi`; with `EMBEDDING_CA_FILE` its transport's SSL context loaded that CA (and not the default store — assert `cert_store_stats()` count equals the file's certs); without it, httpx's default verification; with `respx`, a 307 from the provider endpoint is **not** followed by either provider (the redirect target route records zero calls). An AST sweep over `src/services/embeddings.py` and `src/control_panel/routes.py` fails on any `httpx.AsyncClient(` / `httpx.Client(` construction outside `transport_security` — the "no bypassing client" scenario.
- [x] 2.7 `tests/test_internal_transport_startup.py` (embedding half): the report line for `http://ollama:11434` + override names scheme/host/port/`n/a`/override and emits one event `reason="embedding"`, `outcome="override"` whose serialised form contains none of `ollama`, `11434`, `http://`; loopback `http` logs the line and emits nothing; `https` + CA reports `ca-file` and emits nothing; a URL with a path and query logs neither.

## 3. Documentation

- [x] 3.1 `docs/architecture/indexing-and-embeddings.md`, "Embedding providers": the scheme policy (the five rules and why the inactive provider is not checked), the override and why it defaults to refusal (design D6's asymmetry argument), `EMBEDDING_CA_FILE` parsed at boot and replacing certifi's bundle (never the OS store), the single client factory with `trust_env=False` and `follow_redirects=False` and why (ambient proxies and `SSL_CERT_*`), `httpx.URL` as the only parser plus the raw-string check, and the sandbox rule (URL-dependent checks skipped, file validity never).
- [x] 3.2 `docs/architecture/schema-and-migrations.md`: a section "Database transport (#184)" — the mode table, context-vs-string (asyncpg's advisory `prefer` and libpq's silent `require` upgrade), the refusal list and why URL/env TLS inputs are refused rather than merged (SQLAlchemy's `connect_args` override), the two connection creators, the socket-route refusal (D3a — the five routes and why a mixed multi-host list is refused) and the per-connection check (D3b), the startup assertion, and accepted limitations 1–4 and 6.
- [x] 3.3 `docs/architecture/security-event-logging.md`: the catalogue row for `internal_transport_plaintext` (`reason`, `outcome`, emitted where) and a sentence in "What stays on the bare logger" explaining why this one startup record is catalogued (design D5).
- [x] 3.4 `.env.example`: a commented `DATABASE_SSL_MODE` / `DATABASE_SSL_CA_FILE` / `DATABASE_SSL_CERT_FILE` / `DATABASE_SSL_KEY_FILE` block under `DATABASE_URL` (modes, default, "do not put `ssl`/`sslmode` in the URL", the system-bundle spelling for a publicly-trusted database). Beside `OLLAMA_URL=http://ollama:11434`: an **uncommented** `EMBEDDING_ALLOW_PLAINTEXT=true` with a comment saying what it acknowledges and when to remove it; a commented `EMBEDDING_CA_FILE`. Beside `OPENAI_BASE_URL`: a note that the policy applies to it too. No host-specific value.
- [x] 3.5 `DEPLOYMENT.md`: an "Internal transport" subsection — the upgrade note (add `EMBEDDING_ALLOW_PLAINTEXT=true` before deploying if the embedding URL is plaintext to a non-loopback host), how to read the startup transport lines, and the operator steps to go strict (generic: internal CA, PostgreSQL `ssl=on` + `hostssl`/`hostnossl reject`, then `DATABASE_SSL_MODE=verify-full`; Ollama behind TLS, then `https` + `EMBEDDING_CA_FILE`). Generic; no host values.
- [x] 3.6 `CLAUDE.md` "Key decisions": one line — database TLS is `DATABASE_SSL_MODE` only (URL/`PGSSL*` TLS inputs refused), strict modes are an explicit context and the lifespan exits on an unencrypted session; the active embedding URL must be `https` or loopback unless `EMBEDDING_ALLOW_PLAINTEXT`, and every embedding HTTP client comes from `embedding_http_client`. Link to the two architecture notes.

## 4. Coordination

- [x] 4.1 Supervisor files the infrastructure follow-up issue with the text in design.md, "Operator follow-up", and cross-links #184, #185 and the #189 network follow-up. (Filed as #286.)

## 5. Test harness

- [x] 5.1 `tests/conftest.py` (Codex r1 #4): add the six new settings to `SETTINGS_ENV_KEYS` (the hermeticity test enforces this) and `EMBEDDING_ALLOW_PLAINTEXT=true` to `CONTROLLED_TEST_ENV`. `CONTROLLED_TEST_ENV` applies **only while the singleton is imported** and is restored afterwards, so it does not cover later constructions — hence 5.2. Also scrub every `PGSSL*`, `PGHOST`, `PGHOSTADDR`, `PGSERVICE`, `PGSERVICEFILE`, `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY`/`NO_PROXY` (either case) and `SSL_CERT_FILE`/`SSL_CERT_DIR` from the test process environment, so a developer's shell can neither fail the suite nor mask a regression.
- [x] 5.2 **Fresh `Settings(...)` constructions.** Add an autouse fixture in `tests/conftest.py` that `monkeypatch.setenv("EMBEDDING_ALLOW_PLAINTEXT", "true")` for every test, so every `Settings(_env_file=None, …)` built with the default `OLLAMA_URL` still constructs — this covers, among others, `tests/test_issue_261_controller.py`, `tests/test_issue_261_config.py`, `tests/test_issue_261_auth.py`, `tests/test_issue_261_tool_admission.py`, `tests/test_config_validation.py`, `tests/test_asvs_proxy_trust.py`, `tests/test_asvs_oauth_client_expiry.py`, `tests/test_transfer_config.py`, `tests/test_limiter_settings_env.py`, `tests/test_mcp_rate_limits.py`, `tests/test_issue_16_numeric_bounds.py`, `tests/test_issue_2_secret_key_placeholder.py`, `tests/test_password_policy.py`, `tests/test_fts.py`, `tests/test_issue_206_fingerprint.py` and `tests/integration/test_issue_261_concurrency_pg.py` (regenerate the list with `grep -rlE "Settings\(" tests`). Dedicated transport tests opt out with a `transport_defaults` marker (the fixture skips `setenv` and instead `delenv`s) so they exercise the real unset default. **Generated dotenv fixtures** (e.g. `tests/test_config_validation.py` writes a `.env`; grep for `_env_file=` with a path and for `write_text(` of `KEY=` lines) either gain the line or are confirmed to be covered by the process variable — which wins over a dotenv value in pydantic-settings, so a test that means to set it **false** in a dotenv must run under the opt-out marker.
- [x] 5.3 **Subprocess spawners with a scrubbed environment** do not inherit the autouse fixture: add `EMBEDDING_ALLOW_PLAINTEXT=true` explicitly to `tests/_transport_body_limit_cases.py`, `tests/test_transport_body_limit.py`, `tests/test_nested_mount_publication.py`, `tests/test_limiter_settings_env.py`, `tests/integration/_harness.py` (alembic subprocess) and the integration modules that build their own `env={…}` (`test_asvs_client_expiry_pg.py`, `test_issue_163_ops_health_pg.py`, `test_oauth_grants_pg.py`, `test_pgvector_search.py`, `test_transfer_pg.py`, `test_issue_190_log_config_process.py`). Regenerate with `grep -rln "env={\|OMCP_TEST_TRUST_ENV\|\"DATABASE_URL\":" tests`.
- [x] 5.4 **Offline lifespan fakes.** Every test that drives the real `lifespan` with the database stubbed must also stub `check_database_transport` (and `log_embedding_transport`, which emits a record) or it will attempt a real connection: at least `tests/test_indexer_root_overlap.py`, `tests/test_issue_22_indexer_cleanup_on_aenter_failure.py`, `tests/test_issue_206_startup_guards.py`, `tests/test_vector_iterative_scan.py`, `tests/test_dim_mismatch_check.py`, `tests/test_issue_160_indexer_runs.py`, `tests/test_issue_163_ops_health.py`, `tests/test_issue_190_log_format.py`, `tests/test_issue_87_beneath_root_lookup.py` and `tests/test_mcp_rate_limits.py` — regenerate with `grep -rlE "lifespan|_check_embedding_dim|_check_pgvector_version" tests`. Prefer one shared helper (e.g. `tests/_lifespan_stubs.py`) over ten copies. The dedicated startup tests (1.10, 2.7) exercise the real probe against a mocked session instead.
- [x] 5.5 `.github/workflows/ci.yml`: confirm no `PGSSL*`, `PGHOST*`, `PGSERVICE*`, proxy or `SSL_CERT_*` variable is set in the CI environment and the suites pass unchanged.

## 6. Merge, verify, deploy

- [x] 6.1 Merge both slices; run `pytest tests`, `make test-integration` and `make audit` **once on the merged result**. (Done on the merged tree before PR #284.)
- [x] 6.2 Own the seams: `log_embedding_transport()` is called from the lifespan right after `check_database_transport()`; grep for production callers of every new export (`database_ssl_connect_args` ×2, `validate_database_url_transport` ×2, `install_strict_transport_listener` ×2, `check_database_transport`, `log_embedding_transport`, `embedding_http_client` ×3).
- [x] 6.3 `openspec-verifier` subagent audits the merged tree against both spec deltas. (Verifier: 0 blocking; N1 recorded as an accepted limitation in design.md.)
- [x] 6.4 Adversarial Codex round(s) against the implementation (mandatory: embedding path + boot refusal). Two rounds by default; triage per the review budget; declined findings go to design.md's accepted limitations. (Codex implementation round 1: one MINOR, fixed; no round 2 needed per the review budget.)

## 7. Deploy and live check

- [x] 7.1 **Before** `make deploy`: add `EMBEDDING_ALLOW_PLAINTEXT=true` to the deploy-dir `.env` (production Ollama is `http://` on the bridge). Confirm no `PGSSL*` variable and no TLS query parameter are present there, and that no `HTTP(S)_PROXY` was relied on for the embedding hop (the factory now ignores it). Leave `DATABASE_SSL_MODE` unset. (Done: override added to the deploy-dir `.env`; `DATABASE_SSL_MODE` unset.)
- [x] 7.2 `make deploy`; `make db-check` clean. (Deployed 2026-09-22 from main 536e8eb; no migration; `alembic check` clean.)
- [x] 7.3 Live check from the container logs: the startup lines read `Database transport: mode=prefer encrypted=False …` and `Embedding transport: provider=ollama scheme=http … plaintext_override=True`, and exactly two `internal_transport_plaintext` records (`database`/`prefer`, `embedding`/`override`) appear. A deliberate negative check on a **scratch** container (not production): `DATABASE_SSL_MODE=require` against the live server refuses to start with the CRITICAL line; removing the override refuses to start with the message naming it. (Both startup lines and exactly the two expected events observed; no ERROR/CRITICAL/Traceback in the first 15 min. The scratch-container negative check was not run; the refusal paths are covered by 1.10/1.11.)
- [x] 7.4 End-to-end MCP exercise (the project's substitute for a browser pass): call `semantic_search`, `find_related` and `keyword_search` against the live server and confirm results; open the panel settings page and confirm the provider shows reachable. Record the tools called. (Called `semantic_search`, `find_related`, `keyword_search` on the live server: correct, relevant results. Panel settings page not checked.)
- [x] 7.5 Archive with `openspec archive internal-transport-tls -y`, commit, push; comment on #184 and #185 with what shipped and the follow-up link (closure per the owner's answer to design question 2). (This archive; #184/#185 commented, stay open until #286 lands.)
