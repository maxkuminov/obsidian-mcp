Slices are file-scoped and disjoint: no two slices edit the same file. The
migration lives in **Slice A only**. A, then B/C/D in parallel, then E/F in
parallel, then G, then §8. Two cross-slice dependencies are named explicitly
where they occur (B pre-adds attributes F later reads; E consumes B's result
type) — neither creates a shared file.

## 1. Slice A — schema, config and the state accessor (owns `alembic/versions/023_indexer_state.py`, `src/models/db.py`, `src/config.py`, new `src/services/index_state.py`, `tests/integration/test_schema_check.py`, new `tests/test_issue_206_fingerprint.py`, `docs/architecture/schema-and-migrations.md`). **Runs first — every other slice imports from it.**

- [ ] 1.1 Migration 023, in 022's shape: `SET LOCAL lock_timeout='10s'` / `statement_timeout='60s'` at the top and `RESET` at the end; a `_reconcile`-style create-or-verify for **both** units; `MARKER` constants for the table comment and the column comment; `downgrade()` drops each unit only if it carries its marker, per-unit all-or-nothing; module docstring carrying the "why a table, why a CHECK, why no backfill, why the server default is the truth for every existing row, deploy window, locks" reasoning in 022's voice.
- [ ] 1.2 023 creates `indexer_state (key VARCHAR(64) PRIMARY KEY, value TEXT NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT now())` plus `ck_indexer_state_key` restricting `key` to `('embedding_fingerprint','fts_fingerprint','embed_rotation_cursor')`. Resolve the constraint through `pg_constraint` (`conrelid`, `contype`, `convalidated`, `pg_get_constraintdef` against the server's own rendering derived from a scratch `TEMP` table, plus the marker as its constraint comment) — **never by name**, per 013's rule. Refuse a pre-existing table/constraint that is not exactly 023's, naming what it found, changing nothing.
- [ ] 1.3 023 adds `notes_metadata.chunks_truncated BOOLEAN NOT NULL DEFAULT FALSE` with its own marker comment; same refuse-don't-adopt reconciliation as 022's `links_truncated`. **No backfill and no `indexer_state` row written** — the adoption rule at startup owns that.
- [ ] 1.4 `src/models/db.py`: `_INDEXER_STATE_TABLE_MARKER` and `_CHUNKS_TRUNCATED_COLUMN_MARKER` module constants byte-identical to 023's; `class IndexerState(Base)` with `__tablename__ = "indexer_state"`, the `CheckConstraint` and the `{"comment": ...}` tuple-tail form `IndexerRun` uses, and `_TABLE_MARKER: ClassVar[str]`; `NoteMetadata.chunks_truncated` beside `links_truncated`, with the comment spelling out why it is a column and not only a log line (the error ring buffer is bounded and process-lifetime; the vector tools would read a capped note's head as the whole note).
- [ ] 1.5 `src/config.py`: `MAX_CHUNKS_PER_NOTE = 1_000` as a module constant beside `MAX_LINKS_PER_NOTE`, with the arithmetic in the comment (`MAX_NOTE_BYTES` 10 MiB ÷ `CHUNK_SIZE` 512 tokens ≈ 5,120 chunks; the cap is ~2 MB of cleaned text) **and the accepted worst case written down** — the cap × the provider's 30 s per-call bound is one note's embedding time, which is the delay a budget stop can add. Also `MAX_EMBED_FAILURE_MESSAGE_CHARS = 200`. Two `Settings` fields: `embed_chunk_budget_per_user: int = Field(5000, ge=0)` and `embed_time_budget_seconds_per_user: int = Field(300, ge=0)`, `0` disabling each, documented as enforced only when a pass serves more than one active user scope.
- [ ] 1.6 `src/config.py`: a model validator rejecting `chunk_overlap >= chunk_size`, naming both values. The `step = max(char_size - char_overlap, 1)` floor (#10) turns the equal case from a hang into a one-character step — ~3 KB of prose becomes ~3,000 chunks and every ordinary note hits the cap. Record that interaction in the comment at both keys.
- [ ] 1.7 New `src/services/index_state.py` — a separate module so `embeddings.py` and `indexer.py` can both import it without a cycle. `KEY_EMBEDDING_FINGERPRINT` / `KEY_FTS_FINGERPRINT` / `KEY_ROTATION_CURSOR`; `async def get_state(session, key) -> str | None`; `async def set_state(session, key, value) -> None` (upsert, sets `updated_at`, **no internal try/except — a failure propagates**, per design D7d); `def embedding_fingerprint() -> str` over `{"v":1,"provider","model","dimensions","chunk_size","chunk_overlap","max_chunks_per_note"}` and `def fts_fingerprint() -> str` over `{"v":1,"configs": sorted(...)}`, both `json.dumps(..., sort_keys=True, separators=(",",":"))`; `def compare_fingerprint(stored, current) -> FingerprintVerdict` returning `ABSENT` / `MATCH` / `DIFFERS(fields)` / `UNREADABLE(reason)`, where an unparseable value or an unknown `v` is `UNREADABLE` and never rewritten. The `model` field takes the **active** provider's model through the same branch `get_provider()` uses — one function, so the provider and the model cannot be read from different branches.
- [ ] 1.8 `tests/integration/test_schema_check.py`: 023 cases — fresh (both units present, typed, marked; CHECK validated with the canonical definition; `alembic check` clean), the migration writes no row and backfills nothing, a disallowed `key` is rejected on insert, impostor table / impostor constraint (`CHECK (true)` under the right name) / impostor column each refused naming what was found with nothing changed, stamp-back `alembic stamp 022` → `upgrade head` preserves recorded rows and a set `chunks_truncated`, downgrade drops only marked work. **Raise the module's asserted head from `017` to `023` and keep every existing 013/014/016/017/022 case** — the head assertion is what makes "head" a value somebody chose.
- [ ] 1.9 `tests/test_issue_206_fingerprint.py`: canonical rendering is stable and single-spelled; provider / model / dimension / chunk-size / overlap / **chunk-cap** changes each alter the embedding fingerprint; changing the *inactive* provider's model does not; `FTS_CONFIGS` reordering does not alter the FTS fingerprint while a membership change does; `compare_fingerprint` returns `DIFFERS` with the changed field named, `UNREADABLE` for garbage and for an unknown `v`, `ABSENT` for `None`; `chunk_overlap == chunk_size` is rejected by `Settings`.
- [ ] 1.10 `docs/architecture/schema-and-migrations.md`: 023's two units, why the CHECK is not tidiness (a mistyped key reads as absent and absent means *adopt*), why nothing is backfilled (016's argument), the gate's head moving to 023 and why the earlier waves' cases stay.
- [ ] 1.11 `make test-schema` green; `alembic check` clean against a fresh migrate.

## 2. Slice B — the embedding service (owns `src/services/embeddings.py`, new `tests/test_issue_201_embed_outcome.py`, new `tests/test_issue_202_chunk_cap.py`, new `tests/test_issue_200_stale_service.py`, and the existing `tests/test_issue_11_embed_note_preserves_vectors_on_failure.py`, `tests/test_issue_127_embed_batch_deadline.py`, `tests/test_vector_iterative_scan.py`, `tests/test_search_phase_timing.py`, `tests/test_issue_161_result_telemetry.py`, `tests/integration/test_pgvector_search.py`). **After A is merged** (imports `MAX_CHUNKS_PER_NOTE`, `MAX_EMBED_FAILURE_MESSAGE_CHARS`).

- [ ] 2.1 `NoteEmbedOutcome` enum (`EMBEDDED`, `CERTIFIED_EMPTY`, `PROVIDER_FAILED`, `PROVIDER_CARDINALITY_MISMATCH`); frozen `EmbedNoteFailure(exc_type, message, requested, received)` with `message` truncated at `MAX_EMBED_FAILURE_MESSAGE_CHARS` **at capture**; frozen `EmbedNoteResult(outcome, chunks, truncated, failure)` with `failure` non-null exactly for the two failing outcomes. **No `__int__` and no `__radd__`** — a first draft claimed `total_chunks += result` would keep working; it raises `TypeError`, so every caller reads `result.chunks` (Slice E) and every test that compared the return to an integer is updated here.
- [ ] 2.2 `chunk_text_bounded(content, *, chunk_size, overlap, max_chunks) -> tuple[list[str], bool]` returning the first N chunks in document order and whether it truncated; `chunk_text` keeps its signature and delegates. Preserve the `step = max(char_size - char_overlap, 1)` guard (#10) and the short-circuit's existing unstripped-single-chunk behaviour.
- [ ] 2.3 `embed_note` returns `EmbedNoteResult`: zero chunks → `CERTIFIED_EMPTY` (unchanged certify-and-delete); provider raise → `PROVIDER_FAILED` carrying the class, the truncated message and `requested`, nothing written; vector/chunk mismatch → `PROVIDER_CARDINALITY_MISMATCH` carrying `requested` and `received`, nothing written; otherwise `EMBEDDED`. Uses `chunk_text_bounded`. **The truncation ERROR line is emitted by the caller after the certifying transaction commits, not here** — the result carries `truncated` and the caller logs it (Slice E), so a certification that matches no row leaves no permanent ERROR for a write that rolled back. Certification is otherwise unchanged: same `certify_embedded` predicate, same stamp-before-delete order, same per-note flush.
- [ ] 2.4 `semantic_search`: add `"stale": bool` (`embedded_content_hash IS DISTINCT FROM content_hash`, computed in Python from the already-hydrated `NoteMetadata`) and `"embedding_truncated": bool` to each result dict; set `"chunk"` to `None` for a stale row instead of the 500-character clip, so a caller cannot obtain the superseded text at all. Statement, `SET LOCAL`s, overfetch, re-sort, dedupe and exact-fallback eligibility unchanged.
- [ ] 2.5 Update the existing tests this breaks, in this slice: `test_issue_11_*` and `test_issue_127_embed_batch_deadline.py` compare `embed_note`'s return to an integer → assert on `outcome` and `chunks`; `test_vector_iterative_scan.py:265` asserts the exact semantic result key set → add the two new keys; `test_search_phase_timing.py:120` and `test_issue_161_result_telemetry.py:104` build fake `(chunk, note, distance)` tuples → give the fake note `content_hash`, `embedded_content_hash` (equal, so the rows stay fresh) and `chunks_truncated`; `tests/integration/test_pgvector_search.py` → same key-set update. **Also pre-add those three attributes to `test_vector_iterative_scan.py`'s `_RelatedRow` fake (`:281`)**, equal hashes and `False`, so Slice F's `find_related` change lands against a fake that already carries them — the stub-and-swap discipline for a cross-worktree dependency, recorded here so F does not need to touch this file.
- [ ] 2.6 Tests (`test_issue_201_embed_outcome.py`): provider raise → `PROVIDER_FAILED` with the class name and a message truncated at the bound, no `note_embeddings` row touched, `embedded_content_hash` unchanged; cardinality mismatch → `PROVIDER_CARDINALITY_MISMATCH` with `requested`/`received`, previous vectors intact; zero chunks → `CERTIFIED_EMPTY` and stamped; success → `EMBEDDED`; `int(result)` raises rather than silently working, and `total_chunks += result` is not a supported idiom.
- [ ] 2.7 Tests (`test_issue_202_chunk_cap.py`): a note over the cap yields exactly `MAX_CHUNKS_PER_NOTE` rows holding the first N chunks in document order, `truncated=True`, and **is certified**; a note at the cap exactly is not marked; a note edited under the cap re-embeds with `truncated=False`; cardinality is still exact over the capped list (one vector short → refused, previous vectors intact).
- [ ] 2.8 Tests (`test_issue_200_stale_service.py`): a row whose hashes differ is returned with `stale=True` and `chunk is None`; a NULL `embedded_content_hash` is stale (`IS DISTINCT FROM`, not `!=`); a fresh row keeps its 500-character chunk; `embedding_truncated` mirrors the column; the result set is identical in membership and order to the pre-change one for the same corpus.

## 3. Slice C — startup guards, the reset script and operator docs (owns `src/main.py`, `scripts/reset_embeddings.py`, `.env.example`, `README.md`, new `tests/test_issue_206_startup_guards.py`). **After A is merged. Runs in parallel with B and D.**

- [ ] 3.1 `src/main.py`: `_check_embedding_fingerprint()` in `_check_embedding_dim`'s shape, driven by `compare_fingerprint`. Table absent → return (defer to alembic). `ABSENT` → adopt, write, log WARNING that it is assumed and not verified. `MATCH` → silent. `DIFFERS` → `logging.critical` naming both fingerprints and the differing fields, pointing at `make reset-embeddings`, then `sys.exit(1)`. `UNREADABLE` → the same refusal, saying the stored value could not be interpreted, and **write nothing**.
- [ ] 3.2 `src/main.py`: `_check_fts_fingerprint()` — identical structure and **identical disposition: `DIFFERS` and `UNREADABLE` both exit non-zero**, pointing at `make rebuild-tsvectors`. The first draft warned and served; a stale stemmer is not merely incomplete — `running` stored as the lexeme `run` makes a `simple` query for `run` match a note that does not contain the word, a false positive an agent acts on. Reverting `FTS_CONFIGS` clears the refusal with no rebuild, which is the exit that keeps this from being an outage. No WARNING-vs-ERROR split remains.
- [ ] 3.3 Lifespan: call both **after** `_validate_fts_configs()` (`main.py:275`), so a misspelled config name still fails with `validate_fts_configs`'s own message listing the installed configs rather than as an opaque fingerprint diff. Both sit below the sandbox short-circuit at `main.py:263`, so `MCP_SANDBOX_MODE` skips them with everything else — assert that.
- [ ] 3.4 `scripts/reset_embeddings.py`: record the current embedding fingerprint in the same transaction as the column recreate and the `embedded_content_hash` wipe, before the commit; **a failed record rolls the reset back and exits non-zero** (design D7d — this is not instrumentation). Keep the `docker compose run --rm` contract intact (#142): the script must read the edited `.env`, which is what makes it write the *new* fingerprint.
- [ ] 3.5 `scripts/reset_embeddings.py`: create `ix_note_embeddings_embedding_hnsw` **only when the configured dimension admits an HNSW index**, matching the conditional the pre-warm probe and the panel path already apply. Today it creates it unconditionally, so a deployment configured above pgvector's index-dimension limit gets a wiped column and an aborted transaction. Log the omission.
- [ ] 3.6 `.env.example`: add the file's existing sentence — "Changing post-deploy requires \`make reset-embeddings\`." — to the comment above `EMBEDDING_MODEL` (`:127`) and above `OPENAI_EMBEDDING_MODEL` (`:140`); note at `CHUNK_SIZE`/`CHUNK_OVERLAP` that the overlap must stay below the size and why.
- [ ] 3.7 `README.md`: widen "Switching providers" (`:915`) to cover a model change within one provider, and **replace the reset-before-recreate ordering with deploy-then-reset**, explaining that the new image refuses at the fingerprint or dimension guard and therefore embeds nothing while the reset runs. Add the `FTS_CONFIGS` runbook: deploy → refusal → `make rebuild-tsvectors` (all scopes, all-or-nothing) → restart, with "revert `FTS_CONFIGS`" named as the immediate escape. Add the reset pointer to the `EMBEDDING_MODEL` and `OPENAI_EMBEDDING_MODEL` config-table rows (`:896`, `:900`).
- [ ] 3.8 Tests (`test_issue_206_startup_guards.py`): model / chunk-size / overlap / chunk-cap change → `SystemExit(1)` with both fingerprints and the field named; FTS membership change → `SystemExit(1)` naming the rebuild command; FTS reorder → silent; misspelled config name → fails on `validate_fts_configs`'s message, not a fingerprint diff; absent key → adopted, written, warned once, second start silent; unreadable value and unknown `v` → refuse and leave the stored value byte-identical; refusing start writes nothing and a second start refuses identically; reverting the configuration clears the refusal; absent table → returns without deciding; sandbox mode → neither read nor written; the reset script writes the new fingerprint, a failed write rolls the reset back, and the HNSW index is skipped above the dimension limit.

## 4. Slice D — the panel (owns `src/control_panel/routes.py`, `src/control_panel/templates/dashboard.html`, `docs/architecture/control-panel.md`, new `tests/test_issue_201_dashboard.py`). **After A is merged. Runs in parallel with B and C.**

- [ ] 4.1 One shared predicate helper for "not current" (`embedded_content_hash IS NULL OR embedded_content_hash IS DISTINCT FROM content_hash`), called by the dashboard and by `/settings/reset-embeddings/progress` so the two cannot drift. The progress endpoint keeps its admin-only, unscoped behaviour and its existing JSON keys.
- [ ] 4.2 `dashboard()` (`routes.py:383`): add `stats.notes_pending` and `stats.notes_chunks_truncated`, **scoped by `_scope_user_id(user)` exactly as the coverage counts above them are**. Leave `embedding_pct` and `notes_with_embeddings` untouched — the bar keeps meaning "has at least one vector".
- [ ] 4.3 `templates/dashboard.html:110-125`: render the pending count and the truncated count beside the Coverage row, with the pending figure carrying a short label distinguishing it from coverage ("N not current"). Tokens only, no new CSS values — `checks/token_coverage.py` must stay green.
- [ ] 4.4 The Danger-zone reset paths (`routes.py:2007-2013`, `:2028`, `:2057`) record the current embedding fingerprint in the same locked transaction as the wipe, and a failed record rolls that transaction back and surfaces as a flash error rather than being swallowed. Confirm this path's HNSW creation is already conditional on the dimension; if it is not, make it so here (Slice C owns only the CLI script).
- [ ] 4.5 `docs/architecture/control-panel.md`: the two coverage questions and why the bar is not redefined; the scoping rule for the new counts and why an unscoped copy of the poller's query would leak another tenant's backlog.
- [ ] 4.6 Tests: a fully-covered, fully-stale vault renders 100% with a pending count equal to the note count; a non-admin's pending count excludes another user's notes; a zero pending count is still rendered; a capped note is counted; the progress endpoint's response is byte-compatible with its previous shape; the Danger-zone reset writes the fingerprint and rolls back when that write fails.

## 5. Slice E — the pass (owns `src/services/indexer.py`, `scripts/rebuild_tsvectors.py`, the existing `tests/test_issue_19_embed_vault_rechecks_pause.py`, `tests/test_issue_91_indexed_root.py`, `tests/test_issue_160_indexer_runs.py`, new `tests/test_issue_201_pass_record.py`, new `tests/test_issue_202_fairness.py`, new `tests/integration/test_issue_202_rotation_pg.py`, new `tests/integration/test_issue_206_rebuild_coverage_pg.py`). **After A and B are merged** (imports `EmbedNoteResult` and the state accessor).

- [ ] 5.1 `_embed_vault_pinned` consumes `EmbedNoteResult` by field (`result.chunks`, never `+= result`): `outcome.embedded += 1` only for `EMBEDDED` / `CERTIFIED_EMPTY`; `PROVIDER_FAILED` and `PROVIDER_CARDINALITY_MISMATCH` go through a new `EmbedPassResult.record_failure_detail(failure)` that sets `first_error` from the structured failure (`"{exc_type}: {message}"`, or `"CardinalityMismatch: {received} vectors for {requested} chunks"`) and increments the same counter as `record_failure(exc)`. The exclusion branch, the hash-mismatch skip and `StaleCertification` keep their present, non-failure handling. `_record_index_run` is **not** touched — #160's asymmetry.
- [ ] 5.2 Set/clear `notes_metadata.chunks_truncated` from `EmbedNoteResult.truncated` in the certifying transaction, and emit the one ERROR line naming the path and the cap **after that transaction commits**; clear the marker on the exclusion branch and on `CERTIFIED_EMPTY`, since both leave the note with no vectors.
- [ ] 5.3 `_reconcile_exclusions` (`indexer.py:2683`) takes the pass's `EmbedPassResult` and reports its own provider failures into it at `:2807`; its zero-chunk probe at `:2796` uses the bounded chunker. **Define `attempted` as the backlog size plus the number of sweep rows that actually reached the provider** — not the rows the sweep scanned, or three failures out of three attempts would render as "3 of 16,700".
- [ ] 5.4 `_active_user_ids()` gains `ORDER BY User.id`. New `_rotated_user_ids()` reads `indexer_state['embed_rotation_cursor']` and rotates the ordered list to start at the smallest id strictly greater than it, wrapping; used by `run_indexer_loop` only (`:3358` startup, `:3431` tick). `_reindex_background` keeps calling the unrotated function.
- [ ] 5.5 After each user's per-user sequence finishes — success or failure — write the cursor to that user's id, in its own short session opened by the lock holder **after** the wrapped body's session has closed (`_write_indexer_run`'s discipline), wrapped so a failure is logged and swallowed. This is the one write in this change that stays swallow-on-failure; the fingerprint writes do not.
- [ ] 5.6 Per-user budget: a small accumulator (chunks embedded, wall-clock elapsed) created per user per pass, consumed by the backlog loop and by the sweep, checked at the two note boundaries beside `_is_paused()` (`:2530`, `:2744`) and **never inside a note**. Enforced only when the pass is serving more than one active user scope; skipped entirely when both settings are `0`; **never before the first note of that user's pass has completed**. A stop logs one WARNING and writes nothing to `error`.
- [ ] 5.7 `_embed_vault_pinned` reads `indexer_state['embedding_fingerprint']` once per user pass and embeds nothing when it differs from this process's own, logging at ERROR naming both and recording nothing (design D7c — this is what makes the reset ordering enforced rather than documented; a live old-configuration container stops embedding within one tick of any reset).
- [ ] 5.8 Move the FTS fingerprint write **out of** `_rebuild_tsvectors_pinned` and into a new outer driver: enumerate `SELECT DISTINCT user_id FROM notes_metadata` (every scope holding rows, `NULL` included — **not** `_active_user_ids()`), rebuild each, and write `fts_fingerprint` in the **same transaction** as all of them; any scope that raises or whose root cannot be pinned rolls the whole operation back, writes no fingerprint, and names that scope. `scripts/rebuild_tsvectors.py` calls the driver instead of looping over active users.
- [ ] 5.9 Tests (`test_issue_201_pass_record.py`): a provider that raises for every note → run row with non-null `error` naming N of M and the first failure's class and message, `notes_embedded = 0`, distinguishable from an empty-backlog pass; one failing note among many → count and error both correct; **a reconciliation-only outage (empty backlog, sweep re-embeds all fail) → run row failed, with `attempted` equal to the sweep's provider attempts and not its scan size**; an excluded / hash-mismatched / paused note contributes neither a failure nor an attempt; the heartbeat stays ok through a total outage while the run row says failed. Update the fakes in `test_issue_19_*`, `test_issue_91_*` and `test_issue_160_*` that monkeypatch `indexer.embed_note` to return `EmbedNoteResult`.
- [ ] 5.10 Tests (`test_issue_202_fairness.py`): two users, the first over budget → the first stops at a note boundary and the second is served in the same pass; a note in flight when the budget is exhausted is finished and certified; a user whose first note alone exceeds the budget still embeds it; a single active scope is unbudgeted; a budget stop leaves `error` null; a budget-stopped sweep leaves repaired rows repaired and the next pass completes the remainder; the scan and link backfill are **not** budgeted (asserted, so the declared scoping is a fact rather than a comment).
- [ ] 5.11 Tests (`tests/integration/test_issue_202_rotation_pg.py`): a pass truncated after user B resumes at C; a cursor naming a deleted user resolves to the next id and does not raise; a complete cycle wraps to the first id; a manual reindex leaves the cursor unchanged; a raising cursor write does not fail the pass. Plus: a pass whose stored embedding fingerprint differs from the process's embeds nothing and records nothing (5.7).
- [ ] 5.12 Tests (`tests/integration/test_issue_206_rebuild_coverage_pg.py`): rows owned by an inactive user and rows with a NULL owner are both rebuilt; a second scope raising rolls the first scope's vectors back and writes no fingerprint, naming the scope; a complete rebuild commits vectors and fingerprint together; an unpinnable scope blocks the record and is named.

## 6. Slice F — the tool surface (owns `src/mcp_server/tools.py`, `src/mcp_server/server.py`, `tests/integration/test_search_recall.py`, new `tests/test_issue_200_tool_render.py`). **After B is merged. Runs in parallel with E.** Slice B has already given `test_vector_iterative_scan.py`'s fakes the three new attributes, so F does not touch that file.

- [ ] 6.1 `find_related_stmt` (`tools.py:1912`) selects `NoteMetadata.content_hash`, `NoteMetadata.embedded_content_hash` and `NoteMetadata.chunks_truncated` alongside its six existing columns; per-row dict gains `stale` and `embedding_truncated`; `chunk` is `None` for a stale row. No predicate, `SET LOCAL`, overfetch, re-sort, dedupe or fallback change — confirm `tests/integration/test_search_recall.py`'s EXPLAIN assertions still hold with the added columns.
- [ ] 6.2 `find_related_impl`: compute the **source**'s own staleness from the row it already loaded at `:1956` and state it on **every return path where that row exists** — the ranked path, the true-zero-result path (`:2029-2035`), and any other post-load return. The `related_source_not_embedded` branch keeps its own message and marker unchanged: a source with no vectors is a different fact with a different fix.
- [ ] 6.3 Render, in `get_links_impl`'s idiom (`tools.py:1721-1769`): the header line of both tools carries the stale count **always, including zero**; a stale row carries `— stale: true` and its preview line is replaced by the withheld notice naming `read_note`; a truncated row carries `— embedding_truncated: true`; when any row is stale, a bold trailing block says how many and that the previews were withheld while the paths and titles are current.
- [ ] 6.4 `semantic_search_impl` (`tools.py:1164`): same rendering; handle `chunk is None` (today's `r['chunk'][:200]` would raise).
- [ ] 6.5 `server.py` docstrings for `semantic_search` (`:274`) and `find_related` (`:561`) gain a declared-degradation paragraph in `get_links`'s voice (`server.py:523-526`): what `stale: true` means, that the preview is withheld and `read_note` is the remedy, what `embedding_truncated: true` means, for `find_related` that a stale source makes every neighbour an answer about the note's previous content — **and the declared bound**, that the signal reports what the index has committed, so an edit the indexer has not yet seen is not marked.
- [ ] 6.6 Tests (`test_issue_200_tool_render.py`): a stale row is present, marked, and carries no note text; the header count is rendered when it is zero; a fresh row's preview is unchanged; `find_related` with a stale source states it on the ranked path **and on the empty-result path**; the not-embedded branch is unchanged; the withheld notice contains nothing read from the note's current bytes; a capped note is marked in both tools; a row whose `content_hash` has not yet been updated by the scan is **not** marked, pinning the declared bound; `usage_logs.params` gains no new key.

## 7. Slice G — the architecture notes (owns `docs/architecture/indexing-and-embeddings.md`, `docs/architecture/search.md`). **After E and F are merged.**

- [ ] 7.1 `indexing-and-embeddings.md`: the typed embed outcome, its bounded failure record, and why it is a return value rather than a raise (the sweep's declared convergence exception); why there is no `__int__` shim; the chunk cap as a declared degradation in the keyword-retreat/link-cap voice, including why a capped note is certified (#127), why the ERROR follows the commit, and why the cap cannot name the true count; the `CHUNK_OVERLAP < CHUNK_SIZE` interaction; the persisted rotation cursor and why an in-memory one is a no-op; the note-boundary budget, its at-least-one-note clause, its single-scope exemption, its embed-stage-only scope, the accepted worst case (cap × per-call bound), and why it writes nothing to `error`; the sweep's contribution to the accumulator and the `attempted` denominator; the two fingerprints, their format, the adoption rule, **both failing closed**, the unreadable-value rule, the coverage-proving rebuild, the reset ordering and the pass-level re-check, and the "a fingerprint write is not instrumentation" rule.
- [ ] 7.2 `search.md`: the staleness annotation, `IS DISTINCT FROM` rather than `!=`, why filtering was refused (the edit window, the outage, and the exact-fallback interaction), why the preview alone is withheld and everything else kept, the `find_related` stale-source line on every return path, and the **declared bound** that the signal reports only what the scan has committed.

## 8. Verification

- [ ] 8.1 Merge A; then B, C, D; then E, F; then G. Run the full suite **once on the merged result** — that is the authoritative gate, not the per-worktree runs. Grep for production callers of every new export (`chunk_text_bounded`, `EmbedNoteResult`, `EmbedNoteFailure`, `record_failure_detail`, `get_state`/`set_state`, `embedding_fingerprint`, `fts_fingerprint`, `compare_fingerprint`, `_rotated_user_ids`, the rebuild driver) before believing a slice is done, and grep for any surviving `+= result` on an `embed_note` return.
- [ ] 8.2 `make test-schema` green, with its asserted head at 023 and every earlier wave's cases still running.
- [ ] 8.3 `make audit` (pip-audit) clean.
- [ ] 8.4 `openspec validate --strict` valid.
- [ ] 8.5 `openspec-verifier` subagent pass against the spec deltas; iterate to zero blocking gaps.
- [ ] 8.6 Adversarial Codex review, framed for this product: the expensive failures are a destructive write and a silently wrong search result an agent acts on unseen. Point it at the chunking/embedding path and the two vector read paths — mandatory triggers per `CLAUDE.md`. Fix BLOCKER/MAJOR before shipping; record accepted limitations.
- [ ] 8.7 Before deploying, confirm the production database has **no `notes_metadata` rows owned by an inactive or unassigned user** (design open question 3): with the keyword fingerprint failing closed and the rebuild requiring every retained scope, such rows would block a future `FTS_CONFIGS` change.
- [ ] 8.8 `make deploy` **with the embedding and FTS configuration unchanged** (design D7 risk: the first startup adopts whatever it finds). Confirm `make db-check` clean, that the startup log shows both fingerprints adopted exactly once, and that `indexer_state` holds two rows.
- [ ] 8.9 Live MCP exercise against the deployed server, naming the tools called: `create_note` a small note; `semantic_search` for it before the next pass (absent or `stale: true` with the preview withheld), then after the pass (a preview and `0 stale` in the header); `edit_note` it and search **immediately**, before the pass — the row must come back **unmarked**, which is the declared pre-scan bound, recorded in the report as observed rather than as a defect; wait for the pass and search again to see the single stale row among fresh ones; `find_related` on the edited note before the pass completes (the stale-source line, including on an empty result); `read_note` to confirm the current text is what the notice points at; `get_links` on the same note to confirm the shipped `truncated:` token is unaffected. Delete the test note afterwards.
- [ ] 8.10 Post-deploy: confirm the dashboard's pending count moves with the backlog and returns to zero, and that the coverage bar's value is unchanged from before the deploy for an unchanged vault.
- [ ] 8.11 Archive the change; PR closing #201, #200, #202, #206; update the ASVS report's issue rows in the vault and file the residuals as follow-up issues (`semantic_search` hydrating full vectors; `OPENAI_BASE_URL` excluded from the fingerprint; the scan and link backfill unbudgeted; the pre-scan staleness window).
