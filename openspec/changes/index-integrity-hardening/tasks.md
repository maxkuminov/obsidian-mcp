Slices are file-scoped and disjoint: no two slices edit the same file. The
migration lives in **Slice A only**. A, then B/C/D in parallel, then E/F in
parallel, then G, then §8.

## 1. Slice A — schema, config and the state accessor (owns `alembic/versions/023_indexer_state.py`, `src/models/db.py`, `src/config.py`, new `src/services/index_state.py`, `tests/integration/test_schema_check.py`, new `tests/test_issue_206_fingerprint.py`, `docs/architecture/schema-and-migrations.md`). **Runs first — every other slice imports from it.**

- [ ] 1.1 Migration 023, in 022's shape: `SET LOCAL lock_timeout='10s'` / `statement_timeout='60s'` at the top and `RESET` at the end; a `_reconcile`-style create-or-verify for **both** units; `MARKER` constants for the table comment and the column comment; `downgrade()` drops each unit only if it carries its marker, per-unit all-or-nothing; module docstring carrying the "why a table, why a CHECK, why no backfill, why the server default is the truth for every existing row, deploy window, locks" reasoning in 022's voice.
- [ ] 1.2 023 creates `indexer_state (key VARCHAR(64) PRIMARY KEY, value TEXT NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT now())` plus `ck_indexer_state_key` restricting `key` to `('embedding_fingerprint','fts_fingerprint','embed_rotation_cursor')`. Resolve the constraint through `pg_constraint` (`conrelid`, `contype`, `convalidated`, `pg_get_constraintdef` against the server's own rendering derived from a scratch `TEMP` table, plus the marker as its constraint comment) — **never by name**, per 013's rule. Refuse a pre-existing table/constraint that is not exactly 023's, naming what it found, changing nothing.
- [ ] 1.3 023 adds `notes_metadata.chunks_truncated BOOLEAN NOT NULL DEFAULT FALSE` with its own marker comment; same refuse-don't-adopt reconciliation as 022's `links_truncated`. **No backfill and no `indexer_state` row written** — the adoption rule at startup owns that.
- [ ] 1.4 `src/models/db.py`: `_INDEXER_STATE_TABLE_MARKER` and `_CHUNKS_TRUNCATED_COLUMN_MARKER` module constants byte-identical to 023's; `class IndexerState(Base)` with `__tablename__ = "indexer_state"`, the `CheckConstraint` and the `{"comment": ...}` tuple-tail form `IndexerRun` uses, and `_TABLE_MARKER: ClassVar[str]`; `NoteMetadata.chunks_truncated` beside `links_truncated`, with the comment spelling out why it is a column and not only a log line (the error ring buffer is bounded and process-lifetime; the vector tools would read a capped note's head as the whole note).
- [ ] 1.5 `src/config.py`: `MAX_CHUNKS_PER_NOTE = 1_000` as a module constant beside `MAX_LINKS_PER_NOTE`, with the arithmetic in the comment (`MAX_NOTE_BYTES` 10 MiB ÷ `CHUNK_SIZE` 512 tokens ≈ 5,120 chunks; the cap is ~2 MB of cleaned text, far beyond any real note) and the "declared degradation, certified anyway" note. Two `Settings` fields: `embed_chunk_budget_per_user: int = Field(5000, ge=0)` and `embed_time_budget_seconds_per_user: int = Field(300, ge=0)`, `0` disabling each, documented as enforced only when a pass serves more than one active user scope.
- [ ] 1.6 New `src/services/index_state.py` — a separate module so `embeddings.py` and `indexer.py` can both import it without a cycle. `KEY_EMBEDDING_FINGERPRINT` / `KEY_FTS_FINGERPRINT` / `KEY_ROTATION_CURSOR`; `async def get_state(session, key) -> str | None`; `async def set_state(session, key, value) -> None` (upsert, sets `updated_at`); `def embedding_fingerprint() -> str` and `def fts_fingerprint() -> str` returning `json.dumps(obj, sort_keys=True, separators=(",", ":"))` over `{"v":1,"provider","model","dimensions","chunk_size","chunk_overlap"}` and `{"v":1,"configs": sorted(...)}`; `def describe_fingerprint_difference(stored, current) -> str` parsing both and naming the differing fields. The `model` field takes the **active** provider's model through the same branch `get_provider()` uses — one function, so the provider and the model cannot be read from different branches.
- [ ] 1.7 `tests/integration/test_schema_check.py`: 023 cases — fresh (both units present, typed, marked; CHECK validated with the canonical definition; `alembic check` clean), the migration writes no row and backfills nothing, a disallowed `key` is rejected on insert, impostor table / impostor constraint (`CHECK (true)` under the right name) / impostor column each refused naming what was found with nothing changed, stamp-back `alembic stamp 022` → `upgrade head` preserves recorded rows and a set `chunks_truncated`, downgrade drops only marked work.
- [ ] 1.8 `tests/test_issue_206_fingerprint.py`: canonical rendering is stable and single-spelled; provider switch / model change / dimension change / chunk-size change / overlap change each alter the embedding fingerprint; changing the *inactive* provider's model does not; `FTS_CONFIGS` reordering does not alter the FTS fingerprint while a membership change does; `describe_fingerprint_difference` names the changed field.
- [ ] 1.9 `docs/architecture/schema-and-migrations.md`: 023's two units, why the CHECK is not tidiness (a mistyped key reads as absent and absent means *adopt*), why nothing is backfilled (016's argument), and the schema-gate cases.
- [ ] 1.10 `make test-schema` green; `alembic check` clean against a fresh migrate.

## 2. Slice B — the embedding service (owns `src/services/embeddings.py`, new `tests/test_issue_201_embed_outcome.py`, new `tests/test_issue_202_chunk_cap.py`, new `tests/test_issue_200_stale_service.py`). **After A is merged** (imports `MAX_CHUNKS_PER_NOTE`).

- [ ] 2.1 `NoteEmbedOutcome` enum (`EMBEDDED`, `CERTIFIED_EMPTY`, `PROVIDER_FAILED`, `PROVIDER_CARDINALITY_MISMATCH`) and frozen `EmbedNoteResult(outcome, chunks, truncated)` with `__int__` returning `chunks` so `total_chunks += result` and the suite's bare-int expectations keep working.
- [ ] 2.2 `chunk_text_bounded(content, *, chunk_size, overlap, max_chunks) -> tuple[list[str], bool]` returning the first N chunks in document order and whether it truncated; `chunk_text` keeps its signature and delegates. Preserve the `step = max(char_size - char_overlap, 1)` guard (#10) and the short-circuit's existing unstripped-single-chunk behaviour.
- [ ] 2.3 `embed_note` returns `EmbedNoteResult`: zero chunks → `CERTIFIED_EMPTY` (unchanged certify-and-delete); provider raise → `PROVIDER_FAILED`, nothing written; vector/chunk mismatch → `PROVIDER_CARDINALITY_MISMATCH`, nothing written; otherwise `EMBEDDED`. Uses `chunk_text_bounded`; on truncation logs one ERROR naming the path and the cap (never a true chunk count) and reports `truncated=True`. Certification is unchanged — same `certify_embedded` predicate, same stamp-before-delete order, same per-note flush.
- [ ] 2.4 `semantic_search`: add `"stale": bool` (`embedded_content_hash IS DISTINCT FROM content_hash`, computed in Python from the already-hydrated `NoteMetadata`) and `"embedding_truncated": bool` to each result dict; set `"chunk"` to `None` for a stale row instead of the 500-character clip, so a caller cannot obtain the superseded text at all. Statement, `SET LOCAL`s, overfetch, re-sort, dedupe and exact-fallback eligibility unchanged.
- [ ] 2.5 Tests (`test_issue_201_embed_outcome.py`): provider raise → `PROVIDER_FAILED`, no `note_embeddings` row touched, `embedded_content_hash` unchanged; cardinality mismatch → `PROVIDER_CARDINALITY_MISMATCH`, previous vectors intact; zero chunks → `CERTIFIED_EMPTY` and stamped; success → `EMBEDDED`; `int(result)` equals the chunk count in every case.
- [ ] 2.6 Tests (`test_issue_202_chunk_cap.py`): a note over the cap yields exactly `MAX_CHUNKS_PER_NOTE` rows holding the first N chunks in document order, `truncated=True`, one ERROR line, and **is certified**; a note at the cap exactly is not marked; a note edited under the cap re-embeds with `truncated=False`; cardinality is still exact over the capped list (one vector short → refused).
- [ ] 2.7 Tests (`test_issue_200_stale_service.py`): a row whose hashes differ is returned with `stale=True` and `chunk is None`; a NULL `embedded_content_hash` is stale (`IS DISTINCT FROM`, not `!=`); a fresh row keeps its 500-character chunk; `embedding_truncated` mirrors the column; the result set is identical in membership and order to the pre-change one for the same corpus.

## 3. Slice C — startup guards, the reset script and operator docs (owns `src/main.py`, `scripts/reset_embeddings.py`, `.env.example`, `README.md`, new `tests/test_issue_206_startup_guards.py`). **After A is merged. Runs in parallel with B and D.**

- [ ] 3.1 `src/main.py`: `_check_embedding_fingerprint()` in `_check_embedding_dim`'s shape — read `indexer_state`, absent table → return (defer to alembic), absent key → adopt, write, log WARNING that it is assumed and not verified; equal → silent; different → `logging.critical` naming both fingerprints and the differing fields, pointing at `make reset-embeddings`, then `sys.exit(1)`.
- [ ] 3.2 `src/main.py`: `_check_fts_fingerprint()` — same reads, same adoption, but a mismatch logs at ERROR naming both lists, the differing entries and `make rebuild-tsvectors`, and **returns**. It SHALL NOT rewrite the stored value on a mismatch; only `rebuild_tsvectors` does (Slice E), or a guard that fires once would then go silent while the index stayed stale. Cite `_check_mount_identity_support` as the warn-and-serve precedent.
- [ ] 3.3 Lifespan: call both **after** `_validate_fts_configs()` (`main.py:275`), so a typo'd config name still fails with `validate_fts_configs`'s own message listing the installed configs rather than as an opaque fingerprint diff. Both sit below the sandbox short-circuit at `main.py:263`, so `MCP_SANDBOX_MODE` skips them with everything else — assert that.
- [ ] 3.4 `scripts/reset_embeddings.py`: record the current embedding fingerprint in the same transaction as the column recreate and the `embedded_content_hash` wipe, before the commit. Keep the `docker compose run --rm` contract intact (#142) — the script must read the edited `.env`, which is exactly what makes it write the *new* fingerprint.
- [ ] 3.5 `.env.example`: add the file's existing sentence — "Changing post-deploy requires \`make reset-embeddings\`." — to the comment above `EMBEDDING_MODEL` (`:127`) and above `OPENAI_EMBEDDING_MODEL` (`:140`).
- [ ] 3.6 `README.md`: widen "Switching providers" (`:915`) to cover a model change within one provider and to name the startup fingerprint refusal beside the existing dimension-guard paragraph; add the reset pointer to the `EMBEDDING_MODEL` and `OPENAI_EMBEDDING_MODEL` rows of the config table (`:896`, `:900`) and the rebuild pointer to `FTS_CONFIGS` (`:887`).
- [ ] 3.7 Tests (`test_issue_206_startup_guards.py`): model change → `SystemExit(1)` with both fingerprints and the field named; chunk-size and overlap changes → same; FTS list change → warns, returns, stored value unchanged, a second start warns again; FTS reorder → silent; absent key → adopted, written, warned once, second start silent; absent table → returns without deciding; sandbox mode → neither read nor written; the reset script writes the new fingerprint and a subsequent check is silent.

## 4. Slice D — the panel (owns `src/control_panel/routes.py`, `src/control_panel/templates/dashboard.html`, `docs/architecture/control-panel.md`, new `tests/test_issue_201_dashboard.py`). **After A is merged. Runs in parallel with B and C.**

- [ ] 4.1 One shared predicate helper for "not current" (`embedded_content_hash IS NULL OR embedded_content_hash IS DISTINCT FROM content_hash`), called by the dashboard and by `/settings/reset-embeddings/progress` so the two cannot drift. The progress endpoint keeps its admin-only, unscoped behaviour and its existing JSON keys.
- [ ] 4.2 `dashboard()` (`routes.py:383`): add `stats.notes_pending` and `stats.notes_chunks_truncated`, **scoped by `_scope_user_id(user)` exactly as the coverage counts above them are**. Leave `embedding_pct` and `notes_with_embeddings` untouched — the bar keeps meaning "has at least one vector".
- [ ] 4.3 `templates/dashboard.html:110-125`: render the pending count and the truncated count beside the Coverage row, with the pending figure carrying a short label distinguishing it from coverage ("N not current"). Tokens only, no new CSS values — `checks/token_coverage.py` must stay green.
- [ ] 4.4 The Danger-zone reset paths (`routes.py:2007-2013`, `:2028`, `:2057`) record the current embedding fingerprint in the same locked transaction as the wipe.
- [ ] 4.5 `docs/architecture/control-panel.md`: the two coverage questions and why the bar is not redefined; the scoping rule for the new counts and why an unscoped copy of the poller's query would leak another tenant's backlog.
- [ ] 4.6 Tests: a fully-covered, fully-stale vault renders 100% with a pending count equal to the note count; a non-admin's pending count excludes another user's notes; a zero pending count is still rendered; a capped note is counted; the progress endpoint's response is byte-compatible with its previous shape; the Danger-zone reset writes the fingerprint.

## 5. Slice E — the pass (owns `src/services/indexer.py`, new `tests/test_issue_201_pass_record.py`, new `tests/test_issue_202_fairness.py`, new `tests/integration/test_issue_202_rotation_pg.py`). **After A and B are merged** (imports `EmbedNoteResult` and the state accessor).

- [ ] 5.1 `_embed_vault_pinned` consumes `EmbedNoteResult`: `outcome.embedded += 1` only for `EMBEDDED` / `CERTIFIED_EMPTY`; `PROVIDER_FAILED` and `PROVIDER_CARDINALITY_MISMATCH` go through `outcome.record_failure(...)`, so `failure_summary` reaches `PassStats.errors` and the run row's `error`. The exclusion branch, the hash-mismatch skip and `StaleCertification` keep their present, non-failure handling. `_record_index_run` is **not** touched — #160's asymmetry.
- [ ] 5.2 Set/clear `notes_metadata.chunks_truncated` from `EmbedNoteResult.truncated` in the certifying transaction; clear it on the exclusion branch and on `CERTIFIED_EMPTY`, since both leave the note with no vectors.
- [ ] 5.3 `_reconcile_exclusions` (`indexer.py:2683`): same consumption of the result at `:2807`; its zero-chunk probe at `:2796` uses the bounded chunker so "produces no chunks" means the same thing in both places.
- [ ] 5.4 `_active_user_ids()` gains `ORDER BY User.id`. New `_rotated_user_ids()` reads `indexer_state['embed_rotation_cursor']` and rotates the ordered list to start at the smallest id strictly greater than it, wrapping; used by `run_indexer_loop` only (`:3358` startup, `:3431` tick). `_reindex_background` and `scripts/rebuild_tsvectors.py` keep calling the unrotated function.
- [ ] 5.5 After each user's per-user sequence finishes — success or failure — write the cursor to that user's id, in its own short session opened by the lock holder **after** the wrapped body's session has closed (`_write_indexer_run`'s discipline), wrapped so a failure is logged and swallowed and can never fail the pass.
- [ ] 5.6 Per-user budget: a small accumulator (chunks embedded, wall-clock elapsed) created per user per pass, consumed by the backlog loop and by the sweep, checked at the two note boundaries beside `_is_paused()` (`:2530`, `:2744`) and **never inside a note**. Enforced only when the pass is serving more than one active user scope; skipped entirely when both settings are `0`; **never before the first note of that user's pass has completed**. A stop logs one WARNING and writes nothing to `error`.
- [ ] 5.7 `_rebuild_tsvectors_pinned` records the current FTS fingerprint inside its single atomic transaction, so a rollback records nothing.
- [ ] 5.8 Tests (`test_issue_201_pass_record.py`): a provider that raises for every note → run row with non-null `error` naming N of M and the first message, `notes_embedded = 0`, and a row distinguishable from an empty-backlog pass; one failing note among many → count and error both correct; an excluded / hash-mismatched / paused note contributes no failure; the heartbeat stays ok through a total outage while the run row says failed.
- [ ] 5.9 Tests (`test_issue_202_fairness.py`): two users, the first over budget → the first stops at a note boundary and the second is served in the same pass; a note in flight when the budget is exhausted is finished and certified; a user whose first note alone exceeds the budget still embeds it; a single active scope is unbudgeted; a budget stop leaves `error` null; a budget-stopped sweep leaves repaired rows repaired and the next pass completes the remainder.
- [ ] 5.10 Tests (`tests/integration/test_issue_202_rotation_pg.py`): a pass truncated after user B resumes at C; a cursor naming a deleted user resolves to the next id and does not raise; a complete cycle wraps to the first id; a manual reindex leaves the cursor unchanged; a raising cursor write does not fail the pass.

## 6. Slice F — the tool surface (owns `src/mcp_server/tools.py`, `src/mcp_server/server.py`, new `tests/test_issue_200_tool_render.py`). **After B is merged. Runs in parallel with E.**

- [ ] 6.1 `find_related_stmt` (`tools.py:1912`) selects `NoteMetadata.content_hash` and `NoteMetadata.embedded_content_hash` (and `NoteMetadata.chunks_truncated`) alongside its six existing columns; per-row dict gains `stale` and `embedding_truncated`; `chunk` is `None` for a stale row. No predicate, `SET LOCAL`, overfetch, re-sort, dedupe or fallback change — `tests/integration/test_search_recall.py` EXPLAINs this statement.
- [ ] 6.2 `find_related_impl`: compute the **source**'s own staleness from the row it already loaded at `:1956` and state it once above the list; keep both existing error markers and the true-zero-result path exactly as they are.
- [ ] 6.3 Render, in `get_links_impl`'s idiom (`tools.py:1721-1769`): the header line of both tools carries the stale count **always, including zero**; a stale row carries `— stale: true` and its preview line is replaced by the withheld notice naming `read_note`; a truncated row carries `— embedding_truncated: true`; when any row is stale, a bold trailing block says how many and that the previews were withheld while the paths and titles are current.
- [ ] 6.4 `semantic_search_impl` (`tools.py:1164`): same rendering; handle `chunk is None` (today's `r['chunk'][:200]` would raise).
- [ ] 6.5 `server.py` docstrings for `semantic_search` (`:274`) and `find_related` (`:561`) gain a declared-degradation paragraph in `get_links`'s voice (`server.py:523-526`): what `stale: true` means, that the preview is withheld and `read_note` is the remedy, what `embedding_truncated: true` means, and — for `find_related` — that a stale source makes every neighbour an answer about the note's previous content.
- [ ] 6.6 Tests (`test_issue_200_tool_render.py`): a stale row is present, marked, and carries no note text; the header count is rendered when it is zero; a fresh row's preview is unchanged; `find_related` with a stale source states it once; the withheld notice contains nothing read from the note's current bytes; a capped note is marked in both tools; `usage_logs.params` gains no new key beyond the existing markers.

## 7. Slice G — the architecture notes (owns `docs/architecture/indexing-and-embeddings.md`, `docs/architecture/search.md`). **After E and F are merged.**

- [ ] 7.1 `indexing-and-embeddings.md`: the typed embed outcome and why it is a return value rather than a raise (the sweep's declared convergence exception); the chunk cap as a declared degradation in the keyword-retreat/link-cap voice, including why a capped note is certified (#127) and why the ERROR line cannot name the true count; the persisted rotation cursor and why an in-memory one is a no-op; the note-boundary budget, its at-least-one-note clause, its single-scope exemption, and why it writes nothing to `error`; the two fingerprints, their format, the adoption rule, the fail-closed/warn asymmetry, and the "only the repair writes the fingerprint" rule.
- [ ] 7.2 `search.md`: the staleness annotation, `IS DISTINCT FROM` rather than `!=`, why filtering was refused (the edit window, the outage, and the exact-fallback interaction), why the preview alone is withheld and everything else kept, and the `find_related` stale-source line.

## 8. Verification

- [ ] 8.1 Merge A; then B, C, D; then E, F; then G. Run the full suite **once on the merged result** — that is the authoritative gate, not the per-worktree runs. Grep for production callers of every new export (`chunk_text_bounded`, `EmbedNoteResult`, `get_state`/`set_state`, `embedding_fingerprint`, `fts_fingerprint`, `_rotated_user_ids`) before believing a slice is done.
- [ ] 8.2 `make test-schema` green (023 carries a table, a CHECK and a column).
- [ ] 8.3 `make audit` (pip-audit) clean.
- [ ] 8.4 `openspec validate --strict` valid.
- [ ] 8.5 `openspec-verifier` subagent pass against the spec deltas; iterate to zero blocking gaps.
- [ ] 8.6 Adversarial Codex review, framed for this product: the expensive failures are a destructive write and a silently wrong search result an agent acts on unseen. Point it at the chunking/embedding path and the two vector read paths — mandatory triggers per `CLAUDE.md`. Fix BLOCKER/MAJOR before shipping; record accepted limitations.
- [ ] 8.7 `make deploy` **with the embedding configuration unchanged** (design D7 risk: the first startup adopts whatever it finds). Confirm `make db-check` clean, that the startup log shows both fingerprints adopted exactly once, and that `indexer_state` holds two rows.
- [ ] 8.8 Live MCP exercise against the deployed server, naming the tools called: `create_note` a small note, `semantic_search` for it before the next pass (expect it absent or `stale: true` with the preview withheld), then again after the pass (expect a preview and `0 stale` in the header); `edit_note` it and repeat to see a single stale row among fresh ones; `find_related` on the edited note before the pass completes (expect the stale-source line); `read_note` to confirm the current text is what the notice points at; `get_links` on the same note to confirm the shipped `truncated:` token is unaffected. Delete the test note afterwards.
- [ ] 8.9 Post-deploy: confirm the dashboard's pending count moves with the backlog and returns to zero, and that the coverage bar's value is unchanged from before the deploy for an unchanged vault.
- [ ] 8.10 Archive the change; PR closing #201, #200, #202, #206; update the ASVS report's issue rows in the vault and record the two residuals (`semantic_search` hydrating full vectors; `OPENAI_BASE_URL` excluded from the fingerprint) as follow-up issues.
