Slices A–D are file-disjoint; the ownership lists below are the contract, and a finding outside your own files is reported, not fixed. **Slice A lands first** — B, C and D all import `src/services/security_events.py` and must not stub it. B, C and D then run in parallel worktrees. `src/oauth/grants.py` is deliberately **not** owned by any slice: `revoke_grant_family` keeps returning its rowcount and gains no logging (design D10).

## 1. Slice A — the logging foundation (#190)

Owns: `src/logging_setup.py` (new), `src/services/security_events.py` (new), `src/config.py`, `src/main.py`, `src/mcp_stdio.py`, `src/services/error_log.py`, `src/mcp_server/auth.py`, `src/services/vault.py`, `src/services/quotas.py`, `CLAUDE.md`, `docs/architecture/security-event-logging.md` (new); tests `tests/test_issue_190_log_format.py`, `tests/test_issue_190_field_allowlist.py`, `tests/test_issue_190_suppressor.py`, `tests/integration/test_issue_190_log_config_process.py`.

- [x] 1.1 `src/config.py`: add `LOG_LEVEL` (default `INFO`) and `LOG_FORMAT` (`json` | `text`, default `json`) settings, refusing an unknown format at startup.
- [x] 1.2 `src/logging_setup.py`: `configure_logging(level=None, fmt=None)` per design D1 — walk the root's handlers, remove and close every one **except** `error_log.installed_handler()`, install a single `StreamHandler(sys.stderr)`, set the root level, re-apply the `httpx`/`httpcore` WARNING floors, idempotent, never raises. The module docstring records why this is not `basicConfig(force=True)`.
- [x] 1.3 `src/logging_setup.py`: the JSON formatter per D2/D16 — `ts` (UTC ISO-8601, ms, `Z`), `level`, `logger`, `msg` (bounded), `stack` (only with `exc_info`; **head 4 KiB + tail 3 KiB with an elision marker naming the dropped byte count, and the exception type line and the traceback's final line guaranteed present**) and `error_type` from the exception class; then the allow-listed `extra` keys, checked against their declared type and truncated to their declared bound. Three disjoint declarations per D18: `FORMATTER_OWNED` = {ts, level, logger, msg, stack} (produced here; dropped, and strict-raising, if a call site passes one), `EMITTER_CONTROL` = {permit, event, subject, level, exc_info} (consumed by the emitter, never rendered as data), and `ALLOWED_FIELDS` (name, type, bound) for everything a call site may pass. `error_type` is dual: derived from `exc_info` when present, accepted as a field otherwise, **exception wins**. **A value failing its type check is dropped, never converted** — `user_id="x"` yields no `user_id` — string truncation is not a conversion, every field is optional, and the formatter never raises. The `text` renderer emits the same object as one line of `ts level logger msg k=v …`.
- [x] 1.4 `src/services/error_log.py`: add `installed_handler()` returning the module's handler instance or `None`, so the reconfiguration can exempt it. `attach()`/`detach()` semantics and the observation window are unchanged.
- [x] 1.5 `src/main.py`: replace the `logging.basicConfig` at `:32` with `configure_logging()`, positioned after the import block so it runs after `src.mcp_server.server` has constructed `FastMCP`; wrap `_rate_limit_exceeded_handler` at `:299` in a local handler emitting `rate_limit_exceeded` (`route`, `method`, `client_ip`, `limit_count`, `window_seconds`) and returning the delegate's response unchanged; register the suppressor's shutdown flush in the lifespan.
- [x] 1.6 `src/mcp_stdio.py`: call `configure_logging(level=WARNING)` in place of `basicConfig`; register the suppressor flush via `atexit`; a test asserts nothing reaches stdout.
- [x] 1.7 `src/services/security_events.py`: `EVENT_FIELDS` (the catalogue table as a per-event permitted field set — `emit` drops an undeclared field and raises under the test suite's strict flag), `acquire(event, subject) -> Permit | None` and `emit(permit_or_event, *, level=WARNING, exc_info=None, subject=None, **fields)` — **the allowance is charged once, at `acquire`, and consuming a permit performs no second check** (D7; there is no `should_emit`) — `redacted_token_tag(value)` (the exact behaviour of `_redacted_prefix`; the field is **absent, not empty, when there is no value**), and `client_ip(request)` (D3 — `request.client.host` only, never a header).
- [x] 1.7a The suppressor of D7: subject is `user_id` when the request already resolved one, else the trusted client IP, else `-`, and **must be computable before any work the permit gates** (**never** `token_tag`, `username_submitted` or `client_id_submitted`); `MAX_EVENTS_PER_WINDOW` = 10 per `(event, subject)` and `MAX_EVENTS_PER_SUBJECT_PER_WINDOW` = 50 per subject, per 60 s window; **every level is accounted, INFO included** (owner decision); ≤ 512 keys per map evicted oldest-window-first, and **an entry holding a nonzero withheld count emits its summary before it is evicted**; summaries are also emitted lazily before the next acquire for a key and flushed at shutdown, carry the suppressed event's own level, and are never suppressed or counted; lock-guarded; **fails open** (an internal error returns a permit).
- [x] 1.8 `src/mcp_server/auth.py`: `_redacted_prefix` delegates to `redacted_token_tag` (one definition); the ten `auth_failure` sites **move from `logger.warning` to `security_events.emit`** (D18's table — they are the highest-volume caller-triggerable refusal in the server and must pass the permit), move `key_prefix` to `token_tag`, **move the OAuth branches' `oauth_tokens.id` out of `key_id` into `oauth_token_id`** (D15), and gain `client_ip` and `route`. The bare no-Bearer 401 at `:96` stays unlogged (design Non-Goals).
- [x] 1.9 `src/services/vault.py` and `src/services/quotas.py`: **migrate the four caller-triggerable events to `security_events.emit`** so they pass the same permit (D18's table) — `quota_admission_failed` (ERROR), `quota_counter_prune_failed`, `publication_refused_confirmation_unavailable`, `publication_refused_vault_assignment_changed` — replacing `extra={"error": str(exc)}` with `error_type` as you go. `src/services/vault.py:1826`'s post-publish flush note is **not** a refusal and stays on the bare logger.
- [x] 1.10 Tests — `tests/test_issue_190_log_format.py`: one physical line per record including a traceback; a long traceback is head/tail elided with the type line and final line present and a short one is whole; UTC `Z` timestamp parses; `configure_logging()` idempotent; the ring handler survives in **both** call orders and still receives an ERROR; the `text` renderer emits the same fields.
- [x] 1.11 Tests — `tests/test_issue_190_field_allowlist.py`: an unknown key is dropped; a field the event does not declare is dropped (and raises under the strict flag); **a mistyped value is dropped rather than converted** and the call does not raise; each string is truncated to its bound; `redacted_token_tag` is stable and no record contains a ≥12-character substring of its input; the tag field is **absent** when no credential was presented; `client_ip` ignores a forged `X-Forwarded-For` from an untrusted peer; `EVENT_FIELDS ⊆ ALLOWED_FIELDS` and every catalogue row has an entry; the three name spaces are disjoint and a call site passing a `FORMATTER_OWNED` name gets the formatter's value, not its own; `error_type` from `exc_info` wins over a passed one; and the **AST sweep** of D14 — parse every module under `src/`, collect field names from `logger.<level>(..., extra={...})` and from `security_events.emit(...)` keywords **excluding `EMITTER_CONTROL` keywords and rejecting `FORMATTER_OWNED` ones**, fail on any name outside `ALLOWED_FIELDS` (and outside that event's `EVENT_FIELDS` where the event name is a literal) **and on any dynamic expansion** (`extra=<name>`, `{**base}`, `emit(**fields)`), naming file, line and key.
- [x] 1.12 Tests — `tests/test_issue_190_suppressor.py`: the per-`(event, subject)` cap; the per-subject cap across events; a second subject is unaffected; **rotating token tags from one client address share one allowance**; **an INFO flood is bounded and its summary carries the exact count**; **acquiring a permit and emitting it charges the allowance exactly once** (and an acquired-but-unspent permit stays charged); the summary is emitted lazily before the next acquire and carries the withheld count; **an entry holding a withheld count emits its summary before eviction** (fill the map past its bound and assert no count is lost); an outstanding count is flushed at shutdown; a summary is never suppressed; an internal failure fails open (a permit is returned and the record is emitted).
- [x] 1.13 Test — `tests/integration/test_issue_190_log_config_process.py`: spawn a **subprocess** that imports `src.main`, logs a record with `extra`, and prints the root handler list; assert exactly one non-ring handler, that the captured stderr line parses as JSON, and that the extra field is present. This is the actual-process assertion the #190 audit note requires.
- [x] 1.14 Docs: `docs/architecture/security-event-logging.md` — why the SDK wins the import race and what the reconfiguration does instead of `force=True`; the allow-list with the provenance classes and the message/traceback rule; the **whole event catalogue table** from `design.md`; the suppressor, its subject rule and what it deliberately does not suppress; the residuals (nothing bounds event admission — `mcp-rate-limits` does; retention is an operator concern; per-process suppression under multiple workers); why WARNING events do not reach the health page's buffer. Add the index row to `CLAUDE.md`.

## 2. Slice B — authentication events (#191). Starts after A is merged.

Owns: `src/auth/routes.py`, `src/oauth/routes.py`, `src/auth/passwords.py`; tests `tests/test_issue_191_panel_auth_events.py`, `tests/test_issue_191_oauth_events.py`, `tests/test_issue_191_secret_canaries.py`.

- [ ] 2.1 `src/auth/routes.py`: split the merged failure condition at `:173-180` into `unknown_user` / `inactive_user` / `bad_password` **for the reason code only** — one shared `_render_login(..., status_code=401)` return, byte-identical across the three. Emit `panel_login_failed` (`username_submitted`; `user_id` only where a row resolved) and `panel_login_succeeded` **after the `last_login_at` commit at `:186`** and before the session write (D17).
- [ ] 2.2 `src/auth/routes.py`: `panel_logout` in `logout`, reading the session **before** `request.session.clear()` at `:205` and emitting `user_id_session`/`username_session` — the suffix is the provenance, and no database lookup is added on the logout path; `panel_bootstrap_admin_created` **after the commit at `:328`** (D17 — not after the insert, or a failed commit leaves a false success record) and `panel_bootstrap_refused` at each of `:234, 242, 250, 258, 267, 285` with its own reason code.
- [ ] 2.3 `src/oauth/routes.py` `/token`: `oauth_token_issued` at `:757` and `oauth_token_refreshed` at `:918`, **both after their commit** (D17); `oauth_token_refused` at every refusal listed in the catalogue, each carrying `<rfc_code>.<sub_reason>` and using `client_id_submitted` until a client row resolves, with `user_id`/`grant_id` **omitted** on the early refresh failures at `:772`/`:808` that have neither; `oauth_token_rotation_failed` at `:914-916` with `exc_info` (the traceback is discarded today). Exactly one emission per outcome — no emission inside a retry or a fall-through that also emits later.
- [ ] 2.4 `src/oauth/routes.py` `/authorize`: `oauth_consent_granted` after the commit at `:583`, `oauth_consent_denied` at `:493` (resolve the session user for the record — a deny carries no identity today), `oauth_authorize_refused` at each refusal, and `oauth_cross_user_client_refused` inside `_cross_user_client_error` (`:127-139`) so both `:528` and `:570` inherit it.
- [ ] 2.5 `src/oauth/routes.py` `/register` and `/revoke`: `oauth_client_registered` **after the registration commits**, carrying `client_name_submitted` (client-supplied text) and never the secret; `oauth_client_registration_refused` with reason codes; `oauth_revoke_noop` at `:942, 948, 982, 983` (`client_id_submitted`); `oauth_revoke_refused` at `:985`; `oauth_grant_revoked` **after the commit** at `:1003-1004`, carrying the rowcount `revoke_grant_family` returns plus the resolved client, user, grant and request context (design D10 — nothing is emitted inside `grants.py`, which this slice does not touch).
- [ ] 2.6 Tests: each outcome produces exactly one emission attempt with the expected reason (assert the acquire-call count with the suppressor forced open); the three login failures return byte-identical responses; the revocation no-ops keep their 200-with-empty-body; **a failed commit emits no success record** — force the bootstrap commit, the `last_login_at` commit and the revocation commit to raise and assert nothing was emitted for them; the unsuffixed identifier fields appear only on the paths where a row resolved.
- [ ] 2.6a `src/auth/passwords.py`: migrate the malformed-bcrypt-hash warning to `security_events.emit("password_hash_malformed", user_id=…)` (D18's table) — a caller can drive it through the login form.
- [ ] 2.6b Confirm every OAuth event that has a request in hand carries `client_ip` — the token exchange, the refresh, `oauth_token_rotation_failed` and `oauth_cross_user_client_refused` included.
- [ ] 2.7 Test (`tests/test_issue_191_secret_canaries.py`): two halves per design D16. **Submitted:** a distinct 32-character random canary in every caller-controlled credential position (password, client secret, authorization code, refresh token, PKCE verifier, session cookie, CSRF token, MCP bearer, transfer bearer) through the real routes. **Generated:** capture what the server produced — the DCR `client_secret` (**not** the `client_id`, a public identifier the catalogue logs deliberately), the `/authorize` code, the access/refresh pair from an exchange and from a rotation, an `omcp_` key from key creation, and a minted transfer token — and assert absence of those too. No captured record may contain any of these values or any ≥12-character substring of one, in any field, message or traceback.

## 3. Slice C — the tool surface (#192 write refusals, #193 exceptions). Starts after A is merged; parallel with B and D.

Owns: `src/mcp_server/tools.py`, `src/services/usage_stats.py`, `docs/architecture/usage-attribution.md`; tests `tests/test_issue_192_write_refusal_marker.py`, `tests/test_issue_193_tool_exception.py`, `tests/integration/test_issue_193_tool_exception_pg.py`.

- [ ] 3.1 `src/mcp_server/tools.py`: declare `_PERMISSION_DENIED_MARKER = "permission_denied"` and `_TOOL_EXCEPTION_MARKER = "tool_exception"` beside the existing marker block at `:287-372`.
- [ ] 3.2 `_require_write` (`:1213`): record the marker through `timing.record("error", _PERMISSION_DENIED_MARKER)` and emit `tool_write_refused` (tool, user id, actor) **at this one definition**, so all nine call sites inherit both. Resolve the tool name from the tracked context rather than a new parameter, so no caller signature changes. `request_download`'s `_mint_preflight(need_write=False)` path stays unaffected.
- [ ] 3.3 `_tracked` (`:665-737`): restructure per design D5 so the `except Exception as exc` guards **only** `result = await fn(*args, **kwargs)` at `:711` — not the three admission gates before it and not the parameter/`_log_usage` work after it. The outer `try`/`finally: timing.clear(token)` is unchanged. In the handler: measure the duration, emit `tool_exception` at ERROR with `exc_info`, then the best-effort row (3.4), then a bare `raise`. **`Exception`, never `BaseException`, for the body.**
- [ ] 3.4 `_log_usage` (`:210`): return an explicit status — `True` when a row was inserted, `False` when it gave up — with its existing swallow-everything behaviour and every existing caller unchanged. In `_tracked`'s exception handler the audit `await` is wrapped in `except BaseException as audit_exc` (the one place `BaseException` appears, scoped to this bookkeeping await): log `tool_usage_log_failed` with the audit failure's class and **re-raise the original tool exception**, so a cancellation arriving during the audit never replaces it. The row carries `_truncate_params` + `timing.current()` with `error`/`error_type` merged over the top, the measured duration and `response_size = 0`.
- [ ] 3.4a `src/mcp_server/tools.py`: **migrate every caller-triggerable event in this module to `security_events.emit`** (D18's table), so they pass the same permit as the new ones: `tool_refused_no_vault` (:477), `tool_refused_over_quota` (:522), `usage_log_credential_gone` (:252), the two `f"Failed to log usage for {tool}"` warnings → `usage_log_failed` (`tool`, `error_type`, `reason` = `initial` | `after_clearing_fks`), `"could not measure a structured tool result"` → `tool_result_measure_failed`, and `"Failed to rewrite links in %s"` → `move_rewrite_failed` (the path stays in `msg`, per D16). These events carry no `client_ip` — nothing below the middleware has one (Residual R8).
- [ ] 3.5 `src/services/usage_stats.py`: leave `PRE_BODY_REFUSAL_ERROR_MARKERS` unchanged and add a comment naming both new markers as deliberately classified post-body, with the reason, so the next reader does not re-derive it.
- [ ] 3.6 Tests (`tests/test_issue_192_write_refusal_marker.py`): a read-only credential on each of the nine gated tools produces a row carrying `permission_denied` and one warning record; the refusal string is unchanged; `request_download` produces neither; the marker does not match `pre_body_refusal_sql`; the row is written for every call even when the log records are being suppressed; and a flood of no-vault / over-quota refusals is bounded and accounted by the suppressor rather than reaching the sink through a direct logger call.
- [ ] 3.7 Tests (`tests/test_issue_193_tool_exception.py`): a raising body emits one ERROR with the class name and a traceback and writes exactly one row with `error = tool_exception`, `error_type` and a non-negative duration; the same exception instance propagates; **a body that completes and a failure raised afterwards in the telemetry tail produces no `tool_exception`**; **a quota-gate failure before the body produces no `tool_exception`**; a `_log_usage` returning `False` logs `tool_usage_log_failed` and leaves the original exception intact; a cancellation **during the audit await** is superseded and the original tool exception propagates (D11 — the supersession is deliberate and the test pins it, including that the coroutine unwinds rather than continuing); `CancelledError` from the body writes no row and emits no record; a body that recorded a post-body marker and then raised is written as `tool_exception`; the phase-timing holder is still cleared in `finally`.
- [ ] 3.8 Test (`tests/integration/test_issue_193_tool_exception_pg.py`): against PostgreSQL, a raising tool's row is present and readable, is counted as executed by `executed_sql`, and does not break `/admin/performance`'s aggregates for that window.
- [ ] 3.9 Docs: `docs/architecture/usage-attribution.md` — extend the marker register with `permission_denied` and `tool_exception` on the post-body side (with the reasoning that puts them there), declare `error_type` a reserved `params` key of type string that no reader casts, and record that in-band tool refusals outside the enumerated set stay unmarked (residual R1).

## 4. Slice D — HTTP refusal surfaces and the usage page (#192). Starts after A is merged; parallel with B and C.

Owns: `src/csrf.py`, `src/control_panel/routes.py`, `src/control_panel/users.py`, `src/api/routes.py`, `src/transfer/routes.py`, `src/services/transfer.py`, `src/services/usage_filters.py`, `src/control_panel/templates/usage.html`, `docs/architecture/control-panel.md`, `docs/architecture/file-transfer.md`; tests `tests/test_issue_192_transfer_refusal_log.py`, `tests/test_issue_192_panel_forbidden.py`, `tests/integration/test_issue_192_usage_outcome_pg.py`.

- [ ] 4.1 `src/services/transfer.py`: add the frozen `TransferRefusal(reason, row=None)` and the **read-only** `classify_token_refusal(session, token, *, direction)` of design D8 — select by token hash alone and derive the reason under a **total precedence**: `unknown_token` → `wrong_direction` → state (`already_claimed` | `already_completed` | `already_consumed`) → `expired` → `claim_lost`. **`lookup_token` and `claim_upload` keep their single filtered query and their semantics exactly**; the helper takes no admission decision. Unit-test the overlapping cases (expired **and** consumed → consumed; expired **and** wrong direction → wrong direction; pending, unexpired, right direction → claim lost).
- [ ] 4.2 `src/transfer/routes.py`: `_load_valid` (`:163-180`) returns `TransferRefusal` instead of bare `None` for the three predicates it evaluates itself (`credential_invalid`, `root_reassigned`, `path_invalid`); its accept path still returns the row. Emit `transfer_refused` at every `_not_found()` site (`:261, 265, 280, 284, 374, 382, 391, 412, 527, 668, 673, 680, 691, 697`) with the reason, `redacted_token_tag(token)` where a token was presented (**no tag for `missing_token`**), route, method and `client_ip`; identity only where a row resolved. **Acquire the permit first — `acquire("transfer_refused", subject=client_ip(request))` — and run `classify_token_refusal` only when a permit is returned**, then `emit(permit, …)` (no second allowance check). The subject is the trusted client address because the token's owner is knowable only after the diagnosis the permit gates; a suppressed refusal issues no diagnosis read and an accepted request issues none at all. **Wrap `classify_token_refusal` in its own `try/except Exception`, outside the admission decision**: on failure emit the record best-effort with `reason="diagnosis_failed"` and `error_type`, and return the same `_not_found()` regardless (D8) — a diagnosis read must never turn a 404 into a 500. The gate refusal at `:527` emits the single generic `prepublish_revalidation_failed` (owner decision; Residual R9), while the earlier re-validation at `:391` splits its `or` chain to keep `credential_invalid` / `root_reassigned` / `path_invalid` distinct. `_not_found()` and `NOT_FOUND_BODY` are untouched.
- [ ] 4.2a `src/transfer/routes.py`: migrate the module's existing caller-triggerable log lines to `security_events.emit` (D18's table) — `transfer_refused_mount_boundary` (×2), `transfer_refused_unsupported_fs`, `transfer_root_unusable`, `transfer_post_publish_failure` (`exc_info`), `transfer_prepublish_failure` (`exc_info`) — keeping their existing levels and their 503/response behaviour exactly.
- [ ] 4.3 `src/control_panel/routes.py`: `require_admin_panel` (`:225`) gains `request: Request`; emit `panel_forbidden` there (`:234`) and in `_assert_key_owner` (`:713`), `_assert_oauth_client_owner` (`:1055`) and `_assert_oauth_token_owner` (`:1067`), each with its reason code, carrying `actor_user_id`/`actor_username` for the refused principal and `user_id` for the resource's owner where one is known (absent for `admin_required`) — the three helpers take a keyword-only `request` from their call sites. Also migrate this module's caller-triggerable failures to `security_events.emit` (D18's table): `panel_ondemand_index_failed`, `panel_ondemand_embed_failed`, `panel_health_strip_failed`. Emit `oauth_grant_revoked` in the panel's revoke handler **after its commit** (D10), carrying `actor_user_id` (the acting administrator) alongside `user_id` (the grant's owner) — D19.
- [ ] 4.4 `src/api/routes.py`: update the `_assert_key_owner(api_key, user)` call at `:202` for the new signature, and emit `panel_forbidden` with `reason=not_your_key` at the **inline duplicate ownership check** in `revoke_key` (`:224-225`), which the first draft missed.
- [ ] 4.5 `src/control_panel/users.py`: emit `panel_forbidden` with `reason=actor_revoked` at the **three** `_actor_still_privileged` refusals — `edit_user_submit` (`:320`), `delete_user` (`:449`), `reset_password` (`:601`). **`create_user` has neither the lock nor the re-check**, so it has no such refusal to log; do not add one (that is Residual R7, a pre-existing race for a change that owns this critical section). `panel_password_reset` is emitted **after the commit** that follows the write at `:609-611` (D17), carrying `actor_user_id` (the administrator) and `user_id`/`username` (the target), never the new password.
- [ ] 4.6 `src/csrf.py`: emit `csrf_refused` at `:57` with route, method, `client_ip` and the session user id when one is present; the 403 and its detail are unchanged.
- [ ] 4.7 `src/services/usage_filters.py`: `recent_logs` selects three raw values — `ul.params->>'error'`, `ul.params->>'error_type'`, `ul.params->>'over_quota'` — **as text, with no `::boolean` cast** — and exposes all three on the row without discarding any of them.
- [ ] 4.8 `src/control_panel/routes.py` + `src/control_panel/templates/usage.html`: map the three raw values to a display outcome in the route with the precedence of design D9 (`tool_exception` → failure with the class name; any other error marker → refusal with that marker; `over_quota == "true"` → refusal for quota; any other non-empty value → refusal showing the raw value; none → no outcome), and render the badge with existing theme tokens only.
- [ ] 4.9 Tests: every refusal cause on every bearer-protected transfer endpoint yields a **byte-identical** 404 (status, headers, body) and a distinct logged reason; the full existing redemption suite still passes unchanged, proving the accepted set did not move; a missing header logs no tag; a suppressed refusal issues no diagnosis query and an accepted redemption issues none (assert on statement counts); an enumeration burst from one address is bounded on that address's allowance however many distinct tokens it presents; **an injected diagnosis-query failure yields a byte-identical 404 and a `diagnosis_failed` record**; the migrated transfer and panel events pass the suppressor rather than the bare logger; each panel 403 including `api/routes.py:224` and the CSRF 403 emits its record with route and user id; the usage page renders the failure badge for a seeded `tool_exception` row, the refusal badge for a seeded `permission_denied` row, and the raw value for a row whose over-quota value is nonsense, all without a 500.
- [ ] 4.10 Docs: `docs/architecture/file-transfer.md` — the refusal log beside the uniform-404 section: the log is the only place the reason exists, the response is still the sole external answer, the diagnosis is read-only, runs after the decision and only behind the suppressor, and `lookup_token`/`claim_upload` were deliberately not re-shaped. `docs/architecture/control-panel.md` — the ring buffer's survival rule (cross-reference the new note), the usage page's outcome column, its precedence, and why the values are read as text.

## 5. Integration and verification

- [ ] 5.0 Cross-slice check on the merged tree: no `logger.warning`/`logger.error`/`logger.exception` call remains in `src/mcp_server/auth.py`, `src/mcp_server/tools.py`, `src/transfer/routes.py` or `src/control_panel/routes.py` outside D18's table (a test asserts it), and `EVENT_FIELDS` covers every catalogue row.
- [ ] 5.1 Merge A, then B, C and D; run the full suite once on the merged result (not on the per-slice worktrees), plus `make audit` and `make db-check` (nothing here touches the schema — `alembic check` must still say "No new upgrade operations detected", and **no migration number is claimed; 024 stays free**).
- [ ] 5.2 `npx -y @fission-ai/openspec@1.3.1 validate --all --strict` clean.
- [ ] 5.3 `openspec-verifier` subagent against the merged tree; iterate to zero blocking gaps.
- [ ] 5.4 Adversarial Codex pass (mandatory — this change touches the write-refusal path, the transfer refusal surface and `_tracked`). Frame it as a defensive control review: does any record carry credential material, does any response change, does the diagnosis move the accepted-token set, can the suppressor drop something that matters or be reset by a rotating credential, can the exception path mask or alter an exception or report a completed write as failed, can a log call raise into a request path. Fix BLOCKER/MAJOR; re-run naming what was addressed.
- [ ] 5.5 `make deploy`; confirm `make db-check` clean and `make logs` now shows one JSON object per line with a UTC `Z` timestamp and populated fields on an `auth_failure`.
- [ ] 5.6 Live MCP exercise (no browser gate in this project — name the tools actually called). With a **read-only** credential: `create_note`, `edit_note`, `write_file`, `delete_file`, `request_upload` → each refused, each producing one `tool_write_refused` record and a `permission_denied` row; `keyword_search`, `read_note`, `list_notes` → unaffected. With a **readwrite** credential: `create_note`, `edit_note`, `set_frontmatter`, `delete_note` on a scratch note; `request_upload` then redeem with a corrupted token against `/transfer/upload/info` → uniform 404 plus one `transfer_refused` record carrying a `sha:` tag and an `unknown_token` reason, and a request with no header → a record with no tag; `check_upload` for the real handle. Confirm on `/admin/usage` that the refused writes render as refusals and the successful ones do not.
- [ ] 5.7 Tool-exception path: identify a tool argument that raises a genuine unhandled exception in a **local** container first (candidates: `read_file` on a directory path, `read_note` for an index row whose file was removed), reproduce it once against the deployed server, and confirm the ERROR record in `make logs`, the `tool_exception` row rendered as *failed* on `/admin/usage`, and the entry on `/admin/health`. Then remove the scratch notes and any test capability.
- [ ] 5.8 `/openspec-archive-change`; PR closing `#190`, `#191`, `#192`, `#193`; update the ASVS report's issue rows in the vault and file the residuals: R1 (unmarked in-body tool refusals), R2 (nothing bounds event admission — carry it to `mcp-rate-limits`, #188/#194), R3 (per-process suppression under multiple workers), R4 (uvicorn's loggers keep their format), R5 (`permission_denied` dilutes write-tool percentiles), R6 (co-located tenants share a suppression allowance behind one source address), R7 (`create_user` takes neither the admin lock nor the actor re-check — a pre-existing race), R8 (MCP tool events carry no `client_ip`), R9 (the publish gate's refusal cause is opaque).
