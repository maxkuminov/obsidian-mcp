Slices A–D are file-disjoint. **Slice A lands first** — B, C and D all import `src/services/security_events.py` and must not stub it. B, C and D then run in parallel worktrees. No slice may edit a file another slice owns; a finding outside your own files is reported, not fixed.

## 1. Slice A — the logging foundation (#190)

Owns: `src/logging_setup.py` (new), `src/services/security_events.py` (new), `src/config.py`, `src/main.py`, `src/mcp_stdio.py`, `src/services/error_log.py`, `src/mcp_server/auth.py`, `src/services/vault.py`, `src/services/quotas.py`, `CLAUDE.md`, `docs/architecture/security-event-logging.md` (new); tests `tests/test_issue_190_log_format.py`, `tests/test_issue_190_field_allowlist.py`, `tests/integration/test_issue_190_log_config_process.py`.

- [ ] 1.1 `src/config.py`: add `LOG_LEVEL` (default `INFO`) and `LOG_FORMAT` (`json` | `text`, default `json`) settings with validation that refuses an unknown format at startup.
- [ ] 1.2 `src/logging_setup.py`: `configure_logging(level=None, fmt=None)` per design D1 — walk the root's handlers, remove and close every one **except** `error_log.installed_handler()`, install a single `StreamHandler(sys.stderr)`, set the root level, re-apply the `httpx`/`httpcore` WARNING floors, idempotent, never raises. Module docstring records why this is not `basicConfig(force=True)`.
- [ ] 1.3 `src/logging_setup.py`: the JSON formatter per D2 — `ts` (UTC ISO-8601, ms, `Z`), `level`, `logger`, `msg` (bounded 16 KiB), `stack` (bounded 8 KiB, only with `exc_info`) and `error_type` derived from the exception class; then the allow-listed `extra` keys, coerced to their declared type and truncated to their declared bound; everything else dropped. `ALLOWED_FIELDS` is the single declaration, with the type and bound beside each name. The `text` renderer emits the same object as one line of `ts level logger msg k=v …`.
- [ ] 1.4 `src/services/error_log.py`: add `installed_handler()` returning the module's handler instance or `None`, so the reconfiguration can exempt it. `attach()`/`detach()` semantics and the observation window are unchanged.
- [ ] 1.5 `src/main.py`: replace the `logging.basicConfig` at `:32` with `configure_logging()`, positioned after the import block so it runs after `src.mcp_server.server` has constructed `FastMCP`; wrap `_rate_limit_exceeded_handler` at `:299` in a local handler that emits `rate_limit_exceeded` (route, method, client_ip, limit) and returns the delegate's response unchanged.
- [ ] 1.6 `src/mcp_stdio.py`: call `configure_logging(level=WARNING)` in place of `basicConfig`; assert in a test that nothing is written to stdout.
- [ ] 1.7 `src/services/security_events.py`: `emit(event, *, level=WARNING, exc_info=None, **fields)`, `redacted_token_tag(value)` (the exact behaviour of `_redacted_prefix`), `client_ip(request)` (D3 — `request.client.host` only, never a header), and the suppressor of D7 (`MAX_EVENTS_PER_WINDOW = 10`, `SUPPRESSION_WINDOW_SECONDS = 60`, ≤ 512 keys, lock-guarded, cannot raise, INFO exempt, `events_suppressed` summary).
- [ ] 1.8 `src/mcp_server/auth.py`: `_redacted_prefix` delegates to `redacted_token_tag` (one definition); the ten `auth_failure` sites move `key_prefix` to `token_tag` and gain `client_ip` and `route`. No new refusal is added — the bare no-Bearer 401 at `:96` stays unlogged (design Non-Goals).
- [ ] 1.9 `src/services/vault.py` and `src/services/quotas.py`: replace `extra={"error": str(exc)}` with `error_type`; all other existing `extra` keys are already allow-listed and stay as they are.
- [ ] 1.10 Tests — `tests/test_issue_190_log_format.py`: one physical line per record including a traceback; UTC `Z` timestamp parses; `configure_logging()` idempotent; the ring handler survives in **both** call orders and still receives an ERROR; the `text` renderer emits the same fields. `tests/test_issue_190_field_allowlist.py`: an unknown key is dropped; each field is truncated to its bound; a coercion failure degrades to a truncated string rather than raising; `redacted_token_tag` is stable and contains no substring of its input; `client_ip` ignores a forged `X-Forwarded-For` from an untrusted peer; the source sweep over `src/` fails on any `extra=` key outside the allow-list; the suppressor bounds a flood, emits one summary, does not suppress a second subject, and cannot raise.
- [ ] 1.11 Test — `tests/integration/test_issue_190_log_config_process.py`: spawn a **subprocess** that imports `src.main`, logs a record with `extra`, and prints the root handler list; assert exactly one non-ring handler, that the captured stderr line parses as JSON, and that the extra field is present. This is the actual-process assertion the #190 audit note requires.
- [ ] 1.12 Docs: `docs/architecture/security-event-logging.md` — why the SDK wins the import race and what the reconfiguration does instead of `force=True`; the allow-list with its two rules (identifiers from resolved rows; presented material only as `token_tag`); the **whole event catalogue table** from `design.md`; the suppressor and what it deliberately does not suppress; why WARNING events do not reach the health page's buffer. Add the index row to `CLAUDE.md`.

## 2. Slice B — authentication events (#191). Starts after A is merged.

Owns: `src/auth/routes.py`, `src/oauth/routes.py`, `src/oauth/grants.py`; tests `tests/test_issue_191_panel_auth_events.py`, `tests/test_issue_191_oauth_events.py`.

- [ ] 2.1 `src/auth/routes.py`: split the merged failure condition at `:173-180` into `unknown_user` / `inactive_user` / `bad_password` **for the reason code only** — one shared `_render_login(..., status_code=401)` return, byte-identical across the three. Emit `panel_login_failed` and `panel_login_succeeded` (`:183`, before the session write).
- [ ] 2.2 `src/auth/routes.py`: `panel_logout` in `logout`, reading the session **before** `request.session.clear()` at `:205`; `panel_bootstrap_admin_created` at `:305` and `panel_bootstrap_refused` at each of `:234, 242, 250, 258, 267, 285` with its own reason code.
- [ ] 2.3 `src/oauth/routes.py` `/token`: `oauth_token_issued` at `:757`, `oauth_token_refreshed` at `:918`, and `oauth_token_refused` at every refusal listed in the catalogue, each carrying `<rfc_code>.<sub_reason>`; `oauth_token_rotation_failed` at `:914-916` with `exc_info` (the traceback is discarded today).
- [ ] 2.4 `src/oauth/routes.py` `/authorize`: `oauth_consent_granted` after the commit at `:583`, `oauth_consent_denied` at `:493` (resolve the session user for the record — a deny carries no identity today), `oauth_authorize_refused` at each refusal, and `oauth_cross_user_client_refused` inside `_cross_user_client_error` (`:127-139`) so both `:528` and `:570` inherit it.
- [ ] 2.5 `src/oauth/routes.py` `/register` and `/revoke`: `oauth_client_registered` (never the secret) and `oauth_client_registration_refused` with reason codes; `oauth_revoke_noop` at `:942, 948, 982, 983`, `oauth_revoke_refused` at `:985`.
- [ ] 2.6 `src/oauth/grants.py`: emit `oauth_grant_revoked` from `revoke_grant_family` with the rowcount it already computes and currently has discarded at the call site, so the panel's revoke and `/revoke` both record it. **Do not** add a second record in `src/control_panel/routes.py` — that file belongs to Slice D.
- [ ] 2.7 Tests: each outcome emits exactly one record with the expected reason; the three login failures return byte-identical responses; the revocation no-ops keep their 200-with-empty-body; a full-surface sweep asserts that no captured record contains the submitted password, client secret, authorization code, access or refresh token or PKCE verifier (nor any substring longer than eight characters of them).

## 3. Slice C — the tool surface (#192 write refusals, #193 exceptions). Starts after A is merged; parallel with B and D.

Owns: `src/mcp_server/tools.py`, `src/services/usage_stats.py`, `docs/architecture/usage-attribution.md`; tests `tests/test_issue_192_write_refusal_marker.py`, `tests/test_issue_193_tool_exception.py`, `tests/integration/test_issue_193_tool_exception_pg.py`.

- [ ] 3.1 `src/mcp_server/tools.py`: declare `_PERMISSION_DENIED_MARKER = "permission_denied"` and `_TOOL_EXCEPTION_MARKER = "tool_exception"` beside the existing marker block at `:287-372`.
- [ ] 3.2 `_require_write` (`:1213`): record the marker through `timing.record("error", _PERMISSION_DENIED_MARKER)` and emit `tool_write_refused` (tool, user id, actor) **at this one definition**, so all nine call sites inherit both. Resolve the tool name from the tracked context rather than a new parameter, so no caller signature changes. `request_download`'s `_mint_preflight(need_write=False)` path stays unaffected.
- [ ] 3.3 `_tracked` (`:665-737`): add `except Exception as exc` before the existing `finally`, per design D5 — measure the duration, emit `tool_exception` at ERROR with `exc_info`, then a best-effort usage row (the same `_truncate_params` + `timing.current()` merge as the success path, with `error` and `error_type` merged over the top, `response_size = 0`) inside its own `try/except` that logs `tool_usage_log_failed` and swallows, then a bare `raise`. **`Exception`, never `BaseException`.**
- [ ] 3.4 `src/services/usage_stats.py`: leave `PRE_BODY_REFUSAL_ERROR_MARKERS` unchanged and add a comment naming both new markers as deliberately classified post-body, with the reason, so the next reader does not re-derive it.
- [ ] 3.5 Tests (`tests/test_issue_192_write_refusal_marker.py`): a read-only credential on each of the nine gated tools produces a row carrying `permission_denied` and one warning record; the refusal string is unchanged; `request_download` produces neither; the marker does not match `pre_body_refusal_sql`.
- [ ] 3.6 Tests (`tests/test_issue_193_tool_exception.py`): a raising body emits one ERROR with the class name and a traceback and writes exactly one row with `error = tool_exception`, `error_type` and a non-negative duration; the same exception instance propagates; a failing `_log_usage` leaves the original exception intact and logs `tool_usage_log_failed`; `CancelledError` writes no row and emits no record; a body that recorded a post-body marker and then raised is written as `tool_exception`; the phase-timing holder is still cleared in `finally`.
- [ ] 3.7 Test (`tests/integration/test_issue_193_tool_exception_pg.py`): against PostgreSQL, a raising tool's row is present and readable, is counted as executed by `executed_sql`, and does not break `/admin/performance`'s aggregates for that window.
- [ ] 3.8 Docs: `docs/architecture/usage-attribution.md` — extend the marker register with `permission_denied` and `tool_exception` on the post-body side (with the reasoning that puts them there), and declare `error_type` a reserved `params` key of type string that no reader casts.

## 4. Slice D — HTTP refusal surfaces and the usage page (#192). Starts after A is merged; parallel with B and C.

Owns: `src/csrf.py`, `src/control_panel/routes.py`, `src/control_panel/users.py`, `src/transfer/routes.py`, `src/services/usage_filters.py`, `src/control_panel/templates/usage.html`, `docs/architecture/control-panel.md`, `docs/architecture/file-transfer.md`; tests `tests/test_issue_192_transfer_refusal_log.py`, `tests/test_issue_192_panel_forbidden.py`, `tests/integration/test_issue_192_usage_outcome_pg.py`.

- [ ] 4.1 `src/transfer/routes.py`: emit `transfer_refused` at every `_not_found()` site (`:261, 265, 280, 284, 374, 382, 391, 412, 527, 668, 673, 680, 691, 697`) with its own reason code, `redacted_token_tag(token)`, route, method and `client_ip`; log the minting identity **only** where a row resolved. `_not_found()` itself and `NOT_FOUND_BODY` are untouched. Existing mount-boundary and unsupported-filesystem log lines and their 503s are untouched.
- [ ] 4.2 `src/control_panel/routes.py`: `require_admin_panel` (`:225`) gains `request: Request`; emit `panel_forbidden` there (`:234`) and in `_assert_key_owner` (`:713`), `_assert_oauth_client_owner` (`:1055`) and `_assert_oauth_token_owner` (`:1067`), each with its reason code — the three helpers take an explicit `request` argument from their call sites.
- [ ] 4.3 `src/control_panel/users.py`: emit `panel_forbidden` with `reason=actor_revoked` at the four `_actor_still_privileged` refusals (`:241, 320, 449, 601`) and `panel_password_reset` after the write at `:609-611` (actor and target ids, never the new password).
- [ ] 4.4 `src/csrf.py`: emit `csrf_refused` at `:57` with route, method, `client_ip` and the session user id when one is present; the 403 and its detail are unchanged.
- [ ] 4.5 `src/services/usage_filters.py`: `recent_logs` selects `ul.params->>'error'` and `ul.params->>'over_quota' = 'true'` — **text comparison, no `::boolean` cast** (design D9) — and exposes them on the row.
- [ ] 4.6 `src/control_panel/templates/usage.html`: render an outcome badge per row — refusal with the reason for an error marker or over-quota, failure with `error_type` for `tool_exception`, nothing for an ordinary call — using existing theme tokens only, with an unrecognised marker rendered as its own value.
- [ ] 4.7 Tests: every refusal cause on every bearer-protected transfer endpoint yields a byte-identical 404 and a distinct logged reason; the tag is stable and discloses nothing; a branch with no resolved row logs no identity; each panel 403 and the CSRF 403 emit their record with route and user id; the usage page renders the badge for a seeded `permission_denied` row and survives a row carrying a nonsense over-quota value.
- [ ] 4.8 Docs: `docs/architecture/file-transfer.md` — the refusal log beside the uniform-404 section, stating that the log is the only place the reason exists and the response is still the sole external answer. `docs/architecture/control-panel.md` — the ring buffer's survival rule (cross-reference the new note), the usage page's outcome column and why it is read as text.

## 5. Integration and verification

- [ ] 5.1 Merge A, then B, C and D; run the full suite once on the merged result (not on the per-slice worktrees), plus `make audit` and `make db-check` (nothing here touches the schema — `alembic check` must still say "No new upgrade operations detected").
- [ ] 5.2 `npx -y @fission-ai/openspec@1.3.1 validate security-event-logging --strict` clean.
- [ ] 5.3 `openspec-verifier` subagent against the merged tree; iterate to zero blocking gaps.
- [ ] 5.4 Adversarial Codex pass (mandatory — this change touches the write-refusal path, the transfer refusal surface and `_tracked`). Frame it as a defensive control review: does any record carry credential material, does any response change, can the suppressor drop something that matters, can the exception path mask or alter an exception, can a log call raise into a request path. Fix BLOCKER/MAJOR; re-run naming what was addressed.
- [ ] 5.5 `make deploy`; confirm `make db-check` clean and `make logs` now shows one JSON object per line with a UTC `Z` timestamp and populated fields on an `auth_failure`.
- [ ] 5.6 Live MCP exercise (no browser gate in this project — name the tools actually called). With a **read-only** credential: `create_note`, `edit_note`, `write_file`, `delete_file`, `request_upload` → each refused, each producing one `tool_write_refused` record and a `permission_denied` row; `keyword_search`, `read_note`, `list_notes` → unaffected. With a **readwrite** credential: `create_note`, `edit_note`, `set_frontmatter`, `delete_note` on a scratch note; `request_upload` then redeem with a corrupted token against `/transfer/upload/info` → uniform 404 plus one `transfer_refused` record carrying a `sha:` tag; `check_upload` for the real handle. Confirm on `/admin/usage` that the refused writes render as refusals and the successful ones do not.
- [ ] 5.7 Tool-exception path: identify a tool argument that raises a genuine unhandled exception in a **local** container first (candidates: `read_file` on a directory path, `read_note` for an index row whose file was removed), reproduce it once against the deployed server, and confirm the ERROR record in `make logs`, the `tool_exception` row on `/admin/usage`, and the entry on `/admin/health`. Then remove the scratch notes and any test capability.
- [ ] 5.8 `/openspec-archive-change`; PR closing `#190`, `#191`, `#192`, `#193`; update the ASVS report's issue rows in the vault and record the two accepted residuals (uvicorn's own loggers stay in their format; `permission_denied` is post-body and dilutes a write tool's percentiles).
