## 1. Result model

- [x] 1.1 Define `ReadNoteResult` (pydantic) in `src/mcp_server/tools.py` (or a sibling module): `path`, `title`, `tags`, `frontmatter_yaml` (raw block text, authoritative), `frontmatter` (best-effort JSON view), `heading`, `content`, `truncated`, `offset`, `next_offset`, `total_chars`, `outline` (object: `entries` [{ordinal, depth, text, size, exceeds_cap, duplicate}], omitted count, full ordinal range, truncation marker), `metadata_omissions`, `notice`, `error`; `path` always exact (fixed allocation from admission-time path validation). All values made JSON-safe **at model-build time** (never in a serializer — the SDK renders text from the raw object and structuredContent from the validated dump, and they must not diverge); custom serialization omits absent fields from both renderings (no `null`s).
- [x] 1.2 Frontmatter JSON view builder: depth/size-bounded, cycle-safe walk; dates → ISO strings; omit the whole view (stating the omission) on construction failure or JSON-key collision; `frontmatter_yaml` always present under the metadata budget. Tests: `x: &X [*X]` (no raise, view omitted), `1:` vs `"1":` collision (view omitted), dates (view lossy, raw authoritative), nested maps.
- [x] 1.3 Budgets: metadata fields share a metadata budget ≤ cap with deterministic drop priority (JSON view → frontmatter_yaml → tags → heading elision → title elision), every drop/elision recorded in `metadata_omissions`, never an in-band marker, dropped fields omitted whole (frontmatter_yaml lossless-whenever-present); admission-time path-length limit (1,024 chars) added in the vault layer so `path` and path-bearing errors are a fixed allocation; error/notice interpolations bounded; restate the worst-case response (sum of budgets × 2 duplication × ≤6 JSON escaping) in `docs/architecture/vault-tools.md`. Tests: multi-megabyte frontmatter beside a one-char body stays bounded; 40k control characters in content stay within the stated worst case.
- [x] 1.4 Rework `read_note_impl` for every path: whole-note, section (heading field + body-only `content` via the shared helpers), windowed reads, empty content at offset 0 (success; end-reached error only for offset > 0), offset-at/past-end, invalid offset/limit, missing note, unknown selector (error lists available headings), error precedence path → params → selector; caps and outline budgets preserved with the outline's degraded states as data.

## 2. Registration and tracking

- [x] 2.1 `read_note` registration in `src/mcp_server/server.py` returns `ReadNoteResult` (SDK 1.29 emits outputSchema + structuredContent + JSON text); rewrite BOTH `read_note` docstrings (server.py registration AND tools.py `read_note_impl` — field round trip, `frontmatter_yaml` authoritative, CRLF residual, no textual extraction) and BOTH `edit_note` docstrings per the vault-write delta's docstring requirement.
- [x] 2.2 `_tracked`: admission refusals (vault-root gate) return a typed `ReadNoteResult(error=…)` for this tool instead of a bare string (which fails output validation and becomes a protocol error); `_log_usage` records `response_size` as the serialized JSON text length. Test the admission path at MCP level.

## 3. Tests

- [x] 3.1 Field-based tests: both issue #149 forgeries (multiline YAML title, quoted frontmatter key) yield uncorrupted fields and byte-exact section bodies; section `content` round-trips through `edit_note(section=…)`; complete whole-note `content` round-trips through full replace; truncation fields; outline bounding/elision/omission/degradation as data; duplicate flags; structured/text parity (text block == JSON serialization of structuredContent) at MCP level; absent-not-null.
- [x] 3.2 Migrate the known envelope consumers: `tests/test_read_response_cap.py`, `tests/test_issue_128_edit_note_frontmatter.py` (drops its `\n---\n` split), `tests/test_issue_140_section_round_trip.py`, `tests/test_issue_89_tool_names_in_copy.py` (guidance now in `notice`), plus the direct string consumers `tests/test_issue_128_section_mode_frontmatter.py`, `tests/test_issue_77_usage_attribution.py`, `tests/test_issue_88_root_confirmed_before_publish.py`, `tests/test_symlink_mutation_guard.py`; before starting, regenerate the inventory with `rg -l "read_note_impl|read_note\(" tests/` and cover every hit; keep pinned behaviors, restated against fields.

## 4. Docs

- [x] 4.1 `docs/architecture/vault-tools.md`: field-based round trip, forgery history, per-field budget mapping, new worst case, frontmatter raw-vs-view contract.

## 5. Gates

- [x] 5.1 Full suite green; `openspec validate read-note-framing-149 --strict` clean.
- [ ] 5.2 Live-client gate (pre-archive, post-deploy; this project's substitute for the user-representative pass): against the live server with a real MCP client, exercise structured success, JSON text fallback, a missing-note and unknown-section error (confirm the agent-visible result carries `error` in-band with isError=false and is not mistaken for content), a truncated window (confirm the client does not write it back), and the #149 forgery fixtures end-to-end (read → edit_note round trip, byte-exact, then fixtures deleted). Record observed client behavior in the change notes; name the tools called.
