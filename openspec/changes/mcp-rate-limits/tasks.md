## 1. Slice A — enforcement path (owns `src/services/refusals.py` **new**, `src/services/rate_limits.py` **new**, `src/config.py`, `src/auth/session.py`, `src/mcp_server/auth.py`, `src/mcp_server/tools.py`, `Dockerfile`; new tests `tests/test_mcp_rate_limits.py`, `tests/test_mcp_admission_gates.py`, `tests/test_mcp_argument_caps.py`, `tests/test_limiter_settings_env.py`, `tests/integration/test_mcp_pool_availability_pg.py`)

Everything on the request path lives in one slice: the admission gates, the principal binding, the buckets, the slots, the refusal shape and the query cap all touch `_tracked` or `APIKeyMiddleware`. **Slices B, C, D and E all start after A merges.**

- [ ] 1.1 `src/config.py`: `MAX_SEARCH_QUERY_CHARS = 8192` as a module constant beside `MAX_LIST_PATTERN_CHARS`, same comment style, noting that a character cap does not guarantee the provider's token limit (design D15).
- [ ] 1.2 `src/config.py`: settings — `mcp_max_inflight_requests: int = 32`, `mcp_max_inflight_per_credential: int = 8`, `mcp_inflight_table_size: int = 4096`, `mcp_max_concurrent_authentications: int = 3`, `mcp_reserved_connections: int = 3`, `mcp_auth_failure_limit: int | None = 60`, `mcp_auth_failure_window_seconds: int = 300`, `mcp_auth_failure_table_size: int = 4096`, `mcp_rate_limit_per_minute: int | None = 120`, `mcp_rate_limit_burst: int | None = 30`, `mcp_write_rate_limit_per_minute: int | None = 60`, `mcp_write_rate_limit_burst: int | None = 15`, `mcp_max_concurrent_tool_calls: int = 9`, `mcp_max_concurrent_embedding_calls: int = 2`, `mcp_max_concurrent_vector_queries: int = 2`, `mcp_max_concurrent_writes: int = 2`, `mcp_max_concurrent_other_calls: int = 3`, `mcp_max_concurrent_per_principal: int = 3`, per-principal-class and per-tenant-class caps for all four classes (embedding 1/1, vector 1/1, write 1/1, other 2/2), `mcp_slot_wait_seconds: float = 5.0`, `mcp_limiter_max_tracked_principals: int = 10_000`, `mcp_refusal_log_interval_seconds: int = 10`, `default_daily_request_limit: int | None = 5000`. All `ge=1`/`gt=0` so zero is rejected.
- [ ] 1.3 `src/config.py`: **one** shared `BeforeValidator` mapping an empty value, `null` or `none` (stripped, case-insensitive) to `None`, applied to every nullable limiter setting — no per-field variants (D14).
- [ ] 1.4 `src/config.py`: `model_validator` enforcing D14 in full — global ≥ **sum of all four** class ceilings; `mcp_max_concurrent_authentications + mcp_max_concurrent_tool_calls ≤ pool_size + max_overflow − mcp_reserved_connections` (the pool arithmetic, read from the engine's configured values); per-principal ≤ global; for each class, per-principal-class ≤ per-tenant-class **<** class ceiling; per-credential in-flight **<** identity-blind ceiling; in-flight ceiling ≥ global; each bucket's rate and burst both set or both null; `default_daily_request_limit` within 1..1,000,000. Errors name the settings and, for the two arithmetic rules, the sums.
- [ ] 1.5 `src/services/refusals.py` (new, importing nothing from the app so `tools.py`, `quotas.py`, `embeddings.py` and `rate_limits.py` can all use it without a cycle): the `Refusal` dataclass (`code`, `scope`, `limit`, `limit_unit`, `retry_after_seconds`), the closed `code` set, and `render(prose, refusal) -> str` appending the single sentinel line. `retry_after_seconds` omitted for `no_vault_assigned` / `argument_not_encodable`; ≥ 1 elsewhere; documented as a hint, not a guarantee, for `slot_timeout`.
- [ ] 1.6 `src/auth/session.py`: `current_principal: ContextVar[tuple | None]` beside `current_actor` / `current_vault_root`, docstring recording that the OAuth key is `grant_id` and why (D3, issue #64).
- [ ] 1.7 `src/mcp_server/auth.py`: the three pre-SQL admission gates in order — per-credential in-flight (fixed-size salted table indexed by the SHA-256 the middleware already computes; never logged, never persisted), identity-blind in-flight counter, then an `asyncio.Semaphore` of `mcp_max_concurrent_authentications` acquired with a bounded wait and held **only** across the existing `async with async_session()` block. All three refuse with 503 + `Retry-After`, before any session and any query, writing no usage row; all three release in `finally`. A comment records the pool arithmetic (D1) beside the semaphore.
- [ ] 1.8 `src/mcp_server/auth.py`: the failed-auth budget — fixed-size table of `mcp_auth_failure_table_size` counters indexed by a **per-process random salt** + hash of the proxied client address; checked before the credential lookup; refuse when the recorded count is already ≥ the limit; increment on **every** 401 branch (missing bearer, unknown credential, ownerless, inactive user, expired, cross-user grant, no vault scope); address-less requests charged to one shared reserved slot; `Retry-After` = whole seconds remaining in the window, minimum 1; one WARNING per slot per window; null disables. **One helper** for the threshold and the `Retry-After` arithmetic so the two cannot drift.
- [ ] 1.9 `src/mcp_server/auth.py`: bind `current_principal` in both branches from the row already loaded — `("api_key", api_key.id)` and `("oauth", oauth_token.grant_id)` — and `reset()` it in the existing `finally`. No new query.
- [ ] 1.10 `src/services/rate_limits.py` (new): `TokenBucket` (synchronous `take()` → `(admitted, retry_after_seconds)`, monotonic clock, no lock, no `await` between read and write); general and write bucket registries keyed on principal; the bounded registry (hard cap, amortised TTL sweep on insert, **full-and-idle-only** eviction skipping depleted entries and entries with in-flight holders or waiters, shared overflow bucket past the cap); the fixed-size salted counter tables for fingerprints and addresses; the four class semaphores plus per-principal, per-principal-class and per-tenant-class counters; `acquire_slots(principal, user_id, tool_class, deadline)` as an async context manager acquiring in D6's fixed order under **one** shared deadline and releasing in reverse in `finally`; the refusal builders returning `Refusal`s; `reset_state_for_tests()`.
- [ ] 1.11 `src/services/rate_limits.py`: the refusal coalescer — state keyed on `(principal, tool, marker, scope)` holding `(window_start, pending)`; first refusal writes with `suppressed = 0`, refusals inside an open window increment `pending` and issue **no statement of any kind**, a refusal after the window closed writes `suppressed = pending` and resets; `flush_expired()` writing pending rows, called from `_log_usage` on any path, from the indexer tick, and from lifespan shutdown.
- [ ] 1.12 `src/mcp_server/tools.py`: `_tracked` gains `slot: str` (defaulting to the `other` class, so a new tool is classed by construction) and `arg_char_caps: dict[str, int] | None`. Gate order — general bucket → write bucket (write class only) → vault → unencodable-argument → `arg_char_caps` → **slots** → quota → body — with slots held across the quota gate and released in the `finally` that also covers the body (D5). Markers `rate_limited`, `slot_timeout`, `argument_too_long` (pre-body) and `provider_input_rejected` (**post-body**), plus the `rate_limit_scope` (string) and `suppressed` (integer) params keys, each with the "which side of the body/no-body line" comment the register requires.
- [ ] 1.13 `src/mcp_server/tools.py`: route every pre-body refusal through `refusals.render` — the three existing ones **additively** (prose unchanged, sentinel line appended) and the three new ones. Update the existing pre-body-refusal assertions that compare whole strings.
- [ ] 1.14 `src/mcp_server/tools.py`: annotate the tools — `slot="embedding"` on `semantic_search_impl`; `slot="vector"` on `find_related_impl`; `slot="write"` on the eight mutating impls (the same eight also gate on the write bucket); everything else inherits `other`; `arg_char_caps={"query": MAX_SEARCH_QUERY_CHARS}` on `search_notes_impl` and `semantic_search_impl`.
- [ ] 1.15 `Dockerfile`: comment above the `CMD` binding `--workers 1` to these in-process controls (D13).
- [ ] 1.16 Tests `tests/test_mcp_admission_gates.py`: per-credential cap refuses one credential's excess with 503 while a **different** credential is admitted and served; identity-blind ceiling refuses with 503 + `Retry-After` **before any `async_session()`** (patch/count the session factory); the authentication semaphore bounds concurrent auth sessions to its setting and is released before the body starts; all three released on completion and on a raising handler; failed-auth budget parameterised over **every** 401 branch, threshold inclusivity at limit and limit+1, `Retry-After` arithmetic, the address-less shared slot, a refused probe opening no session, window rollover, null disables, the address taken from the proxied client rather than a directly-read header; **100,000 distinct addresses and 100,000 distinct token fingerprints** leave memory proportional to the table sizes and colliding keys share a budget rather than escape one.
- [ ] 1.17 Tests `tests/test_mcp_rate_limits.py`: general bucket sustained/burst; write bucket refuses writes while reads continue for the same principal; cross-principal isolation; admitted path issues **zero** SQL statements (statement-counting, as `tests/test_issue_162_quota_gate.py` does); OAuth principal is the `grant_id` — same across a refresh, **different across two grants of one client and user**; no-principal callers exempt; slot order and the single shared deadline (elapsed ≈ budget, with tolerance); saturated class + exhausted deadline → `slot_timeout`, body not run, nothing queued; **an `other`-class saturation does not block a call already holding an embedding slot** (the D6 invariant); **quota refusal releases slots**; **slot timeout leaves `quota_counters` untouched**; raising body releases every slot; **per-tenant caps for `embedding`, `vector` and `write` alike** — tenant A with an API key *and* an OAuth grant cannot hold a whole class while tenant B waits; refusal shape — `keyword_search` string contains the parseable sentinel line, `read_note` carries the identical text in its structured error field, futile refusals omit the retry field, existing prose preserved; coalescing — refusals inside a window issue **no INSERT and no UPDATE**, scopes are not merged, `1 + suppressed` sums exactly after a flush, and a flush happens via `_log_usage`, via the tick and at shutdown; registry bounds — overflow shares one bucket without affecting an in-cap principal, a depleted or busy entry is never evicted; boot validator rejects each incoherent set, both arithmetic rules, and every zero.
- [ ] 1.18 Tests `tests/test_mcp_argument_caps.py`: 8,192 accepted, 8,193 refused naming `MAX_SEARCH_QUERY_CHARS` with **no embedding call** (patch `get_embedding`), no search statement and no quota statement; the query is not echoed; the usage row **is** written carrying `argument_too_long`; a structured tool refuses in its own shape; **a dense non-ASCII query under the character cap that the provider rejects** yields the same caller-facing `argument_too_long` code with the provider's reason while logging the distinct **post-body** `provider_input_rejected` marker.
- [ ] 1.19 Tests `tests/test_limiter_settings_env.py`: write an actual env file and load `Settings` — empty, `null` and `None` resolve to None for every nullable limiter setting; zero rejected; the class-sum rule, the pool rule and the per-scope rules fail the load with messages naming the settings.
- [ ] 1.20 Tests `tests/integration/test_mcp_pool_availability_pg.py`: under a flood of concurrent **valid-credential** `/mcp` requests far exceeding the ceilings — including a flood from a *single* credential and one from many — a second tenant's request obtains a connection and is served, connections held for authentication never exceed `mcp_max_concurrent_authentications`, and `engine.pool` never reaches exhaustion.

## 2. Slice B — observability (owns `src/services/usage_stats.py`, `docs/architecture/usage-attribution.md`, `tests/test_issue_160_refusal_predicate.py`, `tests/integration/test_issue_160_performance_pg.py`). **After Slice A merges.**

- [ ] 2.1 `src/services/usage_stats.py`: add `rate_limited`, `slot_timeout` and `argument_too_long` to the enumerated pre-body refusal predicate, mirrored from `tools.py` in the existing direction (never an import back into `tools.py`). `provider_input_rejected` is **deliberately not added** — a comment says why.
- [ ] 2.2 `src/services/usage_stats.py`: the per-tool refusal count sums `1 + suppressed` rather than counting rows, with a **guarded** integer cast (`~ '^[0-9]+$'`, the `result_count` precedent).
- [ ] 2.3 `tests/test_issue_160_refusal_predicate.py`: pin the three new mirrored pairs byte-equal; assert the predicate still admits every post-body marker, `provider_input_rejected` included.
- [ ] 2.4 `tests/integration/test_issue_160_performance_pg.py`: a window holding executed rows plus coalesced refusal rows — percentiles from executed rows only, refusal count equal to summed `1 + suppressed`, a malformed `suppressed` rendering without raising, a `provider_input_rejected` row counted as executed, and an unrelated `params.error` row still executed.
- [ ] 2.5 `docs/architecture/usage-attribution.md`: extend the marker register with the three new pre-body values **and** `provider_input_rejected` on the post-body side, with the classification reasoning for each; record that `rate_limit_scope` is a string no reader casts and `suppressed` an integer read with a guarded cast; state that the refusal count is `1 + suppressed`, that it is exact only after a flush, and that an abrupt kill loses at most one interval per key.

## 3. Slice C — default quota surface (owns `src/services/quotas.py`, `src/api/routes.py`, `src/control_panel/routes.py`, `src/control_panel/templates/keys.html`; new tests `tests/test_default_daily_limit.py`, additions to `tests/test_issue_162_quota_gate.py`). **After Slice A merges.** Parallel with B, D and E.

- [ ] 3.1 `src/services/quotas.py`: `Admission` carries the **decision instant** from `admit()`'s single clock read; `retry_after_seconds = ceil(reset_at − decided_at)`, minimum 1, computed once and never re-read (design D20). `quota_refusal_message` renders its unchanged prose plus the sentinel line via `refusals.render`.
- [ ] 3.2 Tests in `tests/test_issue_162_quota_gate.py`: patch the clock so the decision lands milliseconds before UTC midnight and the message is built after it — the interval is the small one, not ~48 hours; assert no clock read occurs between the statement and the rendered refusal.
- [ ] 3.3 `src/api/routes.py`: key creation applies the default when `"daily_request_limit" not in req.model_fields_set`; explicit `null` still means unlimited; an explicit value wins. Update the request-model docstring.
- [ ] 3.4 `src/control_panel/routes.py`: `keys_page` passes the configured default to the template. The create handler performs **no** default substitution — a blank field is an explicit unlimited. The edit handler is untouched.
- [ ] 3.5 `src/control_panel/templates/keys.html`: pre-fill the create-form limit input with the passed default (empty when none); extend the help text — a default, changeable, clearable, empty means unlimited. Edit modal copy unchanged.
- [ ] 3.6 Tests `tests/test_default_daily_limit.py`: omitted vs explicit-null vs explicit-value on the JSON API; panel create with the pre-filled value, and cleared → unlimited **with no POST-side substitution**; **existing keys untouched**; `DEFAULT_DAILY_REQUEST_LIMIT=null` leaves the field empty and creates unlimited keys; a default outside 1..1,000,000 refuses the boot; the edit path never substitutes.

## 4. Slice D — provider input translation (owns `src/services/embeddings.py`; new tests `tests/test_provider_input_limit.py`). **After Slice A merges.** Parallel with B, C and E. Consumes `src/services/refusals.py` and the `provider_input_rejected` marker A defines.

- [ ] 4.1 `src/services/embeddings.py`: both providers detect their own input-limit rejection (OpenAI's documented input-too-large error; Ollama's equivalent where it reports one rather than truncating) and raise a distinct typed exception carrying the provider's stated reason, rather than a generic provider error.
- [ ] 4.2 `semantic_search`'s caller in `tools.py` is Slice A's; Slice D provides the exception and the reason string only, so the two do not share a file. Coordinate through the exception type A imports.
- [ ] 4.3 Tests `tests/test_provider_input_limit.py`: a stubbed provider returning each vendor's input-limit error shape yields the typed exception with the reason preserved; a generic provider error still propagates as today; a dense non-ASCII input under the character cap reaches the provider (the character cap does not pre-empt it).

## 5. Slice E — documentation and the recorded proxy decision (owns `docs/architecture/rate-limits-and-concurrency.md` **new**, `CLAUDE.md`, `docs/architecture/search.md`, `docker-compose.yml`, `README.md`, `.env.example`). **After Slice A merges.** Parallel with B, C and D.

- [ ] 5.1 New `docs/architecture/rate-limits-and-concurrency.md` carrying the load-bearing rationale verbatim from design: the three pre-SQL gates and **the pool arithmetic** (D1); coalescing with a flush lifecycle, and what a hard kill loses (D2); the principal is the grant (D3); the two gate-order rules (D5); **every tool has a class**, the reservation invariant, and why global-first was rejected (D6); per-tenant caps on all three expensive classes and why a reservation was not offered (D7); **velocity is not totality, and who has velocity bounds only** (D8); the one refusal shape (D9); no I/O, no lock, no await (D10); one deadline, and **the per-provider worst case — Ollama 30 s, OpenAI up to 60 s × 3 attempts ≈ 180 s** — with the explicit note that the wait budget is deliberately not sized to it and that `retry_after_seconds` is a hint (D11); bounded registries, full-and-idle eviction, the shared overflow bucket as an accepted limitation, salted fixed tables (D12); in-process state and **`--workers 1` as part of the contract** (D13); one representation for "off" (D14); the two halves of the query cap and why their markers differ (D15); Traefik and the shared-egress residual (D16–D18); the application-level new-key default (D19); the single clock read (D20); four markers (D21).
- [ ] 5.2 `CLAUDE.md`: add the note to the architecture index with its "read before you touch" line; add the rate-limit facts to Key decisions (in-process, single worker, in-band parseable refusals, slots before quota, velocity-only residual for OAuth and grandfathered keys).
- [ ] 5.3 `docs/architecture/search.md`: record `MAX_SEARCH_QUERY_CHARS` — where it is enforced, that it precedes the embedding call and the `tsquery` parse, that provider cost is *not* the reason, and that the provider's own token limit is handled separately in the body.
- [ ] 5.4 `docker-compose.yml`: a comment on the `obsidian-mcp-api-rtr` labels recording that a Traefik `ratelimit` middleware was considered and deliberately not added, with the reason — no labels change.
- [ ] 5.5 `.env.example` and `README.md`: every new setting with its default and one line on what it bounds, the documented "off" representation, and a README operations note showing what an agent sees when rate limited (the sentinel line).

## 6. Verification

- [ ] 6.1 Merge A, then B, C, D and E; run the **full** suite once on the merged result (`pytest`), plus `make audit`. This is the authoritative gate, not the per-worktree runs.
- [ ] 6.2 Grep for production callers of every new export in `src/services/rate_limits.py` and `src/services/refusals.py`, and for the `provider_input_rejected` branch — fan-out ships green-but-unwired code, and a limiter nothing calls is exactly what this step catches.
- [ ] 6.3 `npx -y @fission-ai/openspec@1.3.1 validate --all --strict` clean.
- [ ] 6.4 `openspec-verifier` subagent against the merged tree; iterate to zero blocking gaps.
- [ ] 6.5 Adversarial Codex pass on the **implementation**. Frame it for this product: the expensive failures are a limiter that refuses a legitimate agent on the common path, one that returns something an agent cannot parse or mistakes for an empty result, a slot leak or deadlock that wedges the single worker, a durable quota slot consumed by a call that never ran, pool exhaustion surviving the three admission gates, and any bypass by refreshing a token or minting a second grant. Re-run after each round naming what was addressed; if successive rounds keep producing *new classes* of finding, ship the concurrency layer disabled by configuration rather than tune thresholds for another round.
- [ ] 6.6 `make deploy`; `make db-check` clean; `/health` green; watch `make logs` for the auth-failure WARNING and for boot-time settings validation.
- [ ] 6.7 Live MCP exercise against the deployed server, naming the tools called (there is no browser UI, so this substitutes for the `user-representative` pass): `keyword_search` with an 8,193-character query → refused naming `MAX_SEARCH_QUERY_CHARS`; `semantic_search` at 8,192 → normal result; a dense non-ASCII query under the cap → `argument_too_long` with the provider's reason; a burst of `read_note` on one key past its burst → `rate_limited` refusals whose sentinel line parses to a numeric retry-after, while a **second** credential's `list_notes` succeeds throughout; a burst of `create_note`/`delete_note` above the write bucket while that key's `read_note` still succeeds; concurrent `semantic_search` from one tenant's API key **and** OAuth grant plus a second tenant's → the second tenant is served and one call returns `slot_timeout`; the same for `find_related` and a write tool; an unauthenticated burst → 429 with `Retry-After`; a single-credential valid flood and a many-credential valid flood → 503s while `/health` and another tenant's `get_recent` stay served. Confirm on `/admin/performance` that refusals appear as refusal counts (summing `1 + suppressed`) and not in the percentiles, and that the provider-rejection row is inside the percentiles. Delete the test notes.
- [ ] 6.8 Operator follow-up (not a code task): set a daily limit on the five existing production keys — materially more important after round 2, since D8's accepted limitation means they have velocity bounds only until one is set.
- [ ] 6.9 `/openspec-archive-change`; PR with `Closes #194`, `Closes #188`; record on #194 the accepted residuals — **expiring unused dynamically registered OAuth clients is not closed here**; **OAuth grants and grandfathered NULL-limit keys have velocity bounds only**; a hard kill loses at most one coalescing interval of refusal counts; the shared overflow bucket past 10,000 principals. Note #202 (indexer fairness) as the sibling still open.
