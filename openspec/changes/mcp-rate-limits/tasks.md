## 1. Slice A — enforcement path (owns `src/services/refusals.py` **new**, `src/services/rate_limits.py` **new**, `src/config.py`, `src/auth/session.py`, `src/mcp_server/auth.py`, `src/mcp_server/tools.py`, `src/services/indexer.py`, `src/main.py`, `Dockerfile`; new tests `tests/test_mcp_rate_limits.py`, `tests/test_mcp_auth_failure_budget.py`, `tests/test_mcp_argument_caps.py`, `tests/test_limiter_settings_env.py`)

Everything on the request path lives in one slice: the failed-auth budget, the principal binding, the buckets, the refusal shape, the coalescer and its flush hooks, and the query cap all touch `_tracked`, `APIKeyMiddleware`, or the two places a deferred flush is driven from. **Slices B, C, D and E all start after A merges.**

- [x] 1.1 `src/config.py`: `MAX_SEARCH_QUERY_CHARS = 8192` as a module constant beside `MAX_LIST_PATTERN_CHARS`, same comment style, noting that a character cap does not guarantee the provider's token limit (design D6).
- [x] 1.2 `src/config.py`: settings — `mcp_auth_failure_limit: int | None = 60`, `mcp_auth_failure_window_seconds: int = 300`, `mcp_auth_failure_table_size: int = 4096`, `mcp_rate_limit_per_minute: int | None = 120`, `mcp_rate_limit_burst: int | None = 30`, `mcp_write_rate_limit_per_minute: int | None = 60`, `mcp_write_rate_limit_burst: int | None = 15`, `mcp_limiter_max_tracked_principals: int = 10_000`, `mcp_refusal_log_interval_seconds: int = 10`, `default_daily_request_limit: int | None = 5000`. All `ge=1` so zero is rejected. **No pool-related settings and no import of connection-pool constants** — `src/database.py` is untouched.
- [x] 1.3 `src/config.py`: **one** shared `BeforeValidator` mapping an empty value, `null` or `none` (stripped, case-insensitive) to `None`, applied to every nullable limiter setting — no per-field variants (D15).
- [x] 1.4 `src/config.py`: `model_validator` enforcing D15 — each bucket's rate and burst both set or both null; `default_daily_request_limit` within `DAILY_REQUEST_LIMIT_MIN`..`MAX`. Errors name the settings.
- [x] 1.5 `src/services/refusals.py` (new, importing nothing from the app so `tools.py`, `quotas.py`, `embeddings.py` and `rate_limits.py` can all use it without a cycle): the `Refusal` dataclass (`code`, `scope`, `limit`, `limit_unit`, `retry_after_seconds`), the closed `code` set, `render(prose, refusal) -> str` appending the single sentinel line, **and the provider input-limit exception type** — declared here, not in `embeddings.py`, so Slice A can handle and test it without Slice D (D6). `retry_after_seconds` omitted for `no_vault_assigned` / `argument_not_encodable`; ≥ 1 elsewhere.
- [x] 1.6 `src/auth/session.py`: `current_principal: ContextVar[tuple | None]` beside `current_actor` / `current_vault_root`, docstring recording that the OAuth key is `grant_id` and why (D1, issue #64).
- [x] 1.7 `src/mcp_server/auth.py`: the failed-auth budget — fixed-size table of `mcp_auth_failure_table_size` counters indexed by a **per-process random salt** + hash of the proxied client address; checked before the credential lookup; refuse when the recorded count is already ≥ the limit; increment on **every** 401 branch (missing bearer, unknown credential, ownerless, inactive user, expired, cross-user grant, no vault scope); address-less requests charged to one shared reserved slot; `Retry-After` = whole seconds remaining in the window, minimum 1; one WARNING per slot per window; null disables. **One helper** for the threshold and the `Retry-After` arithmetic so the two cannot drift (D12).
- [x] 1.8 `src/mcp_server/auth.py`: bind `current_principal` in both branches from the row already loaded — `("api_key", api_key.id)` and `("oauth", oauth_token.grant_id)` — and `reset()` it in the existing `finally`. No new query.
- [x] 1.9 `src/services/rate_limits.py` (new): `TokenBucket` (synchronous `take()` → `(admitted, retry_after_seconds)`, monotonic clock, no lock, no `await` between read and write); the general and write bucket registries keyed on principal; the bounded registry (hard cap, amortised TTL sweep on insert, **full-and-idle-only** eviction skipping depleted buckets and entries with an unflushed pending count, shared overflow bucket past the cap); the salted fixed-size counter table for addresses; `reset_state_for_tests()`.
- [x] 1.10 `src/services/rate_limits.py`: the refusal coalescer — state keyed on `(principal, tool, marker, scope)` holding `(window_start, pending, row_template)`, where **`row_template` is the complete immutable attribution** captured when the window opened: owner `user_id`, `key_id` / `oauth_token_id`, the denormalised actor triple, tool, marker, scope and bounded params. `pending` counts refusals **no row yet represents**. First refusal writes its own row with `suppressed = 0` and sets `pending = 0`; refusals inside an open window increment `pending` and issue **no statement of any kind**; a refusal after the window closed writes `suppressed = pending` with itself as the row's base, then resets. `flush_expired()` writes **from the template only**, reading no ContextVar, and writes `suppressed = pending − 1` — the standalone row has no new refusal as a base and must stand for one itself — skipping the write entirely when `pending == 0`. Keys beyond the registry cap fold onto shared entries keyed `(tool, marker, scope)`, dropping only the principal.
- [x] 1.11 `src/services/indexer.py`: call `flush_expired()` once per periodic tick, logging and swallowing its failure — housekeeping must never fail a pass (the `quota_counters` prune precedent).
- [x] 1.12 `src/main.py`: call `flush_expired()` in the lifespan shutdown path **before `engine.dispose()`**, so the last window's counts land while a connection is still obtainable.
- [x] 1.13 `src/mcp_server/tools.py`: `_tracked` gains `write_class: bool = False` and `arg_char_caps: dict[str, int] | None`. Gate order — general bucket → write bucket (write tools only) → vault → unencodable-argument → `arg_char_caps` → quota → body (D3). Markers `rate_limited` and `argument_too_long` (pre-body) and `provider_input_rejected` (**post-body**), plus the `rate_limit_scope` (string) and `suppressed` (integer) params keys, each with the "which side of the body/no-body line" comment the register requires.
- [x] 1.14 `src/mcp_server/tools.py`: route every pre-body refusal through `refusals.render` — the three existing ones **additively** (prose unchanged, sentinel line appended) and the two new ones. `argument_too_long` writes its own row, **not** through the coalescer (D7). Update the existing pre-body-refusal assertions that compare whole strings.
- [x] 1.15 `src/mcp_server/tools.py`: annotate the tools — `write_class=True` on the eight mutating impls; `arg_char_caps={"query": MAX_SEARCH_QUERY_CHARS}` on `search_notes_impl` and `semantic_search_impl`; handle the provider input-limit exception from `refusals.py` in `semantic_search_impl`, rendering `argument_too_long` with the provider's reason and logging `provider_input_rejected`.
- [x] 1.16 `Dockerfile`: comment above the `CMD` binding `--workers 1` to these in-process controls — a second worker doubles every effective rate (D14).
- [x] 1.17 Tests `tests/test_mcp_auth_failure_budget.py`: parameterised over **every** 401 branch; threshold inclusivity at limit and limit+1; `Retry-After` arithmetic; the address-less shared slot; a refused probe opening **no session** (patch/count the session factory); window rollover; null disables; the address taken from the proxied client rather than a directly-read header; **100,000 distinct addresses** leave memory proportional to the table size and colliding addresses share a budget rather than escape one.
- [x] 1.18 Tests `tests/test_mcp_rate_limits.py`: general bucket sustained/burst; write bucket refuses writes while reads continue for the same principal; cross-principal isolation; admitted path issues **zero** SQL statements (statement-counting, as `tests/test_issue_162_quota_gate.py` does); OAuth principal is the `grant_id` — same across a refresh, **different across two grants of one client and user**; no-principal callers exempt; **a rate-refused call leaves `quota_counters` untouched** and the quota is still the last pre-body gate; refusal shape — `keyword_search` string contains the parseable sentinel line, `read_note` carries the identical text in its structured error field, futile refusals omit the retry field, existing prose preserved, and the 429 path carries **no** sentinel line; coalescing — refusals inside a window issue **no INSERT and no UPDATE**, scopes are not merged; **the arithmetic, case by case**: 1 refusal + flush → one row, `suppressed` 0, flush writes nothing, sum 1; 5 refusals + flush → rows with `suppressed` 0 and 3, sum exactly 5; 5 refusals, window closes, a 6th arrives → rollover row `suppressed` 4, sum exactly 6, `pending` reset; a mixed run of rollovers and a final flush sums exactly; **a flush after every ContextVar is cleared and after the credential row is deleted still writes the row with its captured actor columns and does not raise**; flush happens via the next row, via the indexer tick, and at shutdown before engine disposal; registry bounds — overflow shares one bucket and folds coalescer entries onto `(tool, marker, scope)` without affecting an in-cap principal, a depleted or unflushed entry is never evicted; boot validator rejects a half-configured bucket, an out-of-domain default, and every zero.
- [x] 1.19 Tests `tests/test_mcp_argument_caps.py`: 8,192 accepted, 8,193 refused naming `MAX_SEARCH_QUERY_CHARS` with **no embedding call** (patch `get_embedding`), no search statement and no quota statement; the query is not echoed; the usage row **is** written carrying `argument_too_long`, **one row per refusal, not coalesced**; a structured tool refuses in its own shape; **a stubbed provider raising the `refusals.py` input-limit exception** yields the same caller-facing `argument_too_long` code with the provider's reason while logging the distinct post-body `provider_input_rejected` marker.
- [x] 1.20 Tests `tests/test_limiter_settings_env.py`: write an actual env file and load `Settings` — empty, `null` and `None` resolve to None for every nullable limiter setting; zero rejected; a half-configured bucket and an out-of-domain default fail the load with messages naming the settings.

## 2. Slice B — observability (owns `src/services/usage_stats.py`, `docs/architecture/usage-attribution.md`, `tests/test_issue_160_refusal_predicate.py`, `tests/integration/test_issue_160_performance_pg.py`). **After Slice A merges.** Parallel with C, D and E.

- [x] 2.1 `src/services/usage_stats.py`: add `rate_limited` and `argument_too_long` to the enumerated pre-body refusal predicate, mirrored from `tools.py` in the existing direction (never an import back into `tools.py`). `provider_input_rejected` is **deliberately not added** — a comment says why.
- [x] 2.2 `src/services/usage_stats.py`: the per-tool refusal count sums `1 + suppressed` rather than counting rows, with a **guarded** integer cast (`~ '^[0-9]+$'`, the `result_count` precedent).
- [x] 2.3 `tests/test_issue_160_refusal_predicate.py`: pin the two new mirrored pairs byte-equal; assert the predicate still admits every post-body marker, `provider_input_rejected` included.
- [x] 2.4 `tests/integration/test_issue_160_performance_pg.py`: a window holding executed rows plus coalesced `rate_limited` rows and uncoalesced `argument_too_long` rows — percentiles from executed rows only, refusal count equal to summed `1 + suppressed`, a malformed `suppressed` rendering without raising, a `provider_input_rejected` row counted as executed, an unrelated `params.error` row still executed.
- [x] 2.5 `docs/architecture/usage-attribution.md`: extend the marker register with the two new pre-body values **and** `provider_input_rejected` on the post-body side, with the classification reasoning for each; record that `rate_limit_scope` is a string no reader casts and `suppressed` an integer read with a guarded cast; state that the refusal count is `1 + suppressed`, exact only after a flush, that `argument_too_long` is uncoalesced and why, that a deferred flush writes from a captured template through the existing 23503 recovery, and that an abrupt kill loses at most one interval per key.

## 3. Slice C — default quota surface and the quota clock read (owns `src/services/quotas.py`, `src/api/routes.py`, `src/control_panel/routes.py`, `src/control_panel/templates/keys.html`; new tests `tests/test_default_daily_limit.py`, additions to `tests/test_issue_162_quota_gate.py`). **After Slice A merges.** Parallel with B, D and E.

- [ ] 3.1 `src/services/quotas.py`: `Admission` carries the **decision instant** from `admit()`'s single clock read; `retry_after_seconds = ceil(reset_at − decided_at)`, minimum 1, computed once and never re-read (D10). `quota_refusal_message` renders its unchanged prose plus the sentinel line via `refusals.render`.
- [ ] 3.2 Tests in `tests/test_issue_162_quota_gate.py`: patch the clock so the decision lands milliseconds before UTC midnight and the message is built after it — the interval is the small one, not ~48 hours; assert no clock read occurs between the statement and the rendered refusal.
- [ ] 3.3 `src/api/routes.py`: key creation applies the default when `"daily_request_limit" not in req.model_fields_set`; explicit `null` still means unlimited; an explicit value wins. Update the request-model docstring.
- [ ] 3.4 `src/control_panel/routes.py`: `keys_page` passes the configured default to the template. The create handler performs **no** default substitution — a blank field is an explicit unlimited. The edit handler is untouched.
- [ ] 3.5 `src/control_panel/templates/keys.html`: pre-fill the create-form limit input with the passed default (empty when none); extend the help text — a default, changeable, clearable, empty means unlimited. Edit modal copy unchanged.
- [ ] 3.6 Tests `tests/test_default_daily_limit.py`: omitted vs explicit-null vs explicit-value on the JSON API; panel create with the pre-filled value, and cleared → unlimited **with no POST-side substitution**; **existing keys untouched**; `DEFAULT_DAILY_REQUEST_LIMIT=null` leaves the field empty and creates unlimited keys; the edit path never substitutes.

## 4. Slice D — provider input translation (owns `src/services/embeddings.py`; new tests `tests/test_provider_input_limit.py`). **After Slice A merges.** Parallel with B, C and E. Consumes only the exception type declared in Slice A's `src/services/refusals.py`, so A's tests never wait on D.

- [x] 4.1 `src/services/embeddings.py`: both providers detect their own input-limit rejection (OpenAI's documented input-too-large error; Ollama's equivalent where it reports one rather than truncating) and raise the `refusals.py` exception carrying the provider's stated reason, rather than a generic provider error.
- [x] 4.2 Tests `tests/test_provider_input_limit.py`: a stubbed provider returning each vendor's input-limit error shape yields the typed exception with the reason preserved; a generic provider error still propagates as today; a dense non-ASCII input under the character cap reaches the provider (the character cap does not pre-empt it). These stubs are the **deterministic** coverage of the translation branch and do not depend on which provider a deployment runs.

## 4b. Slice F — the write bucket at capability redemption (owns `src/transfer/routes.py`, `src/services/transfer.py`; new tests in `tests/test_transfer_routes.py`). **After Slice A merges.** Parallel with B, C, D and E — no file is shared with any of them.

- [ ] 4b.1 `src/services/transfer.py`: a helper deriving the **minting** principal from a `TransferToken` row plus the credential identity resolution already loads — `("api_key", key_id)`, or `("oauth", grant_id)` from the minting `OAuthToken` row that `resolve_identity_ok`'s credential lookup returns. No additional query, no schema change. The `ck_transfer_tokens_one_credential` invariant means the two branches are exclusive; a row with neither (single-user / sandbox) resolves to no principal and is exempt, matching `_tracked`'s rule.
- [ ] 4b.2 `src/transfer/routes.py` `upload()`: consume one write-bucket token for that principal **before any request body is read and before any bytes are staged**, in the phase-1 session window #208 established. A refusal returns **429** with `Retry-After` and calls `release_claim` — never `consume` — mirroring the `QueueTimeout` → 503 path, because a capability the server declined to serve is a promise still outstanding.
- [ ] 4b.3 Confirm no double charge: `request_upload` / `request_download` / `check_upload` remain unclassed and consume nothing; `import_from_url` keeps consuming the bucket at its tool call (Slice A) and is not charged again here.
- [ ] 4b.4 Tests in `tests/test_transfer_routes.py`: redemptions above the write rate → 429 + `Retry-After`, **claim released not consumed**, no bytes staged or published, and the same capability redeemable after the bucket refills; the tokens come from the **minting** principal, so a capability cannot spend the presenter's allowance; an API-key-minted and an OAuth-minted capability each resolve the right principal, the OAuth one with no extra query (statement-counting); minting alone consumes nothing; a token naming neither credential is exempt rather than refused.

## 5. Slice E — documentation and the recorded proxy decision (owns `docs/architecture/rate-limits.md` **new**, `CLAUDE.md`, `docs/architecture/search.md`, `docker-compose.yml`, `README.md`, `.env.example`). **After Slice A merges.** Parallel with B, C and D.

- [ ] 5.1 New `docs/architecture/rate-limits.md` carrying the load-bearing rationale verbatim from design: the principal is the grant (D1); the gate order and the "nothing durable is consumed by a call that does not run" invariant (D3); **velocity is not totality, and who has velocity bounds only** (D4); the one refusal shape and **why the transport 429 is outside it** (D5); the two halves of the query cap and why their markers differ (D6); coalescing, the captured row template, why `argument_too_long` is uncoalesced, and what a hard kill loses (D7); no I/O, no lock, no await (D8); the application-level new-key default (D9); the single clock read (D10); Traefik and the shared-egress residual (D11–D12); bounded registries and the shared-overflow limitation (D13); in-process state and **`--workers 1` as part of the contract** (D14); three markers (D16). Close with a section naming **what is deliberately absent** — concurrency — pointing at the follow-up and its shadow-mode plan.
- [ ] 5.2 `CLAUDE.md`: add the note to the architecture index with its "read before you touch" line; add the rate-limit facts to Key decisions (in-process, single worker, in-band parseable refusals, quota still last, velocity-only residual for OAuth and grandfathered keys, concurrency deferred).
- [ ] 5.3 `docs/architecture/search.md`: record `MAX_SEARCH_QUERY_CHARS` — where it is enforced, that it precedes the embedding call and the `tsquery` parse, that provider cost is *not* the reason, and that the provider's own token limit is handled separately in the body.
- [ ] 5.4 `docker-compose.yml`: a comment on the `obsidian-mcp-api-rtr` labels recording that a Traefik `ratelimit` middleware was considered and deliberately not added, with the reason — no labels change.
- [ ] 5.5 `.env.example` and `README.md`: every new setting with its default and one line on what it bounds, the documented "off" representation, and a README operations note showing what an agent sees when rate limited (the sentinel line).

## 6. Verification

- [ ] 6.1 Merge A, then B, C, D and E; run the **full** suite once on the merged result (`pytest`), plus `make audit`. This is the authoritative gate, not the per-worktree runs.
- [ ] 6.2 Grep for production callers of every new export in `src/services/rate_limits.py` and `src/services/refusals.py`, for both `flush_expired()` call sites, and for the write-bucket consumption in `src/transfer/routes.py` — fan-out ships green-but-unwired code, and a coalescer nobody flushes is exactly what this step catches.
- [ ] 6.3 `npx -y @fission-ai/openspec@1.3.1 validate --all --strict` clean.
- [ ] 6.4 `openspec-verifier` subagent against the merged tree; iterate to zero blocking gaps.
- [ ] 6.5 Adversarial Codex pass on the **implementation**. Frame it for this product: the expensive failures are a limiter that refuses a legitimate agent on the common path, one that returns something an agent cannot parse or mistakes for an empty result, a durable quota slot consumed by a call that never ran, a coalescer that loses or double-counts refusals or that raises during a shutdown flush, and any bypass by refreshing a token or minting a second grant. **Concurrency and pool interactions are out of scope for this change** and any finding there belongs on the `mcp-concurrency-slots` issue, not on this one.
- [ ] 6.6 `make deploy`; `make db-check` clean; `/health` green; watch `make logs` for the auth-failure WARNING and for boot-time settings validation.
- [ ] 6.7 Live MCP exercise against the deployed server, naming the tools called (there is no browser UI, so this substitutes for the `user-representative` pass): `keyword_search` with an 8,193-character query → refused naming `MAX_SEARCH_QUERY_CHARS`; `semantic_search` at 8,192 → normal result; a dense non-ASCII query under the cap → **conditional on the configured provider**: with `EMBEDDING_PROVIDER=openai` (which rejects oversized input) expect `argument_too_long` carrying the provider's reason; with the default Ollama, which truncates to the model context rather than rejecting, verify that it *is* truncation — a normal result, no refusal — and record that the translation branch is covered by the deterministic stub tests of Slice D rather than live; a burst of `read_note` on one key past its burst → `rate_limited` refusals whose sentinel line parses to a numeric retry-after, while a **second** credential's `list_notes` succeeds throughout; a burst of `create_note`/`delete_note` above the write bucket while that key's `read_note` still succeeds; `request_upload` several times then redeem the capabilities through `PUT /transfer/upload` faster than the write bucket → 429 with `Retry-After`, the claim released, no bytes published, and a successful redemption of the same capability after the bucket refills; an unauthenticated burst → 429 with `Retry-After` and no sentinel line; a key created through the panel carries 5,000 while the five pre-existing keys are still unlimited. Confirm on `/admin/performance` that refusals appear as refusal counts (summing `1 + suppressed`) and not in the percentiles, and that the provider-rejection row is inside the percentiles. Restart the container and confirm the shutdown flush landed the last window's counts. Delete the test notes.
- [ ] 6.8 Operator follow-up (not a code task): set a daily limit on the five existing production keys — D4's accepted limitation means they have velocity bounds only until one is set.
- [ ] 6.9 **File the `mcp-concurrency-slots` issue** before archiving, carrying forward: the pre-authentication in-flight ceilings (identity-blind, per-credential fingerprint, authentication-concurrency semaphore) with the pool arithmetic; the per-class / per-tenant / per-principal / global slot lattice with the "every tool has a class" reservation invariant; the `slot_timeout` refusal; the boot validator for the pool and class-sum relationships; the two carried-forward rejected findings (global-first acquisition, per-tenant reservation); and the requirement that it **ship in shadow mode first** — slots computed and would-be refusals recorded with a `shadow` flag before anything is enforced. Reference the three review rounds and #208.
- [ ] 6.10 `/openspec-archive-change`; PR with `Closes #194`, `Closes #188`; record on #194 the accepted residuals — **concurrency is not bounded by this change** (see the new issue); **expiring unused dynamically registered OAuth clients is not closed here**; **OAuth grants and grandfathered NULL-limit keys have velocity bounds only**; the transport 429 sits outside the in-band refusal contract; a hard kill loses at most one coalescing interval of refusal counts; past 10,000 principals the shared overflow entries lose per-principal attribution. Note #202 (indexer fairness) as the sibling still open.
