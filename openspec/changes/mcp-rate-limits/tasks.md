## 1. Slice A — enforcement path (owns `src/services/rate_limits.py` **new**, `src/config.py`, `src/auth/session.py`, `src/mcp_server/auth.py`, `src/mcp_server/tools.py`, `Dockerfile`; new tests `tests/test_mcp_rate_limits.py`, `tests/test_mcp_auth_failure_budget.py`, `tests/test_mcp_argument_caps.py`)

Everything that runs on the request path lives in one slice on purpose: the principal binding, the bucket, the slots, the failed-auth budget and the query cap all touch `_tracked` or `APIKeyMiddleware`, and splitting them would put two worktrees in the same two files. **Slices B, C and D all start after A merges.**

- [ ] 1.1 `src/config.py`: add `MAX_SEARCH_QUERY_CHARS = 8192` as a module constant beside `MAX_LIST_PATTERN_CHARS`, with the same style of comment (what it costs, why this number, where it is enforced). Add settings `mcp_rate_limit_per_minute: int | None = 120`, `mcp_rate_limit_burst: int | None = 30`, `mcp_max_concurrent_tool_calls: int = 12`, `mcp_max_concurrent_embedding_calls: int = 2`, `mcp_max_concurrent_vector_queries: int = 4`, `mcp_max_concurrent_writes: int = 4`, `mcp_max_concurrent_per_principal: int = 4`, `mcp_max_concurrent_embedding_per_principal: int = 1`, `mcp_slot_wait_seconds: float = 5.0`, `mcp_auth_failure_limit: int | None = 60`, `mcp_auth_failure_window_seconds: int = 300`, `default_daily_request_limit: int | None = 5000` (bounded by `DAILY_REQUEST_LIMIT_MIN`/`MAX`). All `ge=1` / `gt=0` so zero is rejected; null is the only disable.
- [ ] 1.2 `src/config.py`: a `model_validator` enforcing design D8 — per-principal ≤ global, embedding-per-principal ≤ embedding class, global ≥ max class limit — failing the boot with a message naming both settings involved.
- [ ] 1.3 `src/auth/session.py`: add `current_principal: ContextVar[tuple | None]` beside `current_actor` / `current_vault_root`, with the docstring explaining that it is the grant and not the token id (design D1).
- [ ] 1.4 `src/mcp_server/auth.py`: bind `current_principal` in both credential branches from the row already loaded — `("api_key", api_key.id)` and `("oauth", oauth_token.client_id, oauth_token.user_id)` — and `reset()` it in the existing `finally` beside the other seven. No new query.
- [ ] 1.5 `src/services/rate_limits.py` (new): `TokenBucket` (synchronous `take()` returning `(admitted, retry_after_seconds)`, monotonic clock, no lock, no `await` between read and write); the per-principal bucket registry with opportunistic eviction of **full, idle** buckets only (evicting a depleted bucket would grant free capacity — say so in the comment); the class/global `asyncio.Semaphore`s; the per-principal in-flight counters; `acquire_slots(principal, tool_class, deadline)` as an async context manager acquiring in the fixed order of D4 under one shared deadline (D5) and releasing in reverse in `finally`; the refusal-message builders naming control, ceiling and `retry_after_seconds`; a `reset_state_for_tests()` hook.
- [ ] 1.6 `src/mcp_server/tools.py`: `_tracked` gains `slot: str | None = None` and `arg_char_caps: dict[str, int] | None = None`. New gate order — bucket → vault → unencodable-argument → `arg_char_caps` → quota → slots → body → release (design D3; place the length screen next to `_first_unencodable_argument` so both are argument screens at the same altitude). Markers `_RATE_LIMITED_MARKER = "rate_limited"`, `_ARG_TOO_LONG_MARKER = "argument_too_long"` and the `rate_limit_scope` params key declared here with the "which side of the body/no-body line" comment the register requires.
- [ ] 1.7 `src/mcp_server/tools.py`: annotate the tools — `slot="embedding"` on `semantic_search_impl`; `slot="vector"` on `find_related_impl`; `slot="write"` on `create_note_impl`, `edit_note_impl`, `move_note_impl`, `delete_note_impl`, `set_frontmatter_impl`, `write_file_impl`, `delete_file_impl`, `import_from_url_impl`; `arg_char_caps={"query": MAX_SEARCH_QUERY_CHARS}` on `search_notes_impl` and `semantic_search_impl`. Every other tool unchanged.
- [ ] 1.8 `src/mcp_server/auth.py`: the per-IP failed-auth budget — a sliding-window counter checked **before** the credential lookup, incremented only on a 401 branch, keyed on `Request(scope).client.host` (established by `ProxyHeadersMiddleware`, which wraps the mount); over budget → 429 with `Retry-After` and the existing `WWW-Authenticate`; one WARNING `auth_failure_rate_limited` per window naming the address; null limit disables.
- [ ] 1.9 `Dockerfile`: comment above the `CMD` binding `--workers 1` to these in-process controls (design D7).
- [ ] 1.10 Tests `tests/test_mcp_rate_limits.py`: bucket admits at the sustained rate and refuses past the burst; **cross-principal isolation** (A exhausted, B's call still executes and is not delayed); the admitted path issues **zero** SQL statements (statement-counting, as `tests/test_issue_162_quota_gate.py` does); a rate refusal leaves `quota_counters` untouched; refusal text names the ceiling and a `retry_after_seconds ≥ 1`; `read_note` refuses through `refusal_result` in its structured shape; OAuth principal is bucketed and survives a token refresh (same principal tuple); no-principal callers exempt; slot acquisition order and the single shared deadline; a saturated class + exhausted deadline → typed refusal, no queue, body not run; a raising body releases every slot; per-principal embedding cap leaves a slot for a second principal; unclassed tools unaffected by a saturated `write` class; boot-time coherence validator rejects the contradictory sets and rejects zero.
- [ ] 1.11 Tests `tests/test_mcp_auth_failure_budget.py`: over-budget address → 429 + `Retry-After` **with no database session opened** (patch/count `async_session`); valid credential under budget unaffected and does not increment; window rollover; null limit disables; the address is taken from the proxied client, not a header read directly.
- [ ] 1.12 Tests `tests/test_mcp_argument_caps.py`: 8,192-char query accepted, 8,193 refused naming `MAX_SEARCH_QUERY_CHARS` with **no embedding call** (patch `get_embedding` and assert not called) and no search statement; the query text is not echoed; the refusal row carries `argument_too_long`; a structured tool refuses in its own shape via the same branch.

## 2. Slice B — observability (owns `src/services/usage_stats.py`, `docs/architecture/usage-attribution.md`, `tests/test_issue_160_refusal_predicate.py`, `tests/integration/test_issue_160_performance_pg.py`). **Starts after Slice A merges** — it consumes the marker constants A defines.

- [ ] 2.1 `src/services/usage_stats.py`: add `rate_limited` and `argument_too_long` to the enumerated pre-body refusal predicate, mirrored from `tools.py` in the same direction the existing string markers are mirrored (never an import back into `tools.py`, which would close the cycle).
- [ ] 2.2 `tests/test_issue_160_refusal_predicate.py`: pin the new mirrored pairs byte-equal, and assert the predicate still admits the post-body markers (`vault_assignment_changed`, `vault_confirmation_unavailable`, `vault_anchor_lost_at_publish`, `related_source_not_found`, `related_source_not_embedded`) into the aggregates.
- [ ] 2.3 `tests/integration/test_issue_160_performance_pg.py`: a window holding executed rows plus `rate_limited` and `argument_too_long` rows — percentiles from the executed rows only, refusals counted per tool; a row carrying an unrelated `params.error` still counted as executed.
- [ ] 2.4 `docs/architecture/usage-attribution.md`: extend the marker register with both new values on the pre-body side, state the classification reasoning, and record that `rate_limit_scope` is a **string** params key that no reader casts — explicitly outside the reserved `embed_ms` / `db_ms` / `over_quota` hazard.

## 3. Slice C — default quota surface (owns `src/services/quotas.py`, `src/api/routes.py`, `src/control_panel/routes.py`, `src/control_panel/templates/keys.html`; new tests `tests/test_default_daily_limit.py`). **Starts after Slice A merges** — it consumes `settings.default_daily_request_limit`. Runs in parallel with B and D.

- [ ] 3.1 `src/services/quotas.py`: one shared `resolve_default_daily_limit(chosen, chosen_was_provided)` so the two creation paths cannot drift, with the docstring recording that the default is application-level and never a column default (design D12).
- [ ] 3.2 `src/api/routes.py`: `POST` key creation applies the default when `"daily_request_limit" not in req.model_fields_set`; an explicit `null` still means unlimited; an explicit value wins. Update the request-model docstring, which currently documents only the null case.
- [ ] 3.3 `src/control_panel/routes.py`: the create handler applies the default when the form field is left at its pre-filled value or blank per the resolved rule; the **edit** handler is untouched.
- [ ] 3.4 `src/control_panel/templates/keys.html`: pre-fill the create-form limit input with the configured default (empty when none), and extend the help text to say it is a default, that it may be changed or cleared, and that empty means unlimited. Edit modal copy unchanged.
- [ ] 3.5 Tests `tests/test_default_daily_limit.py`: omitted vs explicit-null vs explicit-value on the JSON API; panel create with the pre-filled value and with it cleared; **existing keys untouched** (a NULL-limit key still issues no quota statement and behaves identically after the setting is introduced); `DEFAULT_DAILY_REQUEST_LIMIT=null` restores today's behaviour; a default outside 1..1,000,000 is refused at boot; the edit path never substitutes the default.

## 4. Slice D — documentation and the recorded proxy decision (owns `docs/architecture/rate-limits-and-concurrency.md` **new**, `CLAUDE.md`, `docs/architecture/search.md`, `docker-compose.yml`, `README.md`, `.env.example`). **Starts after Slice A merges.** Runs in parallel with B and C.

- [ ] 4.1 New `docs/architecture/rate-limits-and-concurrency.md` carrying the load-bearing rationale verbatim from design: the principal is the grant not the token; gate ordering and why the bucket is first and the quota last; the fixed slot order and the hold-only-narrow rule; one deadline not four; no I/O, no lock, no await on the common path; in-process state and **`--workers 1` as part of the contract**; the marker classification; why Traefik is the wrong instrument for a 401 budget; the shared-egress residual; why the new-key default is application-level.
- [ ] 4.2 `CLAUDE.md`: add the note to the architecture index table with its "read before you touch" line, and add the rate-limit facts to the Key decisions list (in-process, single worker, in-band typed refusals).
- [ ] 4.3 `docs/architecture/search.md`: record `MAX_SEARCH_QUERY_CHARS` — where it is enforced, that it precedes the embedding call and the tsquery parse, and that the provider-cost argument is *not* the reason (see design D9).
- [ ] 4.4 `docker-compose.yml`: a comment on the `obsidian-mcp-api-rtr` labels recording that a Traefik `ratelimit` middleware was considered and deliberately not added, with the reason — no labels change.
- [ ] 4.5 `.env.example` and `README.md`: the new settings with their defaults and one line each on what they bound; the README's operations section gains "what an agent sees when it is rate limited".

## 5. Verification

- [ ] 5.1 Merge A, then B, C and D; run the **full** suite once on the merged result (`pytest`), plus `make audit`. This is the authoritative gate, not the per-worktree runs.
- [ ] 5.2 Grep for production callers of every new export in `src/services/rate_limits.py` — fan-out ships green-but-unwired code, and a limiter nothing calls is the exact failure this step exists to catch.
- [ ] 5.3 `npx -y @fission-ai/openspec@1.3.1 validate --strict` clean.
- [ ] 5.4 `openspec-verifier` subagent against the merged tree; iterate to zero blocking gaps.
- [ ] 5.5 Adversarial Codex pass (mandatory — this change edits the tool execution path and the admission middleware). Frame it for this product: the expensive failures are a limiter that **refuses a legitimate agent** on the common path, one that silently returns an empty result instead of a typed refusal, a slot leak that wedges the single worker, a deadlock in the acquisition order, and a control that can be bypassed by refreshing an OAuth token. Fix BLOCKER/MAJOR; record accepted limitations. Re-run after each round, naming what was addressed; if successive rounds keep producing *new classes* of finding, ship the concurrency controls disabled by configuration rather than tune thresholds for another round.
- [ ] 5.6 `make deploy`; `make db-check` clean (no migration, but the check is the standing gate); `/health` green; watch `make logs` for `auth_failure_rate_limited` and for boot-time settings validation.
- [ ] 5.7 Live MCP exercise against the deployed server, naming the tools actually called (there is no browser UI, so this substitutes for the `user-representative` pass): `keyword_search` with a 8,193-character query → refused naming `MAX_SEARCH_QUERY_CHARS`; `semantic_search` at 8,192 → normal result; a scripted burst of `read_note` on one key past the burst capacity → in-band `rate_limited` refusals carrying `retry_after_seconds`, while a **second** credential's `list_notes` succeeds throughout; concurrent `semantic_search` beyond the embedding class → one typed slot refusal with retry-after; `create_note` + `edit_note` + `delete_note` under a saturated `write` class; an unauthenticated burst against `/mcp` → 429 with `Retry-After`; a key created through the panel carries 5,000 while the five pre-existing keys are still unlimited. Then confirm on `/admin/performance` that the refusals appear as refusal counts and not in the percentiles, and delete the test notes.
- [ ] 5.8 Operator follow-up (not a code task): set a daily limit on the five existing production keys, per #194's recommendation.
- [ ] 5.9 `/openspec-archive-change`; PR with `Closes #194`, `Closes #188`; record on #194 the accepted residual — **expiring unused dynamically registered OAuth clients is not closed by this change** and belongs with the token-cleanup job; note #202 (indexer fairness) as the sibling still open.
