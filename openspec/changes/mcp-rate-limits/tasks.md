## 1. Slice A — enforcement path (owns `src/services/refusals.py` **new**, `src/services/rate_limits.py` **new**, `src/config.py`, `src/auth/session.py`, `src/mcp_server/auth.py`, `src/mcp_server/tools.py`, `Dockerfile`; new tests `tests/test_mcp_rate_limits.py`, `tests/test_mcp_admission_gates.py`, `tests/test_mcp_argument_caps.py`, `tests/test_limiter_settings_env.py`, `tests/integration/test_mcp_pool_availability_pg.py`)

Everything on the request path lives in one slice on purpose: the admission gates, the principal binding, the buckets, the slots, the refusal shape and the query cap all touch `_tracked` or `APIKeyMiddleware`, and splitting them would put two worktrees in the same two files. **Slices B, C and D all start after A merges.**

- [ ] 1.1 `src/config.py`: `MAX_SEARCH_QUERY_CHARS = 8192` as a module constant beside `MAX_LIST_PATTERN_CHARS`, same comment style (what it costs, why this number, where it is enforced).
- [ ] 1.2 `src/config.py`: settings — `mcp_max_inflight_requests: int = 64`, `mcp_auth_failure_limit: int | None = 60`, `mcp_auth_failure_window_seconds: int = 300`, `mcp_auth_failure_table_size: int = 4096`, `mcp_rate_limit_per_minute: int | None = 120`, `mcp_rate_limit_burst: int | None = 30`, `mcp_write_rate_limit_per_minute: int | None = 60`, `mcp_write_rate_limit_burst: int | None = 15`, `mcp_max_concurrent_tool_calls: int = 12`, `mcp_max_concurrent_embedding_calls: int = 2`, `mcp_max_concurrent_vector_queries: int = 4`, `mcp_max_concurrent_writes: int = 4`, `mcp_max_concurrent_per_principal: int = 4`, `mcp_max_concurrent_embedding_per_principal: int = 1`, `mcp_max_concurrent_embedding_per_user: int = 1`, `mcp_slot_wait_seconds: float = 5.0`, `mcp_limiter_max_tracked_principals: int = 10_000`, `mcp_refusal_log_interval_seconds: int = 10`, `default_daily_request_limit: int | None = 5000`. All `ge=1` / `gt=0` so zero is rejected.
- [ ] 1.3 `src/config.py`: **one** shared `BeforeValidator` mapping an empty value, `null` or `none` (stripped, case-insensitive) to `None`, applied to every nullable limiter setting — no per-field variants (design D14).
- [ ] 1.4 `src/config.py`: `model_validator` enforcing D14 in full — global ≥ **sum** of the three class ceilings; per-principal ≤ global; embedding-per-principal ≤ embedding-per-user ≤ embedding ceiling; in-flight ≥ global; each bucket's rate and burst both set or both null; `default_daily_request_limit` inside `DAILY_REQUEST_LIMIT_MIN`..`MAX`. Errors name the settings and, for the sum rule, the sum.
- [ ] 1.5 `src/services/refusals.py` (new, importing nothing from the app so `tools.py`, `quotas.py` and `rate_limits.py` can all use it without a cycle): the `Refusal` dataclass (`code`, `scope`, `limit`, `limit_unit`, `retry_after_seconds`), the closed `code` set, and `render(prose, refusal) -> str` appending the single sentinel line. `retry_after_seconds` omitted for `no_vault_assigned` / `argument_not_encodable`; always ≥ 1 elsewhere.
- [ ] 1.6 `src/auth/session.py`: `current_principal: ContextVar[tuple | None]` beside `current_actor` / `current_vault_root`, docstring recording that the OAuth key is `grant_id` and why (design D3, issue #64).
- [ ] 1.7 `src/mcp_server/auth.py`: the in-flight admission ceiling — integer incremented at entry of `__call__`, decremented in `finally`, checked **before** `Request(scope)` does any work and before any `async_session()`; over ceiling → 503 + `Retry-After`, no query, no usage row.
- [ ] 1.8 `src/mcp_server/auth.py`: the failed-auth budget — fixed-size table of `mcp_auth_failure_table_size` counters indexed by a **per-process random salt** + hash of the proxied client address; checked before the credential lookup; refuse when the recorded count is already ≥ the limit; increment on **every** 401 branch (missing bearer, unknown credential, ownerless, inactive user, expired, cross-user grant, no vault scope); address-less requests charged to one shared reserved slot; `Retry-After` = whole seconds remaining in the window, minimum 1; one WARNING `auth_failure_rate_limited` per slot per window; null limit disables. One helper for the threshold and the `Retry-After` arithmetic so the two cannot drift.
- [ ] 1.9 `src/mcp_server/auth.py`: bind `current_principal` in both branches from the row already loaded — `("api_key", api_key.id)` and `("oauth", oauth_token.grant_id)` — and `reset()` it in the existing `finally`. No new query.
- [ ] 1.10 `src/services/rate_limits.py` (new): `TokenBucket` (synchronous `take()` → `(admitted, retry_after_seconds)`, monotonic clock, no lock, no `await` between read and write); the general and write bucket registries keyed on principal; the bounded registry (hard cap, amortised TTL sweep on insert, **full-and-idle-only** eviction that skips depleted entries and entries with in-flight holders or waiters, shared overflow bucket past the cap); the class/global `asyncio.Semaphore`s plus per-principal, per-principal-class and per-tenant-class counters; `acquire_slots(principal, user_id, tool_class, deadline)` as an async context manager acquiring in D6's fixed order under **one** shared deadline and releasing in reverse in `finally`; the refusal builders returning `Refusal`s; the coalescing bookkeeper for `rate_limited` / `slot_timeout` rows (`(principal, tool, marker)` → last-written instant + pending `suppressed`); `reset_state_for_tests()`.
- [ ] 1.11 `src/mcp_server/tools.py`: `_tracked` gains `slot: str | None = None` and `arg_char_caps: dict[str, int] | None = None`. New gate order — general bucket → write bucket (write class only) → vault → unencodable-argument → `arg_char_caps` → **slots** → quota → body — with the slots held across the quota gate and released in the `finally` that also covers the body (design D5, BLOCKER 2). Markers `_RATE_LIMITED_MARKER = "rate_limited"`, `_SLOT_TIMEOUT_MARKER = "slot_timeout"`, `_ARG_TOO_LONG_MARKER = "argument_too_long"`, plus the `rate_limit_scope` (string) and `suppressed` (integer) params keys, each with the "which side of the body/no-body line" comment the register requires.
- [ ] 1.12 `src/mcp_server/tools.py`: route every pre-body refusal through `refusals.render` — the three existing ones **additively** (prose unchanged, sentinel line appended; `over_quota` gains `retry_after_seconds` derived from `Admission.reset_at`, never a second clock read) and the three new ones. Update the existing pre-body-refusal assertions that compare whole strings.
- [ ] 1.13 `src/mcp_server/tools.py`: coalesced refusal logging for `rate_limited` and `slot_timeout` — at most one row per `(principal, tool, marker)` per `mcp_refusal_log_interval_seconds`, the written row carrying `suppressed` as a JSON integer so `1 + suppressed` totals exactly.
- [ ] 1.14 `src/mcp_server/tools.py`: annotate the tools — `slot="embedding"` on `semantic_search_impl`; `slot="vector"` on `find_related_impl`; `slot="write"` on `create_note_impl`, `edit_note_impl`, `move_note_impl`, `delete_note_impl`, `set_frontmatter_impl`, `write_file_impl`, `delete_file_impl`, `import_from_url_impl` (the same eight also gate on the write bucket); `arg_char_caps={"query": MAX_SEARCH_QUERY_CHARS}` on `search_notes_impl` and `semantic_search_impl`. Every other tool unchanged.
- [ ] 1.15 `Dockerfile`: comment above the `CMD` binding `--workers 1` to these in-process controls (design D13).
- [ ] 1.16 Tests `tests/test_mcp_admission_gates.py`: in-flight ceiling refuses with 503 + `Retry-After` **before any `async_session()`** (patch/count the session factory), releases on completion and on a raising handler, and is validated ≥ the global ceiling; failed-auth budget parameterised over **every** 401 branch, threshold inclusivity at limit and limit+1, `Retry-After` arithmetic, the address-less shared slot, a refused probe opening no session, window rollover, null disables, and that the address is the proxied one rather than a directly-read header.
- [ ] 1.17 Tests `tests/test_mcp_rate_limits.py`: general bucket sustained/burst behaviour; write bucket refuses writes while reads continue for the same principal; cross-principal isolation (A exhausted, B executes undelayed); admitted path issues **zero** SQL statements (statement-counting, as `tests/test_issue_162_quota_gate.py` does); OAuth principal is the `grant_id` — same across a refresh, **different across two grants of one client and user**; no-principal callers exempt; slot acquisition order and the single shared deadline (elapsed ≈ budget, not a multiple, with tolerance); saturated class + exhausted deadline → `slot_timeout` refusal, body not run, nothing queued; **quota refusal releases slots**; **slot timeout leaves `quota_counters` untouched**; raising body releases every slot; per-tenant embedding cap — tenant A with an API key *and* an OAuth grant cannot hold both embedding slots while tenant B is waiting; unclassed tools unaffected by a saturated `write` class; refusal shape — `keyword_search` string contains the parseable sentinel line, `read_note` carries the identical text in its structured error field, futile refusals omit `retry_after_seconds`, existing prose preserved; coalescing — 1,200 refusals in one interval write a bounded number of rows whose `1 + suppressed` sums to 1,200; registry bounds — 100,000 distinct principals stay under the cap, overflow shares one bucket and refuses no legitimate credential, a depleted or busy entry is never evicted; boot validator rejects each incoherent set and every zero.
- [ ] 1.18 Tests `tests/test_mcp_argument_caps.py`: 8,192 accepted, 8,193 refused naming `MAX_SEARCH_QUERY_CHARS` with **no embedding call** (patch `get_embedding` and assert not called), no search statement and no quota statement; the query text is not echoed; the usage row **is** written carrying `argument_too_long`; a structured tool refuses in its own shape through the same branch.
- [ ] 1.19 Tests `tests/test_limiter_settings_env.py`: write an actual env file and load `Settings` from it — empty value, `null` and `None` all resolve to None for every nullable limiter setting; a zero is rejected; the sum rule and the per-scope rules fail the load with messages naming the settings.
- [ ] 1.20 Tests `tests/integration/test_mcp_pool_availability_pg.py`: under a flood of concurrent **valid-credential** `/mcp` requests far exceeding `MCP_MAX_INFLIGHT_REQUESTS`, a second tenant's request obtains a connection and is served, and `engine.pool` never reaches exhaustion — the cross-tenant availability property the whole change exists for.

## 2. Slice B — observability (owns `src/services/usage_stats.py`, `docs/architecture/usage-attribution.md`, `tests/test_issue_160_refusal_predicate.py`, `tests/integration/test_issue_160_performance_pg.py`). **Starts after Slice A merges** — it consumes the marker constants A defines.

- [ ] 2.1 `src/services/usage_stats.py`: add `rate_limited`, `slot_timeout` and `argument_too_long` to the enumerated pre-body refusal predicate, mirrored from `tools.py` in the same direction the existing string markers are mirrored (never an import back into `tools.py`, which would close the cycle).
- [ ] 2.2 `src/services/usage_stats.py`: the per-tool refusal count sums `1 + suppressed` rather than counting rows, with a **guarded** integer cast (`~ '^[0-9]+$'`, the `result_count` precedent) so a malformed value cannot 500 the window for every user.
- [ ] 2.3 `tests/test_issue_160_refusal_predicate.py`: pin the three new mirrored pairs byte-equal; assert the predicate still admits every post-body marker into the aggregates.
- [ ] 2.4 `tests/integration/test_issue_160_performance_pg.py`: a window holding executed rows plus coalesced `rate_limited` / `slot_timeout` / `argument_too_long` rows — percentiles from the executed rows only, refusal count equal to the summed `1 + suppressed`, a malformed `suppressed` rendering without raising, and a row carrying an unrelated `params.error` still counted as executed.
- [ ] 2.5 `docs/architecture/usage-attribution.md`: extend the marker register with the three new pre-body values and the classification reasoning; record that `rate_limit_scope` is a **string** no reader casts and that `suppressed` is an **integer** read with a guarded cast; state that the refusal count is `1 + suppressed`, not a row count, and that every reader of these markers must move together.

## 3. Slice C — default quota surface (owns `src/services/quotas.py`, `src/api/routes.py`, `src/control_panel/routes.py`, `src/control_panel/templates/keys.html`; new tests `tests/test_default_daily_limit.py`). **Starts after Slice A merges** — it consumes `settings.default_daily_request_limit` and `src/services/refusals.py`. Runs in parallel with B and D.

- [ ] 3.1 `src/services/quotas.py`: `quota_refusal_message` returns its prose plus the sentinel line via `refusals.render`, with `retry_after_seconds` derived from the `Admission.reset_at` it is already handed — never a second clock read.
- [ ] 3.2 `src/api/routes.py`: key creation applies the default when `"daily_request_limit" not in req.model_fields_set`; explicit `null` still means unlimited; an explicit value wins. Update the request-model docstring, which currently documents only the null case.
- [ ] 3.3 `src/control_panel/routes.py`: `keys_page` passes the configured default to the template. The create handler performs **no** default substitution — a blank field is an explicit unlimited. The edit handler is untouched.
- [ ] 3.4 `src/control_panel/templates/keys.html`: pre-fill the create-form limit input with the passed default (empty when none) and extend the help text — it is a default, it may be changed or cleared, an empty field means unlimited. Edit modal copy unchanged.
- [ ] 3.5 Tests `tests/test_default_daily_limit.py`: omitted vs explicit-null vs explicit-value on the JSON API; panel create with the pre-filled value, and cleared → unlimited **with no POST-side substitution**; **existing keys untouched** (a NULL-limit key still issues no quota statement after the setting is introduced); `DEFAULT_DAILY_REQUEST_LIMIT=null` leaves the field empty and creates unlimited keys; a default outside 1..1,000,000 refuses the boot; the edit path never substitutes.

## 4. Slice D — documentation and the recorded proxy decision (owns `docs/architecture/rate-limits-and-concurrency.md` **new**, `CLAUDE.md`, `docs/architecture/search.md`, `docker-compose.yml`, `README.md`, `.env.example`). **Starts after Slice A merges.** Runs in parallel with B and C.

- [ ] 4.1 New `docs/architecture/rate-limits-and-concurrency.md` carrying the load-bearing rationale verbatim from design: why a limiter that starts after authentication is not one (D1); coalescing rather than silence (D2); the principal is the grant, not the token and not client+user (D3); the two gate-order rules — a token is not a slot, nothing durable is consumed by a call that does not run (D5); the fixed slot order, the hold-only-narrow rule and why the sum relationship is **enforced** (D6); the per-tenant cap and why a reservation was rejected (D7); velocity is not totality (D8); the one refusal shape and its composition with both tool output types (D9); no I/O, no lock, no await (D10); one deadline (D11); bounded registries, full-and-idle eviction, shared overflow, salted fixed table (D12); in-process state and **`--workers 1` as part of the contract** (D13); one representation for "off" (D14); why the query cap is in the decorator (D15); why Traefik is the wrong instrument and the shared-egress residual (D16–D18); the application-level new-key default (D19); three markers, not one (D20).
- [ ] 4.2 `CLAUDE.md`: add the note to the architecture index table with its "read before you touch" line, and add the rate-limit facts to Key decisions (in-process, single worker, in-band parseable refusals, slots before quota).
- [ ] 4.3 `docs/architecture/search.md`: record `MAX_SEARCH_QUERY_CHARS` — where it is enforced, that it precedes the embedding call and the `tsquery` parse, and that provider cost is *not* the reason (design D15).
- [ ] 4.4 `docker-compose.yml`: a comment on the `obsidian-mcp-api-rtr` labels recording that a Traefik `ratelimit` middleware was considered and deliberately not added, with the reason — no labels change.
- [ ] 4.5 `.env.example` and `README.md`: every new setting with its default, one line each on what it bounds, and the documented "off" representation; the README's operations section gains "what an agent sees when it is rate limited", showing the sentinel line.

## 5. Verification

- [ ] 5.1 Merge A, then B, C and D; run the **full** suite once on the merged result (`pytest`), plus `make audit`. This is the authoritative gate, not the per-worktree runs.
- [ ] 5.2 Grep for production callers of every new export in `src/services/rate_limits.py` and `src/services/refusals.py` — fan-out ships green-but-unwired code, and a limiter nothing calls is exactly what this step catches.
- [ ] 5.3 `npx -y @fission-ai/openspec@1.3.1 validate --all --strict` clean.
- [ ] 5.4 `openspec-verifier` subagent against the merged tree; iterate to zero blocking gaps.
- [ ] 5.5 Adversarial Codex pass on the **implementation** (mandatory — this change edits the tool execution path and the admission middleware). Frame it for this product: the expensive failures are a limiter that refuses a legitimate agent on the common path, one that returns something an agent cannot parse or mistakes for an empty result, a slot leak or deadlock that wedges the single worker, a durable quota slot consumed by a call that never ran, and any bypass by refreshing a token or minting a second grant. Re-run after each round naming what was addressed; if successive rounds keep producing *new classes* of finding, ship the concurrency layer disabled by configuration rather than tune thresholds for another round.
- [ ] 5.6 `make deploy`; `make db-check` clean (no migration, but the check is the standing gate); `/health` green; watch `make logs` for `auth_failure_rate_limited` and for boot-time settings validation.
- [ ] 5.7 Live MCP exercise against the deployed server, naming the tools actually called (there is no browser UI, so this substitutes for the `user-representative` pass): `keyword_search` with an 8,193-character query → refused naming `MAX_SEARCH_QUERY_CHARS`; `semantic_search` at 8,192 → normal result; a scripted burst of `read_note` on one key past the burst capacity → in-band `rate_limited` refusals whose sentinel line parses to a numeric `retry_after_seconds`, while a **second** credential's `list_notes` succeeds throughout; a burst of `create_note`/`delete_note` above the write bucket while that key's `read_note` still succeeds; concurrent `semantic_search` from one tenant's API key and OAuth grant plus a second tenant's → the second tenant is served and one call returns `slot_timeout`; an unauthenticated burst → 429 with `Retry-After`; a concurrent valid-credential flood → 503s while `/health` and another tenant's `get_recent` stay served. Then confirm on `/admin/performance` that the refusals appear as refusal counts (summing `1 + suppressed`) and not in the percentiles, and delete the test notes.
- [ ] 5.8 Operator follow-up (not a code task): set a daily limit on the five existing production keys, per #194's recommendation.
- [ ] 5.9 `/openspec-archive-change`; PR with `Closes #194`, `Closes #188`; record on #194 the accepted residual — **expiring unused dynamically registered OAuth clients is not closed by this change** and belongs with the token-cleanup job; note #202 (indexer fairness) as the sibling still open.
