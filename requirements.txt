# Core
fastapi==0.135.1
uvicorn[standard]==0.42.0
# 0.0.30 fixes CVE-2026-53539 (fixable HIGH flagged by the trivy deploy gate).
python-multipart==0.0.31

# Database
sqlalchemy[asyncio]==2.0.48
asyncpg==0.31.0
alembic==1.18.4
pgvector==0.4.2

# MCP
# 1.28.1 fixes CVE-2026-52869, CVE-2026-52870, and CVE-2026-59950
# (fixable HIGH findings enforced by the Trivy deploy gate).
mcp[cli]==1.28.1

# Embeddings & search
httpx==0.28.1

# YAML parsing
pyyaml==6.0.3

# Settings
pydantic-settings==2.14.2

# Templates
jinja2==3.1.6

# Security
itsdangerous==2.2.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 probes bcrypt with >72 byte passwords on init; bcrypt 4.1+
# raises instead of warning, so pin to the last compatible release.
bcrypt<4.1

# Rate limiting (Phase 8)
slowapi==0.1.9

# Dev/audit
pip-audit  # dev/audit

# File watching (Phase 6)
# watchfiles==1.0.5

# Transitive pin for CVE-2026-44431 / CVE-2026-44432 in urllib3 < 2.7.0
urllib3>=2.7.0
